# Pipeline Status

> Tier-1 read every session. Current task + any [CHECKPOINT] lines live here.
> Convention copied from D:\ai_whatsapp_agent\docs\FR\_pipeline_status.md.

| Module / Feature | Status | Notes |
|---|---|---|
| 🟡 **Inline order-confirmation send (ADR-001 reversal) — BUILT, IN REVIEW (2026-08-15)** — TDD, committed locally on top of `bc4a127`, NOT pushed | **Owner-directed reversal:** the external scheduler (cron-job.org) driving `/internal/jobs/outbox_drain` stopped working, so the PRIMARY confirmation send moves back INTO the `orders/create` webhook as a genuine INLINE `await` (NOT `BackgroundTasks`/`create_task` — those don't reliably run after the response on Vercel Python, the reason the 2026-08-13 self-invoke attempt was reverted). The ack is legitimately delayed by the send, bounded by a SHORT `_INLINE_SEND_TIMEOUT_SECONDS = 3.0` (distinct from the cron's 20s default) so the whole handler stays under Shopify's <5s ack budget. **What shipped (5 app files, TDD RED→GREEN):** `IngestResult.outbound_id: int | None` re-added (Postgres `INSERT ... ON CONFLICT DO NOTHING RETURNING id` via `fetchval`, in-memory `_OutboundRow.id`; None on conflict/no-draft) — `app/store/{base,memory,postgres}.py`; new `IngestStore.claim_outbound_by_id(id) -> OutboundClaim | None` — atomic single-row `WHERE id=$1 AND state='queued' FOR UPDATE SKIP LOCKED` flip to `processing` (memory + postgres), so the inline path touches ONLY the row it just created and can never collide with the still-live backstop `claim_queued_outbound`/drain (no double-send); `send_one_outbound` gained optional `timeout: float | None = None` (None = cron 20s default UNCHANGED, passed through to `send_template` only when supplied) + new `send_inline_outbound(c, outbound_id)` orchestrator in `app/jobs/outbox_drain.py` (loads controls fresh, `send_mode=='off'` leaves row queued, claims by id, sends with the short timeout, NEVER raises); `channels/shopify_webhook.py` `await`s `send_inline_outbound(c, result.outbound_id)` AFTER the DB writes + best-effort mirror, BEFORE the 200 — one import, no `admin.controls` import in the edge (orchestration lives in the job module, same layering discipline as the Phase-4 `core.conversation` extraction). **Safety invariants held:** `send_mode` kill switch enforced exactly as elsewhere (off/shadow/allowlist-miss → zero Meta calls); a send failure/timeout/`WhatsAppSendError`/unexpected error NEVER turns the ack into a 5xx (row bumped back to `queued` for the backstop, or left `processing` for the drain's 10-min stale reclaim); `/internal/jobs/outbox_drain` kept as manual/backstop; `reminders.py` untouched. **Tests:** +5 webhook (inline fires within the request, 200 on send failure, 200 on unexpected error, shadow suppresses, single-row isolation vs an unrelated queued row) +2 drain (default 20s vs short 3s timeout) +6 store (outbound_id populated/None, claim-by-id claims only its row / None when re-claimed / None unknown, +1 gated PG atomic-claim). **798 passed, 35 skipped** (was 786/34: +12 non-gated, +1 gated PG that skips w/o `TEST_DATABASE_URL`, which stays UNSET); `ruff check .` + `mypy app` strict (64 files) + secrets/print/bare-except grep on all 5 touched app files clean; `git diff -- app/core/order_actions.py` EMPTY; `tests/core/test_button_dispatch.py` green (23). ADR-001 amended (dated section, history preserved), error_learnings + both registries updated. **Next: code-reviewer + security-reviewer (webhook send path + mutation-adjacent; MANDATORY per routing) → fix findings → push after owner approval.** |
| 🟢 **Shopify webhook delivery — CUTOVER TO ADMIN-CREATED WEBHOOKS COMPLETE (2026-08-14)** | ✅ Deployed (`ddd7952`, live) + owner set up Admin-created webhooks (Settings → Notifications → Webhooks, all 5 topics → `/webhooks/shopify`, signed with the store's webhook signing secret, rotated first per the security-review precondition) + verified via live traffic (mix of 200s incl. a real `customer mirror upsert skipped as stale` event, confirming the dual-secret HMAC path genuinely authenticates Admin-created deliveries) → **the 3 app-managed subscriptions (`ORDERS_CREATE`/`ORDERS_UPDATED`/`CUSTOMERS_UPDATE`, all pointed at the same URL) were DELETED via `webhookSubscriptionDelete` (2026-08-14) to stop duplicate delivery/double-processing** — confirmed `webhookSubscriptions` now returns zero edges. **✅ SELF-HEAL CODE REMOVED (2026-08-15, committed locally on top of `044dc66`, NOT pushed):** `ensure_subscription` and its helpers/GraphQL strings, the `_job_ensure_subscription` handler + its `ensure_subscription` entry in `jobs.router.JOBS` (so `/internal/jobs/ensure_subscription` now 404s), and `tests/test_subscriptions.py` were DELETED — the job can no longer recreate the deleted subscriptions and reintroduce duplicate delivery. `app/shopify/subscriptions.py` now holds ONLY the `REQUIRED_TOPICS` tuple, which the untouched receiver `channels/shopify_webhook.py` still imports to derive `HANDLED_TOPICS` (that import + the live receiver + dual-secret HMAC are all unchanged; receipt/verification of webhooks is unaffected — only the subscription-CREATION capability was removed). Suite 786 passed / 34 skipped (was 795/34: −8 test_subscriptions, −1 ensure_subscription job test); ruff + mypy strict (64 files) + secrets grep on both touched app files clean; `git diff -- app/core/order_actions.py` and `app/channels/shopify_webhook.py` both EMPTY; `tests/core/test_button_dispatch.py` green (23). `read_fulfillments`/`read_marketplace_orders` scope still not required under this model (Admin-created webhooks aren't gated by app API scopes) — fulfillment tracking webhooks can now be added the same way regardless of the app's granted scopes. |
| 🟡 **Shopify webhook dual-secret HMAC (Admin-created webhook cutover) — BUILT, IN REVIEW (2026-08-14)** — TDD, committed locally on top of `66b9d27`, NOT pushed | **Capability:** accept webhooks signed with EITHER the app's `shopify:client_secret` (app-created subscriptions) OR the per-store `shopify:webhook_signing_secret` (webhooks the owner creates in Admin → Settings → Notifications), so both delivery sources verify during the cutover — additive, no atomic switch, `subscriptions.ensure_subscription` untouched. **What shipped (2 app files + panel, TDD RED→GREEN):** new `_hmac_verifies(c, key, raw, header) -> bool` helper in `app/channels/shopify_webhook.py` — tries client_secret first (one `get_secret` on the live real-order path), falls through to the signing secret only if configured; each check fails CLOSED independently (unset key → `get_secret` None; corrupt/rotated key → `VaultError`; bad sig → mismatch — none crash the request nor block the other secret); NEITHER verifies → 403 as today. Admin: `ShopifyCredsRequest` gained optional `webhook_signing_secret` (≤256, blank/omitted = keep; additive — NOT part of the first-time client_id+client_secret gate); `set_shopify` encrypt-stores it under `shopify:webhook_signing_secret`; `shopify_status` GET now returns `{"configured", "webhook_signing_secret_configured"}` (never echoes the value); panel field `sh-webhook-signing-secret` in `static/{index.html,admin.js}`. **Tests:** 6 new webhook (client_secret regression when both set, signing-secret verifies, garbage→403, signing-secret-signed rejected when unconfigured, VaultError on either secret fails closed without crashing) + 2 admin (set+status configured flag, never echoed); updated 2 existing admin exact-equality assertions for the new status key. **795 passed, 34 skipped** (was 787/34: +8); `ruff check .` + `mypy app` strict (64 files) + secrets grep on both touched app files clean; `git diff -- backend/app/core/order_actions.py` EMPTY; `tests/core/test_button_dispatch.py` green (23). Registries updated. **Next: code-reviewer + security-reviewer (webhook HMAC surface); push after owner approval.** |
| 🟡 **Fulfillment/tracking capture + order-status Q&A surfacing (Q10) — BUILT + SECURITY-REVIEW FIXES APPLIED (2026-08-14)** — TDD, committed locally on top of `f406c3c`, NOT pushed | **⚠ 2026-08-14 SECURITY-REVIEW CORRECTION PASS (1 HIGH + 4 MEDIUM + 4 LOW, TDD RED→GREEN, committed on top of `f406c3c`, NOT pushed):** **HIGH — inlining scope-gated `fulfillments` into shared `ORDER_FIELDS` would ACCESS_DENY the WHOLE order query** (app lacks `read_fulfillments` scope), taking down `get_order`/`find_order_by_name` and EVERY Confirm/Cancel tap + reconcile + live Q&A. Fixed: fulfillments removed from `ORDER_FIELDS`; new isolated `ShopifyClient.get_order_fulfillments(gid) -> tuple[...]` that swallows any `ShopifyError`→`()`; `get_order`/`find_order_by_name`/`find_customer_orders_by_phone` attach best-effort AFTER the core fetch — structurally safe regardless of scope. Regression test drives the REAL client (ACCESS_DENIED on the fulfillments sub-query) through `resolve_by_gid` and asserts a valid `AuthorizedOrder` w/ `fulfillments == ()`. **MEDIUM#1** — order_tracking prompt reworded: no longer infers "not shipped" from absent tracking (go by fulfillment status instead). **MEDIUM#2** — `upsert_order_mirror` now persists `order.fulfillments` (cap `MAX_MIRROR_FULFILLMENTS=10`) + backfill fetches them via `get_order_fulfillments`, so already-shipped historical orders get tracking. **MEDIUM#3** — `upsert_fulfillment` gained an out-of-order-delivery guard on a new `fulfillments.shopify_updated_at` column (`Fulfillment.updated_at` parsed from the webhook + live `updatedAt`), so a replayed `fulfillments/create` can't revert good tracking; mirrored in-memory. **MEDIUM#4** — in-memory `delete_by_phone` now pops `self.fulfillments` (DPDP erasure). **LOWs:** fulfillment gid no longer clipped (PK); `_ensure_one_topic` catches broad `ShopifyError` (not just GraphQL); `reveal_fields` corruption fails closed to `["order_number"]`; fulfillment mirror logs only `type(exc).__name__` (no tracking-number leak via asyncpg text); stale cost comment updated. **786 passed, 34 skipped** (was 773/34: +13 tests); `ruff check .` + `mypy app` strict (64 files) + secrets grep on all touched app files clean; `git diff f8ce082..HEAD -- app/core/order_actions.py` EMPTY; `tests/core/test_button_dispatch.py` green. Registries + error_learnings updated. **Next: Main Claude re-reviews; apply the `fulfillments.shopify_updated_at` schema + verify `read_fulfillments` scope live BEFORE deploy; push after owner approval.** --- ORIGINAL BUILD NOTES: Closes the Q10 gap ("send the Shopify tracking link" when asked) — tracking data was never captured. **What shipped (11 app files, TDD RED→GREEN):** new `Fulfillment` dataclass + `Order.fulfillments: tuple[Fulfillment,...]` (`app/shopify/models.py`); `fulfillment_from_webhook_payload` REST parser (`app/channels/shopify_orders.py`, order gid CONSTRUCTED from numeric `order_id` — the one case F20's "never reconstruct" can't apply; bool/ASCII-digit guarded; singular-then-array tracking fallback); `fulfillments/create`+`fulfillments/update` webhook branches + soft-fail `_mirror_fulfillment` (`app/channels/shopify_webhook.py`); `FULFILLMENTS_CREATE/UPDATE` added to `REQUIRED_TOPICS` (`app/shopify/subscriptions.py`) — per-topic `_ensure_one_topic` isolation degrades cleanly if `read_fulfillments` scope is missing; `fulfillments(first:5){ id status trackingInfo(first:3){ company number url } createdAt }` added to `ORDER_FIELDS` + `_fulfillments_from_node` (`app/shopify/client.py`, live-fallback path); `upsert_fulfillment(order_gid, Fulfillment)` on the IngestStore Protocol + in-memory + Postgres, all three mirror reads attach fulfillments (phone path batched `= ANY($1)`, no N+1) (`app/store/{base,memory,postgres}.py`); `"tracking"` added to `REVEAL_ALLOWED` + default (`app/admin/controls.py`, `max_length` 4→5) + `DEFAULT_REVEAL_FIELDS` (`app/agents/base.py`) + rendered gated on reveal_fields in `app/agents/order_tracking.py` (+ Q10 prompt: share the link, never fabricate an ETA); admin panel `c-rv-tracking` checkbox (`static/{index.html,admin.js}`). **Schema:** new `fulfillments` table + `idx_fulfillments_order_gid` (`app/store/schema.sql`, additive/idempotent — apply to prod separately). `MirrorOrderSource` needed NO change (fulfillments pass through transparently — proven by a test). **Standing invariants held:** `git diff f8ce082 -- app/core/order_actions.py` EMPTY (read-only enrichment, no mutation touched); `tests/core/test_button_dispatch.py` green (23). **773 passed, 34 skipped** (was 743/32: +30 tests, +2 gated PG fulfillment tests skip cleanly w/o `TEST_DATABASE_URL`); `ruff check .` + `mypy app` strict (64 files) + secrets grep on all 11 touched app files clean. Registries + error_learnings updated. **⚠ `read_fulfillments` scope check NOT executable from this sandbox** — Shopify creds live encrypted in production Postgres (needs `APP_MASTER_KEY`+`DATABASE_URL`+`SHOPIFY_*`, all unset here; network IS reachable). Owner must run the scope check OR watch the next `ensure_subscription` run: if FULFILLMENTS_* come back `"error"`, grant `read_fulfillments` in the Dev Dashboard → release version → reinstall → fresh token. **Next: Main Claude reviews; apply schema SQL to prod; owner runs scope check; push after approval.** |
| 🟡 **Q17 — 1-hour order-confirmation reminder (Flow A) — BUILT + SECURITY-REVIEW FIXES APPLIED (2026-08-14)** — TDD, committed locally on top of `946ef45`, NOT pushed | **⚠ 2026-08-14 SECURITY FIXES (2 HIGH + 2 MEDIUM, TDD RED→GREEN, committed on top of `946ef45`, NOT pushed):** **HIGH#1 — reminder clock anchored to `created_at` (webhook-ingest), not the actual template-send moment** → on first deploy the backlog-flush drain would stamp `template_sent` on hours-old mappings and the next sweep would immediately queue a DUPLICATE template to real customers. Fixed: `find_stale_template_sent` now ages off `updated_at` (stamped `now()` by `set_mapping_status` when the drain sets `template_sent`); verified every `updated_at` writer keeps `status='template_sent' AND <window>` meaning "became template_sent in the window" (taps move status off it; a duplicate-ingest bump only DELAYS, never duplicates). **HIGH#2 — no upper bound + indefinitely-kept mappings (Q15)** → a 30h/45-day-old untapped order would be reminder-blasted on first run. Fixed: added a parameterized 24h ceiling (`_REMINDER_CEILING_MINUTES`), window is now `now()-60min >= updated_at > now()-24h`. **MEDIUM#1 — unbounded + N+1 sweep** (same class as the 2026-08-12 mirror-read finding): added `ORDER BY updated_at LIMIT 25` + index `idx_order_mappings_status_updated (status, updated_at)`; batched the per-row original-payload lookup into one `find_outbound_by_dedupe_keys(list)->dict` (`= ANY($1)`). **MEDIUM#2 — client-decisions doc understated blast radius:** Q17 entry in `client-decisions-all.md` corrected — reminders QUEUE as soon as an order is 1–24h past send and, because `send_mode` is currently `live`, actually go out on the next drain tick (not "no live effect until enabled"). **743 passed, 32 skipped** (was 738/30: +5 non-gated incl. anchoring/ceiling/batched-read tests, +2 gated PG tests that skip cleanly, `TEST_DATABASE_URL` UNSET); `ruff check .` + `mypy app` strict (64 files) + secrets grep on all 4 touched app files clean; `git diff origin/main..HEAD -- backend/app/core/order_actions.py` empty; `tests/core/test_button_dispatch.py` green (23). Touched: `app/store/{base,memory,postgres}.py`, `app/store/schema.sql`, `app/jobs/reminders.py`, `docs/FR/client-decisions-all.md` + tests `tests/test_reminders_job.py`, `tests/store/{test_reminders_store,test_reminders_pg}.py`. Registries + error_learnings updated. **Next: Main Claude reviews; push after owner approval.** --- ORIGINAL BUILD NOTES: **Owner-directed:** if a customer gets `order_confirmation_cod` and does not tap Confirm/Cancel within 1 hour, resend the EXACT same template ONCE as a reminder, never again for that order. **Reuses the existing outbox pipeline — no parallel machinery.** Eligibility = a mapping still at `status='template_sent'` whose `created_at <= now() - 60min` (a Confirm/Cancel tap moves status off `template_sent`, so tapped orders auto-exclude). New `IngestStore` methods (base.py Protocol + memory + postgres): `find_stale_template_sent(older_than_minutes)` (Postgres `make_interval(mins => $1)`, parameterized/injection-safe; in-memory tracks `_mapping_created_at` stamped once on ingest), `find_outbound_by_dedupe_key(dedupe_key) -> OutboundDraft | None` (reuses the ORIGINAL push's payload verbatim, NO Shopify re-fetch), `enqueue_outbound(draft) -> bool` (same `ON CONFLICT (dedupe_key) DO NOTHING` idempotency — the UNIQUE dedupe_key IS the exactly-once guarantee). New job `jobs/reminders.py::run_send_reminders(c) -> {"swept","queued"}` queues `order_reminder:{gid}`; registered in `jobs/router.py` JOBS (inherits dual GET-Bearer/POST-X-Cron-Secret auth); NEW Vercel Cron `*/5 * * * *` for `/internal/jobs/send_reminders` in `vercel.json` (2 crons now, valid JSON). **1 necessary in-scope deviation from the brief's "no changes needed to send_one_outbound":** `_gid_from_dedupe_key` had to accept the `order_reminder:` prefix too (was `order_created:`-only, else the drain rejects the reminder as a corrupt dedupe_key) — surfaced by the mandated E2E test (scenario 5: reminder must flow through the UNCHANGED claim→send_one_outbound pipeline and send with `order:confirm/cancel:{gid}` buttons). Widened to `_DEDUPE_PREFIXES=("order_created:","order_reminder:")`. **⚠ Known long-term note (flagged, not built):** the reminder SEND leaves the mapping at `template_sent` (only a tap moves it off), so a never-tapped order is re-swept every tick forever (dedupe makes each re-sweep queue nothing — `swept=1,queued=0`); fine at v1 100-500 orders/day, inherent to the owner's chosen design — a future `template_sent`-terminal or reminded-flag would bound it. **738 passed, 30 skipped** (was 726/27: +9 in-memory/job tests incl. 6-scenario coverage + 3 gated PG tests that skip cleanly, `TEST_DATABASE_URL` left UNSET); `ruff check .` + `mypy app` strict (64 files) + secrets grep on all 6 touched app files clean; `git diff HEAD -- app/core/order_actions.py` empty; `tests/core/test_button_dispatch.py` green (23). Touched app: `app/store/{base,memory,postgres}.py`, `app/jobs/{reminders(NEW),router,outbox_drain}.py`, `vercel.json`; tests: `tests/test_reminders_job.py`, `tests/store/{test_reminders_store,test_reminders_pg}.py`. Registries + error_learnings updated. **Next: Main Claude reviews; commit already local; push after owner approval.** |
| 🟢 **Outbox queue+cron rework — SECURITY-REVIEW FIXES (2026-08-13)** — 1 HIGH + 2 LOW fixed TDD, committed locally on top of `c736cfb`+`8848c85`, NOT pushed | **HIGH — stranded `processing` rows / permanent message loss:** `claim_queued_outbound`'s predicate was `WHERE state='queued'` only, so any row a killed Vercel invocation left in `processing` (after the atomic `queued→processing` flip, before the send finished) was stranded FOREVER — nothing re-claims a `processing` row (`bump` only fires on a reached send, `purge_older_than` excludes `outbound_messages`, no admin requeue). Widened to `WHERE state='queued' OR (state='processing' AND updated_at < now() - interval '10 minutes')` (Postgres) — the 10-min threshold is ≫ `send_template`'s 20s timeout so a genuinely in-flight row is never stolen, only truly-abandoned rows recovered. In-memory store mirrored (`_OutboundRow.updated_at`, stamped on write + claim; reclaim if `now-updated_at ≥ 10min`). Also `_CLAIM_LIMIT` 25→5 (fits any per-invocation budget; 5/min = 7200/day ≫ the 100-500/day target). Proved by a gated real-Postgres test (stale `processing`→reclaimed, recent→not) + a fast in-memory unit test; the SQL-shape assertion test now pins the new WHERE clause. **LOW#1 — coverage gap:** added `test_post_with_bearer_header_but_no_x_cron_secret_403` (mirror of the GET+old-header test; code already returned 403, pure coverage). **LOW#2 — regression test patched wrong binding:** the webhook "never sends" test now monkeypatches `app.channels.whatsapp_sender.send_template` at its SOURCE (not the drain module's import location), so a FUTURE direct `from ...whatsapp_sender import send_template` inside `shopify_webhook.py` would still trip the guard. **726 passed, 27 skipped** (was 723/26 — +2 in-memory reclaim tests + 1 gated PG reclaim test + LOW#1 test; gated PG skips cleanly, `TEST_DATABASE_URL` left UNSET — real-DB claim/reclaim proof flagged as a manual step against a SEPARATE scratch DB, NEVER production); `ruff check .` + `mypy app` strict (63 files) + secrets grep on all 3 touched app files clean. `git diff origin/main..HEAD -- backend/app/core/order_actions.py` still empty; `tests/core/test_button_dispatch.py` green. Touched: `app/store/{postgres,memory}.py`, `app/jobs/outbox_drain.py` + tests `tests/store/{test_outbox_drain,test_claim_atomic_shape,test_outbox_drain_pg}.py`, `tests/{test_jobs_router,test_shopify_webhook}.py`. error_learnings entry added (stale-`processing` reclaim). **Next: Main Claude reviews the commit; push after owner approval.** |
| 🟡 **Phase 5 — Order push + Confirm/Cancel automation — REVIEWED + REVIEW-FINDINGS FIXED (2026-08-10)** | developer done (all 9 TDD tasks); **code review = APPROVED WITH NITS, security review = PASS WITH NOTES** — both ran on the Phase-5 changed files. **4 review findings fixed TDD (RED→GREEN), 2026-08-10, local `main` only, NOT pushed:** (1) MEDIUM (security+code) **shadow/allowlist kill-switch gating for button taps** — `dispatch_button` now calls `send_decision(send_mode, allowlist_phones, wa_id)` FIRST and on `suppress` (shadow / allowlist-miss / off / unknown) does nothing at all (no resolve, no `tagsAdd`/`orderCancel`, no reply); previously a tap in shadow performed REAL mutations + sends. The webhook keeps its coarse off-gate as defense-in-depth; `dispatch_button` is now the single source of truth (mirrors drain + inbound path). (2) MEDIUM (code) **Meta code regex anchored** — `_META_CODE_RE = r"(?:^|; )code=(\d+)"` so a nested `error_subcode=131026` is no longer misread as an undeliverable send code (that send now retries). (3) MEDIUM (code) **audit symmetry** — the outer `except ShopifyError` now writes an "error" `order_actions` row for the attempted confirm/cancel mutation across ALL ShopifyError subtypes (a failed add_tags or transport cancel is audited, not just successes). (4) LOW (security) **cancel-confirm race** — `_handle_cancel_confirm` advances state (provisional tag + mapping status) BEFORE the async `orderCancel`, so a racing second tap's live re-fetch sees the marker and refuses a duplicate cancel (`cancel_order` called at most once). **LOWs deferred (documented, owner-tracked, NOT changed):** outbox non-atomic claim / `FOR UPDATE SKIP LOCKED` (single-instance v1 — Deferred list item 2), drain `maxDuration` / duplicate-on-kill (Vercel-dashboard config — Deferred list), `_is_dispatched` NOT refactored into `order_tracking` (friend's file). Touched only Phase-5 files: `app/core/order_actions.py`, `app/jobs/outbox_drain.py` + tests `tests/core/test_button_dispatch.py`, `tests/test_whatsapp_webhook.py`, `tests/test_outbox_drain_job.py` (`app/channels/whatsapp.py` needed no change — it already had the off-gate). **587 passed, 11 skipped** (574 baseline + 13 new); ruff + mypy strict (62 files) + secrets grep on both touched app files all clean. error_learnings entry added (anchored field regex + write-idempotency-marker-before-the-async-mutation). **Next: Main Claude commits the fixes; push after owner approval.** | **574 passed, 11 skipped** (pre-fix baseline) (baseline was 505/8; +69 tests, +3 new gated Postgres tests in `tests/store/test_outbox_drain_pg.py` skip cleanly w/o `TEST_DATABASE_URL`); `ruff check .` + `mypy app` strict (62 source files) + secrets grep (every touched `app/` file) all clean. Per-task commits `a1f1988..1ecbf27` on local `main` (HEAD was `a532aa5`), NOT pushed. Plan: `docs/superpowers/plans/2026-08-10-phase5-order-push.md`; spec: `docs/superpowers/specs/2026-08-10-phase5-order-push-design.md`. **What shipped (Flow A — order push):** `IngestStore` outbox-drain + mutation-audit + mapping-status methods + `OutboundClaim` (in-memory + Postgres) [`a1f1988`]; `core.send_policy.send_decision` kill-switch helper [`63e1ceb`]; `jobs.outbox_drain.run_outbox_drain` — claims queued rows, enforces `send_mode` in ONE place, sends `order_confirmation_cod` with `order:confirm/cancel:{gid}` buttons, retryable-vs-undeliverable Meta-code taxonomy [`986fe33`]. **Flow B — deterministic button dispatch (the mutation-safety core):** `core.order_resolver.resolve_by_gid` (live re-fetch + ownership, non-enumerable) [`7504888`]; `core.order_actions.dispatch_button` — NO LLM, ownership-checked before every `tagsAdd`/`orderCancel`, two-phase cancel (1st tap only asks, only the confirm tap cancels), cancel-before-dispatch-only, idempotent via live-state + provisional-tag checks, never raises [`a64e6f8`]; `AdminControls.tags.cancel_requested` provisional tag (default `["bot-cancel-requested"]`, migration-safe) [`a64e6f8`]; `jobs.reconcile.run_reconcile_cancels` + `order_resolver.authorize_own_order` — final `cancelled` tag only once Shopify confirms cancelledAt [`524a9bb`]; `channels.copy` +11 keys × 4 languages (button titles ≤20) [`515ec90`]; `channels.whatsapp` button-tap wiring gated on the kill switch, error-swallowed so a tap never 500s the webhook [`1ecbf27`]. **3 documented deviations (all safety- or layering-motivated, not scope creep):** (1) the drain's undeliverable-vs-retryable branch keys on the META error code extracted from `SendResult.error` (via `_safe_error`'s `code=<n>`), NOT `r.status_code` as the plan literally wrote — those Meta codes live in the body, the HTTP status is 400 for all of them, so the plan's literal check could never match; (2) `order_actions._is_dispatched` uses the CONSERVATIVE inverse (`fulfillment_status not in {None,"UNFULFILLED"}` = dispatched → refuse cancel) rather than the plan's positive `{FULFILLED,PARTIALLY_FULFILLED,RESTOCKED}` set, to stay consistent with the Phase 4 order_tracking agent's own eligibility promise and because a false-refuse (route to support) is safe while a false-cancel of a shipped order is not; (3) the webhook send-mode gate reads the raw `send_mode` config key + whitelists active modes instead of importing `admin.load_controls` into `channels/whatsapp.py`, preserving the Phase-4 layering fix (`channels/` stays admin-free) — behavior-identical (off/unset/corrupt = disabled). Registries (`component_registry.md`, `api_registry.md`) updated for every new/extended module. **Next: code-reviewer (Phase 5 changed files) → security-reviewer (MANDATORY — mutations/orderCancel/send path) → fix findings TDD → re-verify. Main Claude pushes after review.** |
| 🟡 **Phase 4 — Subagent conversation architecture — BUILT, READY FOR REVIEW (2026-08-06)** | developer done (all 15 SDD tasks); code-reviewer + security-reviewer have NOT run yet, so this is not "complete/closed" | **447 passed, 8 skipped** (Postgres-gated tests skip cleanly without `TEST_DATABASE_URL`); ruff + mypy strict (58 source files) + secrets grep (whole `app/` tree, not just this phase's files) all clean. Commit range: plan/spec docs `9a6b02b..c9c8d58` (design + 15-task plan, worktree `worktree-phase-4-subagent-conversation`), code `738502d..6f6d8ed`. **This design (specialized order_tracking/product_search/policy/recommendations/customer_support agents behind a router, real extracted policy text, VIP-by-order-count, dispatch-based cancellation, handoff via `paused_until`/`handoff_attempted_at`) supersedes and replaces the 2026-08-04 single-inline-prompt `engine.py` plan (`docs/superpowers/plans/2026-08-04-phase4-conversation-engine.md`), which was never executed** — the client's round-3 (2026-08-06) scope decisions (Friendly Fashion Advisor personality, AI product search, cross-sell/upsell, VIP recognition, one-attempt handoff) required a materially different architecture, triggering a fresh brainstorm + design + plan instead of a resume. **What shipped:** `app/core/order_resolver.py` (phone/order-name resolution, live Shopify re-fetch, ownership check), `app/core/sanitize.py` (`strip_markdown`), `app/core/memory.py` + `ConversationStore` (windowed history + `paused_until`/`handoff_attempted_at`, in-memory + Postgres), `AdminControls.vip_order_count_threshold`, `ShopifyClient.search_products` + `Product`, `IngestStore.find_mappings_by_phone`/`count_orders_by_phone`, the full `app/agents/` package (`base` shared context/personality/JSON-parsing + `router` + 5 specialists), `deps.Container.conversations`/`active_llm`, and the `POST /webhook/whatsapp` wiring that now runs the full router → agent pipeline (gated on `send_mode`/`paused_until`) for every fresh `InboundText`. **This was NOT a clean first pass through every task** — independent review during the build found and required fixing real issues in Tasks 3, 5, 6, 8, 9, 10, 13, and 14 before they were re-verified clean; see this SDD workspace's task reports (`.superpowers/sdd/2026-08-06-phase4-subagent-architecture/`) and the git log (`738502d..6f6d8ed`) for the fix-round detail, not just the task briefs. **Three deviations from the original 15-task plan, all owner-approved / review-driven, not scope creep:** (1) **Task 5** added `_is_safe_product_query` to `search_products` — a Shopify search-operator-injection defense (rejects quotes/colons/wildcards/boolean-operator-words/unbalanced parens) not in the original brief, found necessary during review since an LLM-derived query string was being interpolated into Shopify's search DSL unfiltered. (2) **Task 6** fixed a raw-JSON-leak bug in `extract_reply_text` during review — an edge case could let literal `{"reply": ...}` JSON syntax or a stray code fence reach the customer verbatim; tightened to fall back to the safe error string instead. (3) **Task 14 (largest deviation):** the conversation pipeline wiring was NOT left inline in `app/channels/whatsapp.py` as the original brief specified — a post-build security review flagged it as a `fastapi-layering.md` violation (`channels/` must stay webhook-edge-only: HMAC/idempotency/parsing/sending, not orchestration importing `admin`/`agents`/`core`/`knowledge`) and it was extracted, mechanically and behavior-neutral, into a **new module `app/core/conversation.py`** (`run_turn`), leaving `whatsapp.py` a thin call site — an owner-approved layering fix, not a redesign. Task 14 also added, beyond the original brief, an internal `asyncio.timeout(TURN_TIMEOUT_SECONDS=55.0)` guard around the whole turn (defense-in-depth against an uncaught platform timeout silently dropping a customer's reply), a check-and-log on `SendResult.ok is False` (previously silently discarded), and an empty-reply guard (falls back to `copy_for("error_fallback","en")` rather than ever persisting/sending a blank message) — all three added during Task 14's own review/fix cycle. Task 14 ALSO attempted a `vercel.json` change (`functions["api/index.py"].maxDuration=60`) that was found to conflict with Vercel's schema (`functions` and `builds` are mutually exclusive; this repo's `vercel.json` needs `builds`/`routes` for its catch-all ASGI routing) and was reverted the same day (commit `6f6d8ed`) — **`vercel.json` is UNCHANGED from before Phase 4.** `maxDuration` still needs to be set manually via the Vercel dashboard (Project Settings → Functions) once the project is connected — see the "Deferred" section below; this is a pending operational step, not code, and the code-side `TURN_TIMEOUT_SECONDS` guard stands on its own regardless. Registries (`component_registry.md`, `api_registry.md`) fully updated for every new/extended module in this phase. **Next: code-reviewer (scoped to the Phase 4 changed files) → security-reviewer (conversation pipeline touches Shopify mutation-adjacent order data, LLM-derived queries, and the WhatsApp send path — a sensitive surface per the routing table).** |
| 🟢 **ROUND 3 CLIENT DECISIONS — Phase 4 business & product scope (2026-08-06)** | ✅ ANSWERED — full detail in `client-decisions-all.md`; two conflicts flagged with already-shipped code | **Phase 4 architecture = subagent, NOT single-inline-prompt** — specialized agents for product search / order tracking / policies / recommendations / customer support. **⚠ Supersedes the committed single-`engine.py` plan** (`docs/superpowers/plans/2026-08-04-phase4-conversation-engine.md`) — needs a fresh design pass, not a resume. **Q15 retention = differentiated**: order/customer data kept INDEFINITELY, only conversation/operational data ages out at 365 days. **⚠ Conflicts with the shipped `purge_older_than`**, which currently ages `order_mappings`/`outbound_messages` by date — harmless today only because `retention_days=0` by default; must be fixed (exclude those tables from age-based purge) before retention is ever turned on. **Q8 switchover = test on a separate number first, then migrate the SAME production WATI number, no parallel run.** **Rate limiting = Postgres-backed** (not Upstash) — closes the pre-deploy hardening item 1 decision, not yet built. **Scale target = 100–500 orders/day.** **New feature scope** (was explicitly out of v1): AI product search with similar-item fallback + never-hallucinate rule, in-stock-only recommendations, cross-sell/upsell/outfit-matching sales strategy, "Friendly Fashion Advisor" personality (materially different from the shipped plain `brand_voice.md` seed), VIP/repeat-customer recognition WITHOUT proactively stating spend/order-count, one-attempt-then-immediate human handoff (same WhatsApp number, no separate support line), Shopify tracking-link-only (no a2ship integration). **Real policy text extracted verbatim** from `D:\TAVAS Website\policy\*.docx` (shipping/return/exchange/refund/privacy/T&C) — supersedes the placeholder `app/knowledge/seeds/{faq,business}.json` content shipped in Phase 3.5. Cancellation policy = before dispatch only (not just financial-status based — Phase 5's cancel flow must check fulfillment status). Still open: Q6 (no-match fallback), Q13 (tag-name compatibility). **Next: re-brainstorm Phase 4 architecture given the scope change before writing a new implementation plan.** |
| **Retention — exclude order/customer data from age-based purge (Q15 conflict fix)** | **DONE (2026-08-06)** — TDD fix, committed locally, NOT pushed | Closes conflict (1) flagged in the ROUND 3 entry above: `purge_older_than` (postgres + in-memory) no longer ages out `order_mappings`/`outbound_messages` — those DELETEs are removed from the automatic age-based path per client Q15 (customer/order data kept INDEFINITELY). It still ages out only the client-approved-for-deletion category: conversations/messages (`last_active_at`), pending_actions/order_actions (`created_at`), processed_messages (`received_at`). `DeletionResult` kept as the shared shape; the age-based job now always reports `order_mappings=0, outbound_messages=0`. `delete_by_phone` (POST /admin/erasure, right-to-erasure-on-request) is UNCHANGED — it may still remove a specific customer's order data on request (indefinite retention is the default, erasure is the override). Regression test proves order/outbound rows are never queried by the age-based purge (not just removed code). Docstrings on both `purge_older_than` impls + `retention.py` + `DeletionResult` state why (so no future editor re-adds them). +1 test → **345 passed, 6 skipped**; ruff + mypy strict (46 files) + secrets grep clean. Touched: `app/store/{postgres,memory,base}.py`, `app/jobs/retention.py`, `tests/store/test_deletion.py`, `tests/test_retention_job.py`. |
| **Admin — DPDP hardening: security-review fixes (ROUND 2)** | **DONE (2026-08-06) — pushed to origin/main same session, 14 commits `4e4d0d0..2b56772`** | **Two-round fix.** Round 1 (`8d0ec96..b815cab`) closed the original 3 MEDIUMs (re-verified genuine), but re-verification found 1 NEW MEDIUM introduced by round 1 + 2 round-1 LOWs incomplete — ALL fixed TDD (RED→GREEN) this round (commits after `b815cab`). **NEW MEDIUM — audit-log injection:** round 1 logged the raw `request.url.path` in `require_admin` BEFORE auth; uvicorn percent-decodes `%0A`→literal newline in `scope["path"]`, so an unauthenticated attacker could split the log line and forge a fake `outcome=success` record (or flood a 4000+ char line). Now logs the matched route TEMPLATE (`request.scope["route"].path`), falling back to a control-char-stripped, 128-char-bounded raw path (sanitized before the logger) only when no route matched. **LOW (incomplete) — Unicode-digit int bypass:** round 1's `try:int()` closed the crash but `int("٣٠")==30` still enabled an unintended 30-day retention purge; now gated `raw.isascii() and raw.isdigit()` before `int()`. **LOW (incomplete, higher blast radius) — signed-webhook staleness:** round 1's `float()` guard still accepted `float("٣٠")` AND fail-opened on `float("nan"/"inf"/"1e400")` (which never raise) — a nan/inf makes `age>staleness*3600` always False, fully DISABLING the staleness guard (mass unwanted template re-sends); now `isascii()+isdigit()` + `math.isfinite` + clamp to `Field(ge=1,le=168)`, else module default. **LOW — one corrupt field reset the whole controls doc:** `load_controls` now validates per top-level field (`_validate_per_field`) so a bad `retention_days` no longer silently re-widens a narrowed `reveal_fields` back to the PII default. **NIT — `_E164_RE` `$`→`\Z`** (Python `$` matches before a trailing newline). error_learnings corrected: the round-1 "isdigit()-as-int-gate" note was a PARTIAL fix (crash only, not the Unicode-bypass) — a new 2026-08-06 round-2 entry documents the complete ASCII-gate + non-finite + audit-injection fixes. **Explicitly NOT touched (owner/client decisions, unchanged from round 1):** no dry_run mode, retention_days floor stays `ge=0`, no vercel.json/cron change. +6 tests → **344 passed, 6 skipped**; ruff + mypy strict (46 files) + secrets grep clean on every touched file. Committed (round 2, NOT pushed). Touched: `app/admin/{router,controls}.py`, `app/channels/shopify_webhook.py` + their tests. |
| **Admin — DPDP hardening: security-review fixes (ROUND 1)** | **DONE (2026-08-06)** — re-verified genuine, superseded by round 2 above | Code review APPROVED; security review CHANGES REQUESTED (2 blocking MEDIUM + 1 non-blocking MEDIUM + LOWs) — fixed TDD (RED→GREEN), commits `8d0ec96..b815cab`. **MED#1 incomplete erasure:** `delete_by_phone`/`purge_older_than` now also cover `pending_actions` (wa_id), `order_actions` (actor_wa_id), and blanket-age `processed_messages` (received_at); `DeletionResult` gained `pending_actions`+`order_actions` counts (in-memory + Postgres + gated PG test extended). `processed_messages` phone-scoped delete is a documented known residual (wamid embeds the phone, no decode mechanism built — deferred note added). **MED#2 non-attributable erasure audit:** resource now `phone:{HMAC-SHA256(number, app_master_key)[:16]}` + `deleted={sum}` (recomputable, non-reversible, not PII; no new secret). **MED#3 panel reverts retention_days to 0:** added `c-retention` field to index.html + admin.js load/save payload. **LOWs (⚠ 2 only PARTIALLY fixed — completed in round 2 above):** `isdigit()`→`try int()` gate in controls (closed the crash, NOT the Unicode bypass) + same-class `float()` guard on the SIGNED shopify_webhook staleness hot path (closed the crash, NOT Unicode/non-finite); E.164 `\d`→`[0-9]` in erasure request + controls `_E164_RE`; `@limiter.limit("10/minute")` on /erasure; `authz denied` audit in `require_admin` for forged/absent cookies (this itself introduced the round-2 NEW MEDIUM). **Explicitly NOT touched:** no dry_run mode, retention_days floor stays `ge=0`, no vercel.json/cron change. +11 tests → 338 passed, 6 skipped. Touched: `app/store/{base,memory,postgres}.py`, `app/admin/{controls,router}.py`, `app/channels/shopify_webhook.py`, `app/admin/static/{index.html,admin.js}`. |
| **Admin — DPDP hardening: audit logging (item 4) + retention/erasure (item 5)** | **REVIEW (2026-08-06)** — developer done, ready for code-reviewer + security-reviewer | Closes the two deferred Phase-3.5 pre-deploy items now the panel is live (Vercel+Supabase connected). **Item 4:** actor-free `_audit(action,outcome,resource)` on `logger("app.admin")` — login success/failure/not_configured, credential_set (shopify/whatsapp), provider_set, knowledge_set, controls_set, erasure; logs NAMES/outcomes only, NEVER a value or the phone. **Item 5:** `POST /admin/erasure` delete-by-phone (require_admin, audit-logged, number never logged) over order_mappings+outbound_messages (phone_e164) + conversations/messages (user_id, defensive/no-writer-yet, FK children-first); `retention_days` config **DEFAULT 0 = disabled** (no policy invented — pending client Q15); purge-by-age `ingest.purge_older_than(cutoff)` + `run_retention_purge` job wired to `/internal/jobs/retention_purge` (CRON_SECRET, default no-op). TDD RED→GREEN. +16 tests → **327 passed, 6 skipped** (2 new gated Postgres tests skip w/o TEST_DATABASE_URL); ruff + mypy strict (46 files) + secrets grep clean on every touched file. Touched: `app/store/{base,memory,postgres}.py` (DeletionResult + delete_by_phone/purge_older_than), `app/admin/controls.py` (retention_days + `_INT_KEYS`), `app/admin/router.py` (`_audit` + audit calls + `POST /erasure`), `app/jobs/retention.py` (NEW), `app/jobs/router.py` (register). Registries + error_learnings updated (machine/interpreter note: this box needed slowapi+litellm installed into codex-runtimes Python). Retention default confirmed 0/disabled. NOT committed/pushed. |
| **Admin — Vertex AI Gemini provider (env service-account auth)** | **REVIEW (2026-08-04)** — developer done, ready for code-reviewer | Ported from cafe (`D:\ai_whatsapp_agent`). Adds a second LLM provider `vertex` ("Gemini (Vertex AI)", model `vertex_ai/gemini-3.5-flash`, `auth_kind="env"`, `request_params={temperature:0.3,reasoning_effort:medium}`) alongside direct `gemini`. TDD, +13 tests → **308 passed, 4 skipped**; ruff + mypy strict (45 files) + secrets grep clean (every touched file). Touched: `app/providers/registry.py` (ProviderInfo `auth_kind`+`request_params`, vertex entry), `app/config/settings.py` (`vertex_credentials_json`/`vertex_project`/`vertex_location` env fields), `app/providers/litellm_provider.py` (frozen `VertexConfig`; `LiteLLMProvider(vertex=…)`; `vertex_ai/*` injects `vertex_credentials/project/location` not `api_key`, raises `ProviderError AUTH` if creds missing; `_redact` scrubs the service-account JSON too), `app/providers/verify.py` (restore `extra_params` kwarg), `app/deps.py` (`build_provider(settings)` wiring `VertexConfig`, lazy), `app/admin/router.py` (GET /providers returns `auth_kind`; `set_provider` env branch verifies via env creds + activates WITHOUT storing a key, SAFE generic failure message never echoing raw error; GET /config `configured=true` for active env provider), static `index.html`+`admin.js` (`applyProviderAuthUI` hides the API-key field + relabels the button for env providers; env save POSTs `{provider}` only). No error_learnings entry (clean port, no surprise). Registries updated. NOT committed/pushed yet. |
| **Phase 3.5 — Admin panel — ✅ COMPLETE (2026-07-30)** — built + code-reviewed + security-reviewed (2 passes) + all in-scope findings fixed & re-verified; 5 pre-deploy hardening items tracked in "Deferred" | **Final: 292 passed, 4 skipped; ruff + mypy strict (45 files) + secrets grep clean. Main Claude independently re-ran gates + verified the HIGH credential-echo fix mechanism (`_VALIDATION_ERROR_DROP=("input","url","ctx")`). Commits `cc28eef..bacc3fe` (local, NOT pushed). Next: Phase 4 (conversation engine) — or connect Supabase/Vercel + close pre-deploy hardening when owner is ready.** | **SECURITY REVIEW (2026-07-30): all in-scope findings fixed TDD (RED→GREEN) — 1 HIGH (default 422 handler echoed plaintext creds → global `RequestValidationError` handler strips `input`/`url`/`ctx`; regression: over-length password/api_key/client_secret NOT in body), MEDIUM `knowledge_models` PatternItem per-example ≤200 cap + `controls` per-tag ≤64 cap + `load_controls` schema fail-safe (`except ValidationError → AdminControls()`), Finding A knowledge 422-not-500 (custom-validator `ValueError` in `ctx`), Finding B cookie Secure from `settings.app_env` (not client `x-forwarded-proto`), Finding C `AdminSecurityHeadersMiddleware` (CSP + X-Frame-Options + X-Content-Type-Options + Referrer-Policy + Cache-Control:no-store on all `/admin`), 2 LOW (`set_provider` UNKNOWN→generic; WhatsApp `phone_number_id`/`waba_id`/`api_version` format patterns). +15 tests → **292 passed, 4 skipped**; ruff + mypy strict (45 files) + secrets grep clean. 1 error_learnings entry logged (FastAPI default 422 echoes `input`; `RequestValidationError.errors()` takes no kwargs). Deferred items (serverless rate-limit store, session revocation, public_base_url pinning, audit log, DPDP retention) left untouched per brief. Prior CODE REVIEW: CHANGES REQUESTED → all 3 findings fixed TDD (RED→GREEN), 2 NITs deferred.** Findings: (1) BLOCKER `admin/auth.py:verify_token` — non-ASCII `admin_session` cookie → `hmac.compare_digest` TypeError → pre-auth 500 on every `require_admin` route (repeat of the documented Shopify/cron anti-pattern; cafe-verbatim port dropped the fix); fixed by ASCII-encode-then-bytes-compare, fail-closed on `UnicodeEncodeError` (unit + endpoint regression: non-ASCII cookie → 401 not 500). (2) MEDIUM `knowledge_models.py` `BusinessBody.extra` uncapped → `Field(max_length=50)` + per-entry key<=200/value<=2000 validator (51 pairs → 422, 50 → 200). (3) MEDIUM `AdminBodyCapMiddleware` only checked Content-Length → chunked (no-length) body bypassed the cap pre-auth on POST /admin/login → now 411 (Length Required) for body-bearing `/admin` methods with no Content-Length, 413 path kept (regression: generator body → 411). 2 NITs DEFERRED w/ owners to **Phase 4**: broad `except` in providers; `accept_on_rate_limit` dead code (intentional YAGNI). Suite now **277 passed, 4 skipped**; ruff + mypy strict (45 files) + secrets grep clean. 1 error_learnings entry logged (re-apply this repo's hardening when porting cafe auth code). All 12 plan tasks done TDD, commits `cc28eef..` (prior session); ruff + mypy strict (45 files) + secrets grep clean. Next: security-reviewer (auth/creds/session surface) → doc-updater.** Plan: `docs/superpowers/plans/2026-07-30-phase3.5-admin-panel.md`; spec: `docs/superpowers/specs/2026-07-30-admin-panel-design.md`. Built: `settings.admin_password` + slowapi/litellm deps + `app/ratelimit.py` · `app/admin/{auth,router,knowledge_models,controls}.py` (login/session + Shopify/WhatsApp/LLM creds + knowledge ×4 + controls + read-only mappings/outbox views, all cookie-auth, secrets never echoed, verify-key-before-save) · `knowledge_overrides` table + ConfigRepo override/bump methods · `app/knowledge/` loader + Thetavas seeds · `app/providers/` (base/registry/litellm/verify, lazy import) · static panel at `/admin/ui`. **Deviations (all logged in error_learnings/registries):** (1) body-cap enforced via a small `AdminBodyCapMiddleware` (ASGI) instead of the plan's router dependency — FastAPI parses the JSON body before path deps, so an oversized invalid body 422s before a dependency could 413; middleware checks Content-Length first (matches the webhook edges). (2) `datetime.UTC`/`StrEnum` instead of the plan's `timezone.utc`/`(str, Enum)` — repo ruff (UP017/UP042) wins, behaviour-equivalent. (3) test subdirs (`tests/{config,admin,store,knowledge,providers}/`) added as subpackages to match the plan's paths against the repo's flat package layout. (4) HTML select labels use hyphens not em-dashes (plain, no-emoji posture). Interpreter: `C:\Python313\python.exe` (3.13.5, all deps present) — the old-machine codex-runtimes path is dead (logged). NOTE: cafe `beyond-loaf-agent` IS editable-installed here → RED steps for cafe-shared modules (auth, providers, knowledge) show false-greens via shadowing until our module exists; each verified to load from `E:\...\backend\app\...` before GREEN. | Owner closed all 4 open decisions (2026-07-30): (1) **static HTML+JS panel at `/admin/ui` + JSON API `/admin/*`** (cafe pattern, no build step); (2) **live knowledge in NEW `knowledge_overrides` table** + seed fallback + `knowledge_version` cache bump (corrects earlier app_config idea — and fixes the real bug: seed FILES reset on every Vercel deploy); (3) **`ADMIN_PASSWORD` env var approved** — CLAUDE.md Critical Rule 1 + no-secrets rule AMENDED (third env exception); (4) **full scope A–D** (creds Shopify/WhatsApp/LLM w/ verify-key-before-save · knowledge structured forms ×4 kinds · operational controls incl. `send_mode` kill switch · read-only mappings/outbox views). Pulled forward from Phase 4: seeds + KnowledgeLoader + minimal providers/verify_key (lazy litellm). New dep: `slowapi` (login rate limit). NOTE: reference project **IS present on this machine** at `D:\ai_whatsapp_agent` (SESSION-HANDOFF's "not present" note is stale — machine changed to `e:\bhavesh_new\watsapp`). |
| Requirement gathering + raw API research (req.md) | DONE (2026-07-27) | Client's curl research preserved verbatim in `docs/raw-api-research.md`. |
| Shopify connection method verification | DONE (2026-07-27) | **Verdict: correct & officially supported.** Dev-Dashboard app + client credentials grant (24h token) + Admin GraphQL API = Shopify's documented pattern for own-org server-to-server integrations. Item-by-item verification in `docs/shopify-connection-verification.md`. Confirmed: orders NOT searchable by phone; `customer_id:`/`name:`/`email:` filters ARE supported; protected customer data = no approval needed for custom apps but must be enabled in Dev Dashboard; orderCancel is async + `orderCancelUserErrors`; ⚠ API version must move 2025-07 → 2026-07 (2025-07 hit end-of-support July 2026). |
| Reference project exploration (D:\ai_whatsapp_agent) | DONE (2026-07-27) | Full report: `docs/reference-project-ai-whatsapp-agent.md`. Stack: FastAPI + Meta Cloud API via httpx + LiteLLM/Gemini (Vertex gemini-3.5-flash) + Supabase Postgres + Vercel. Reuse: webhook HMAC + inbound parser + sender + JSON-intent engine + memory + Fernet vault + idempotency. Gaps to build new: template messages, outbound-initiated flow, Shopify client, identity binding. |
| Client answers — round 1 | DONE (2026-07-28) | (1) Meta Cloud API **direct**; (2) client already purchased WhatsApp Business **verified number**; (4) current 3rd-party does order-push + confirm-tag / cancel-order+tag — we replicate it ourselves; (5) payment fully on Shopify checkout — out of scope; (7) **v1 scope locked: order-created push + CONFIRM/CANCEL buttons; NO address change** (future); (8) bot reveals order id + email only for now; (12/13) host = Vercel + Supabase like cafe bot; (16) languages hi/en/hinglish/gu per customer. HELD: cancel-after-fulfillment (6), a2ship (9), app-ownership audit (10), handoff (14), volume (15). |
| Research — WhatsApp template rules + pricing | DONE (2026-07-28) | **Template IS mandatory for the order-created push** (business-initiated, outside any 24h window) — LLM/own-software changes nothing; free-form business-initiated sends are rejected. Category = UTILITY (~₹0.115–0.13/delivered msg in India per-message pricing; marketing ~₹0.88 — categorisation matters). Once customer replies/taps a button, a 24h customer-service window opens → all follow-ups free-form + free (that's where Gemini converses). Templates need Meta approval; one template per language (en/hi/gu). QUICK_REPLY buttons "Confirm"/"Cancel" come back as deterministic button payloads. **Cafe project has NO template code — `send_template()` must be built new.** |
| Research — Shopify webhook mechanics | DONE (2026-07-28) | **orders/create webhook confirmed = the way we know an order was purchased.** Full order JSON (id, name, line items, amounts, customer + phone — needs protected-data toggle ON). Subscribe via GraphQL `webhookSubscriptionCreate` (works with our client-credentials token). Verify `X-Shopify-Hmac-Sha256` = **base64** HMAC of raw body with app client secret (≠ Meta's hex scheme). Must ack 2xx **within 5s** → ack fast, process after. Retries ~19× over 48h with backoff; **after 19 consecutive failures Shopify DELETES the subscription** → needs monitoring + a re-subscribe safety check. |
| Client Shopify theme exploration (E:\shopify_bhavesh\personal_june_theme) | DONE (2026-07-28) | Full report: `docs/theme-exploration-thetavas.md`. Base = "Jhango Theme V1" + Tavas redesign sections. **No WhatsApp/confirmation app trace in theme** (that software is server-side — confirms our replacement needs zero theme changes). ⭐ KEY FINDING: **Shopflo one-click checkout is LIVE** (`bridge.shopflo.com` in theme.liquid:44) — it owns the COD/prepaid checkout flow; must inspect a real order JSON (note_attributes/tags/gateway/phone placement) before finalizing the webhook handler, and Shopflo is a strong candidate for being the current confirm/cancel sender (asked in client-decisions Q7). Also: dead `cashfree.liquid` (Zecpe→Cashfree→Shopflo churn); `custom.js` had obfuscated eval() code stripped — check LIVE theme for un-cleared version; no customer/order templates; storefront never touches tags. |
| **Architecture plan — v1.1 (owner-approved + independently reviewed)** | **✅ REVIEW DONE (2026-07-28): SOUND WITH FIXES — all amendments accepted & folded in** | Full report: `docs/architecture-review-2026-07-28.md`; amendments in `docs/architecture-plan.md` header + Level 4. No restructure needed. Key fixes: durable **outbox** (serverless has no safe "process after 200"); **send-mode kill switch** off/shadow/allowlist/live (shadow = parallel-run strategy); **InboundButton** (template taps = `type:"button"`, cafe parser would drop them silently); **AuthorizedOrder** gate on all mutations + DB-backed message dedupe; two-phase cancel tagging; two-tier token w/ 12h refresh-ahead; `app/jobs/` + CRON_SECRET; client decisions as CONFIG (tags as lists → Q13 dual-write = config edit); 4 new tables (outbound_messages, pending_actions, order_actions, processed_messages); launch checklist += WABA messaging-tier/business-verification check. 3 new error_learnings entries. **ADRs-001..005 = first Phase 1 task.** ⚠ Needs owner OK: one-line CLAUDE.md Critical Rule 3 amendment ("Meta message id LRU" → "LRU fast path + DB uniqueness as authority"). |
| WhatsApp templates — drafts ready | DONE (2026-07-28) — awaiting owner wording approval, then create via API | `docs/whatsapp-templates.md`: T1 `order_confirmation_cod` (UTILITY, en/hi/gu, Confirm/Cancel quick-reply buttons, ≤25-char caps checked, API JSON included) + optional T2 prepaid no-button variant (needs Q1=C). Design locked: language from order `customerLocale`; `sent_wamid → order_gid` stored for language-independent tap routing. Owner confirmed template approval is fast (2–3 min) once created on the WABA. |
| 🔴→🟢 Phase 0 — external verifications | **4 of 6 CLOSED (2026-07-28)** — live API test on real store, results in `docs/phase0-verification-results.md` | ✅ (b) app-org ownership — closed by behavior (grant succeeds = own-org app); ✅ (c) protected data — order-level PII (email/phone/addresses, E.164 +91) returned; ✅ (d) 2026-07 verified E2E (token, order read, name search, tagsAdd + orderCancel schema-validated safely on bogus id; 2025-07 confirmed dead — silently served as 2025-10); ✅ (e) real order inspected — `sourceName "Created by Shopflo"`, COD = `paymentGatewayNames`, tags `[COD, COD pending, HIGH_RISK, Shopflo]`, order names have no `#`. ✅ (a) PARTIALLY CLOSED (owner 2026-07-28): **number IS registered on Cloud API**, on a separate Meta account (NOT the cafe project's) — remaining: hand over WABA ID / Phone Number ID / system-user token / app secret (see "required from owner" list); ✅ (f) CLOSED (2026-07-28): current tool = **WATI**; the Vercel bridge is the OWNER'S own repo (github.com/C0bhavesh/tavas-wati-webhook — analyzed in `docs/current-wati-bridge-analysis.md`). Bridge only syncs contact attributes INTO WATI (never sends messages); WATI's own automations send the confirm/cancel flow and write tags back via WATI's Shopify integration. Tag vocabulary locked: `COD pending` / `Confirmed by wati` / `Cancel by wati`; COD marker = tag `cod`, prepaid = tag `online`; display number = "tavas"+order_number. Bridge has NO HMAC, NO idempotency, NO DB — our build fixes all three. NO tracking logic in the bridge (tracking msgs, if any, come from WATI automations or a2ship — ask client). Remaining from Q7: which WhatsApp NUMBER WATI uses (overlap risk with our number). NEW Q13 added (tag-name compatibility for ops/a2ship filters). **a2ship mechanism also discovered:** fed by admin-created webhooks to `pro.ad2ship.com` (order-create + order-update), fully independent of us. **Cutover rule: NEVER delete the two a2ship webhook rows; the two tavas-wati rows are the old tool's OFF switch on cutover day.** Our own subscription = app-created via `webhookSubscriptionCreate` (auto, self-healing, signed with OUR client secret, invisible on the admin Notifications page — by design). ⚠ Store webhook signing secret was exposed in a screenshot (2026-07-28) → **as of 2026-08-14 this is no longer "not ours": the app now accepts Admin-created-webhook deliveries too (dual-secret HMAC, commit `ddd7952`), so this exposed secret would be a valid forgery key against our endpoint if entered unrotated.** MUST rotate in Shopify Admin → Settings → Notifications BEFORE it is entered into the admin panel. | **NEW: ❌ `read_customers` scope NOT granted** → customer search + `order.customer` ACCESS_DENIED. Root-cause lead (2026-07-28): granted list contains `customer_read_customers` = **Customer Account API** scope → owner likely toggled the wrong API section; fix = Admin API scopes → release app version → reinstall → fresh token → re-test. Impact contained: owner confirmed **DB-mapping as primary resolution** (webhook + 90-day backfill + daily reconciliation sweep — all verified to work with current scopes), making the customers fallback optional. |
| 🟢 **CLIENT FLOW DECISIONS — ANSWERED (2026-07-29)** | ✅ D-1/D-2/D-3/D-4 + Q14 CLOSED; Q8 pending explanation | **D-1 (Q1) = B — EVERY order gets the push**, not COD-only. Cost + prepaid-cancellation risk flagged to owner (prepaid customers now see a Cancel button on an already-paid order); revisit as config if accidental cancels appear — `push_eligibility` is already a config value (ADR-005), so this is a config edit, not a rebuild. **D-3 (Q3) = English + Hindi + Hinglish.** ⚠ Meta has NO "hinglish" template language code → **templates ship en + hi only**; Hinglish is served in the free-form conversation after the customer replies (no approval needed). Gujarati templates are already approved — kept dormant, zero cost, enable any time. **D-4 (Q4) = A** — cancel is double-checked ("are you sure?") before the mutation fires. **D-2 (Q5) = YES** — bot may reveal order STATUS (confirmed/cancelled/shipped) in addition to order id + email. Items/amounts/tracking stay hidden. **Q14 = owner delegates FAQ/policy CONTENT to us** — we seed sensible Thetavas defaults; client edits them from the admin panel at runtime (no redeploy). ⏳ **Q8 (switchover) still open** — explanation sent to owner 2026-07-29. |
| **Phase 1 — ADRs 001–005** | **DONE (2026-07-28)** | `docs/architecture-decisions.md` — outbox, send-policy kill switch, DB-persisted token, structural mutation safety, client-decisions-as-config. Binding for all implementation. |
| Phase 1 — Skeleton + Shopify client (TokenManager + 5 ops) | **REVIEW (2026-07-28)** — built, **LIVE SMOKE PASSED**; code-reviewer running | All 11 tasks DONE test-first (commits `2a4f0f3..f5748f9`): 38 tests passed, ruff + mypy strict clean, secrets grep EMPTY. 3 minimal deviations (line split, unused import, `type: ignore[call-arg]` — all logged). **Live smoke vs real store: token grant OK · find_order_by_name → tavas3733 OK · get_order OK · customer fallback [] (scope pending, expected) · tagsAdd/orderCancel bogus-gid → clean userErrors.** New learnings logged (editable-install import shadowing; pydantic-settings mypy call-arg). Next: code-reviewer verdict → security-reviewer (creds/token surface) → Phase 2 plan (webhook receiver + outbox + Postgres; needs Supabase DSN). |
| Phase 2 — Shopify webhook receiver + outbox + Postgres | **BUILT — REVIEW (2026-07-28); orchestrator E2E verification PASSED** | All 10 tasks TDD, commits `b99e5f0..4fc59e7`. **95 passed, 2 skipped** (Postgres tests skip w/o TEST_DATABASE_URL), ruff + mypy (26 files) + secrets grep clean. Deviations: asyncpg mypy override (no py.typed), 5 line reflows, 1 unused import removed, Phase 1 security state preserved. **Orchestrator verified with a real Shopflo-shaped signed payload:** valid → 200 `{duplicate:false, queued:true}` · replay same webhook-id → `{duplicate:true, queued:false}`, outbox stays 1 · bad signature → 403 · **hex (Meta-scheme) signature → 403** (proves the two-verifier separation) · phone `+91 96642 90413` → `+919664290413` from shipping fallback · `hi-IN` → `hi` · COD detected · outbox payload carries template/language/name/order/amount. **Code review: APPROVED WITH NITS** — ADR-001 atomicity confirmed (dedupe+mapping+outbox in ONE transaction, in-memory mirrors it), HMAC-before-parse confirmed, no-try/except→5xx-retry posture confirmed, layering + registries clean. 2 MEDIUM follow-ups: asyncpg rowcount suffix-parse fragility (also in security fixes) + **F20 gap: self-heal detects URL drift but NOT API-version drift** (queued). LOW: no test for unset-secret 403; job-exception 500 untested; 3 pre-transaction DB reads on the <5s path (latency note). **Security review: FAIL → all fixes dispatched** (see row below). |
| **Phase 2 — COMPLETE & CLOSED (2026-07-28)** | ✅ built + both reviews + all fixes + orchestrator E2E & PoC verification + pushed | Final: **125 passed, 2 skipped** (Postgres tests await Supabase), ruff/mypy/secrets clean. Commits `b99e5f0..08492cb`, all on GitHub. Code-review follow-ups closed in `08492cb`: **F20 version-drift self-heal** (subscription now re-bound when `apiVersion.handle` ≠ `Settings.shopify_api_version` — a future API bump self-corrects instead of silently serving stale payloads; new `ShopifyClient.api_version` accessor), jobs dispatcher returns **502 without vendor detail** on ShopifyError, unset-secret fail-closed test. Learning logged: empty stored secret vs absent key take different fail-closed branches. **What works now:** a signed Shopify order → verified → atomically recorded (dedupe + phone→order mapping + queued message) → 200. Sending is Phase 3. |
| Phase 2 — security fixes | **ALL CLOSED + INDEPENDENTLY RE-VERIFIED (2026-07-28, commit `7d325dd`)** | **120 passed**, 2 skipped (+25 tests); ruff/mypy/secrets clean. Orchestrator re-ran all 8 attack PoCs against the fixed code — every previous 500 is gone: non-ASCII sig header→403 · non-ASCII cron header→503 · type-confused SIGNED payload→200 · 3000-deep JSON→200 · 2MB body→413 · foreign shop-domain→403 · 200k name→stored clipped to 256 · corrupt master-key ciphertext→403. `statement_cache_size=0` added (Supabase pooler). Pushed to GitHub. |
| Phase 2 — security review | **FAIL (2026-07-28) — 3 HIGH; ALL FIXED** | Report: `docs/security-review-2026-07-28-phase2.md`. Core held under attack (base64-HMAC-before-parse, no injection/bypass/leak; jobs endpoint fail-closed with no job-existence oracle; **forged-webhook-id replay still cannot double-send** because dedupe_key derives from the signed body). Failures were exception-robustness on a public boundary: (H1) non-ASCII header → `hmac.compare_digest` TypeError → **500 in the verifier**; (H2) same on cron endpoint; (H3) **type-confusion 500s on SIGNED payloads** (numeric phone, scalar customer, non-list gateways, non-str locale) — these burn Shopify's 19-failure budget → **subscription deletion**. +5 MEDIUM (RecursionError, no body cap, VaultError→500 on key rotation, no shop-domain check, no field-length caps) +3 LOW incl. **`statement_cache_size=0` needed for Supabase's transaction pooler** (would 500 the webhook). Deferred+tracked: pre-auth secret caching/rate limiting, body-derived idempotency key, public_base_url allowlist, app_env prod default, payload_json PII duplication, structured logging. |
| Phase 2 — plan | DONE (2026-07-28) | Plan: `docs/superpowers/plans/2026-07-28-phase2-webhook-outbox-postgres.md` (10 TDD tasks, full code): E.164 phone util · full Level 4 schema.sql + apply script · IngestStore port (atomic dedupe+mapping+outbox, ADR-001) in-memory + asyncpg impls · payload parser + language rule + eligibility (cod_only default + 6h staleness, F2/F15) · base64 HMAC verifier · POST /webhooks/shopify · subscription self-heal (create/update on URL drift, F20) · authenticated jobs dispatcher (CRON_SECRET, F11) · Postgres-vs-memory container switch. Outbox DRAIN + sweep/backfill = Phase 3 (need the WhatsApp sender). Postgres tests skipif TEST_DATABASE_URL → **Supabase DSN needed for live DB tests + deploy**. |
| Phase 1 — **COMPLETE & CLOSED (2026-07-28)** | ✅ built + code-reviewed + security-reviewed + all fixes landed + live-smoke passed | Final: **54 tests passed**, ruff + mypy strict + secrets grep clean. Commits `2a4f0f3..8c1a320`. Orchestrator re-verified the H2 fix independently: forged `AuthorizedOrder` now raises, genuine match accepted. Phase 1 deliverables: Settings/SecretVault/ConfigService/InMemory store · TokenManager (ADR-003) · ShopifyClient (5 ops, hardened error taxonomy, injection guards) · AuthorizedOrder invariant (ADR-004) · /health · Vercel entrypoint (bom1) · smoke script. |
| Phase 1 — security review | **PASS WITH NOTES (2026-07-28) → ALL FIXES LANDED (commit `8c1a320`)** | Report: `docs/security-review-2026-07-28-phase1.md`. Credential handling CLEAN (no secret leaks at rest/in logs/exceptions/git history; vault, token storage, tests all verified). **2 HIGH found with working PoCs:** (H1) `find_customer_orders_by_phone` search-operator injection — the phone sibling of the ae94609 order-name guard was missed; sits on the Phase 3 ownership path → could resolve a DIFFERENT customer's orders (PII + wrongful cancel); (H2) `AuthorizedOrder` had NO runtime invariant — ADR-004's "structurally impossible" was docstring-only, a forged token passed `cancel_order`. Also 3 MEDIUM (public OpenAPI in prod; smoke script printing PII; smoke script firing real mutations) + 4 LOW (token-prefix print, corrupt `token_expires_at` → permanent outage, English-substring ACCESS_DENIED match, implicit redirect policy). **Deferred to Phase 2/3 (in report):** partial-data error surfacing, ConfigService key classification, required `database_url`/Literal `app_env`, ruff `S` ruleset, lockfile + `.vercelignore`, `_redact()` before APM. |
| Phase 1 — code review | **APPROVED WITH NITS → ALL FIXES LANDED (2026-07-28, commit `ae94609`)** — suite 44 passed, ruff/mypy/secrets clean | 2 MEDIUM (unguarded non-401/non-JSON responses in client._graphql; unguarded token payload parse in _grant), 1 LOW (order-name query input hardening pre-Phase 2), 3 NITs (test import fixed now; /health response model + AuthorizedOrder factory deferred — factory noted for Phase 4/5 order_resolver). Reviewer verified layering, ADR-003/004 enforcement, test quality, secret handling, deviations — all sound. Security review runs after fixes land. |
| **Phase 3 — COMPLETE & CLOSED (2026-07-28)** | ✅ built + code-reviewed (1 blocker + 1 major, both fixed) + security-reviewed (1 HIGH + 3 MED + 2 LOW, all fixed) + independently re-verified by a second security-reviewer pass — **APPROVE**, merged into `main` locally (NOT pushed) | Final commit on the worktree branch before merge: `ee26e23`. Re-verification independently re-derived all 6 fixes with fresh PoCs (not the fix report): tenant guard confirmed fail-closed incl. partial-tenant-match batches; batch parsing confirmed lossless incl. partial-replay dedupe; sender crash-hardening confirmed via 18+ fuzzed shapes; config repr + empty-secret HMAC confirmed. **One non-blocking follow-up logged, must close before Phase 4/5 persists `SendResult.error` to a DB/APM sink:** `_safe_error`'s token redaction only covers the `message` field — `code`/`type`/`error_subcode` are copied verbatim and could carry a token-shaped string from a malformed/proxied error body. Currently unreachable (nothing persists `SendResult.error` yet). Also noted, not blocking: `extract_event()` (singular, test-only, zero app callers) retains the old permissive no-tenant-arg default — consider deleting or making the tenant arg mandatory in Phase 4 so the fail-open shape can't be reintroduced. |
| Phase 3 — security review round | **CHANGES REQUESTED → ALL 6 FIXED, back to REVIEW (2026-07-28)** on worktree `worktree-phase-3-whatsapp-channel` (NOT merged, NOT pushed) | Security review flagged 1 HIGH + 3 MEDIUM + 2 LOW; all closed TDD (RED→GREEN). **HIGH** — tenant `phone_number_id` guard failed OPEN (None sentinel collapsed "absent"/"wrong-type" with "ok-to-skip"; caller only checked `is not None`): folded the check into `extract_events(payload, expected_phone_number_id=...)`, fails CLOSED (present + `str` + `==` ours), regression tests for missing metadata / non-str id / mismatch. **MED1** — Meta 4xx error bodies (which echo the bearer token) stored verbatim in `SendResult.error`: `_safe_error` keeps only `code/type/error_subcode`, redacts `EAA…`-shaped substrings + the configured access token, generic fallback for non-JSON. **MED2** — only `messages[0]`/`entry[0]`/`changes[0]` parsed (Meta batches → silent permanent loss on acked delivery): `extract_events` iterates ALL entries/changes/messages, dedupes each via `record_if_new`, response now `{ok, processed, duplicate, results:[{message_id,duplicate,event_type}]}`; two-message batch test. **MED3** — unhandled exceptions on a 2xx body with unexpected JSON shape: `_extract_wamid` guards decode + nested `.get` with `isinstance`, degrades to `wamid=None`; tests for non-JSON 200 / `messages` not-a-list / `[1]` / `[{}]`. **LOW1** — `WhatsAppConfig` secret fields → `field(repr=False)` (no plaintext in tracebacks/Sentry; `__eq__` preserved). **LOW2** — `verify_meta_hmac` now `if not secret: return False` first line + empty-secret test. **DEFERRED as instructed (untouched):** GET-verify-route rate limit (tracked Phase 2 pre-auth deferral). Suite **218 passed, 3 skipped**; ruff + mypy strict (32 files) + secrets grep clean. 2 new error_learnings logged. Ready for security re-review. |
| Phase 3 — WhatsApp channel (the pipe) | **BUILT — REVIEW (2026-07-28)** on worktree branch `worktree-phase-3-whatsapp-channel` (NOT merged, NOT pushed) | All 9 TDD tasks done. **173 passed, 3 skipped** (Postgres MessageStore tests skip w/o `TEST_DATABASE_URL`), ruff + mypy strict (32 files) + secrets grep clean. Files: `whatsapp_signature.py` (hex HMAC) · `whatsapp_config.py` (Fernet-split loader) · `whatsapp_inbound.py` (InboundText/InboundInteractive/**InboundButton** F4 + `extract_event`) · `whatsapp_sender.py` (send_text/send_template/send_buttons + SendResult/WhatsAppSendError) · `copy.py` (deterministic en/hi/hinglish/gu) · `whatsapp.py` (GET verify + POST receive router) · `MessageStore` port + in-memory + Postgres impls · `deps.Container.messages` wired · router mounted in `main.py` · `scripts/smoke_whatsapp.py`. Deviations: (1) test lines reflowed for ruff `line-length=100` (3 files); (2) `PostgresMessageStore` added in Task 7's commit (not Task 8) since `postgres.py` already exists and `deps.py` imports it — Task 8 adds only its gated test + container-switch assertion; (3) `PostgresMessageStore.record_if_new` uses the repo's existing `_rows_affected(tag) > 0` helper (matches `PostgresIngestStore`) instead of the plan's `.endswith("0")` (suffix-parse fragility flagged in Phase 2). **Pipe only** — no engine/order_resolver/knowledge/mutation dispatch (Phase 4/5 attach at the `event_type` seam). Interpreter: codex-runtimes Python 3.12.13 (no project venv on this machine). **Next: code-reviewer (changed files), then security-reviewer (webhook auth + secrets surface).** OPEN: `copy.py` wording is an owner/client sign-off item before Phase 4 wires it in. Original plan: `docs/superpowers/plans/2026-07-28-phase3-whatsapp-channel.md`. |
| Phase 3 — plan | DONE (2026-07-28) | Plan: `docs/superpowers/plans/2026-07-28-phase3-whatsapp-channel.md`. Not yet executed — needs owner confirmation of Supabase DSN status (or proceed in-memory for now) and re-provided Meta secrets before the `developer` agent runs it. |
| Phase 4 — Flows A+B E2E on test number | PENDING (gate: Phases 2+3 + D-1/D-3/D-4) | order → template → confirm/cancel round-trip. |
| **OWNER CORRECTION (2026-07-28) — the inbound conversation IS the feature; phases RE-SEQUENCED** | ✅ full design written: `docs/inbound-conversation-design.md` | Owner: "that's the whole feature main point… how do you architect when customer messages?" — correct. The LLM/knowledge/reply path was scheduled last and under-described. Now designed end-to-end (10 steps: Meta HEX-HMAC → typed event → DB dedupe → deterministic button dispatch BEFORE the LLM → order_resolver identity+ownership → live Shopify re-fetch → knowledge-assembled prompt → strict-JSON Gemini → hardened parse → intent-not-action → persist) with a module-by-module map of what is `[copy]`/`[adapt]` from `D:\ai_whatsapp_agent`. **New phase order: 3 = WhatsApp channel (pipe) · 4 = THE CONVERSATION (providers+knowledge+engine+resolver+memory, live on the test number) · 5 = order-push/Confirm-Cancel automation · 6 = cutover.** Phases 1–2 remain load-bearing for it: the `order_mappings` table IS how "which orders belong to this WhatsApp number" gets answered (the cafe bot never had to ask that). |
| Knowledge base + deterministic reply copy — added to architecture | Level 3 updated 2026-07-28; builds in Phase 4 | The Level 3 module list carried over the cafe **engine** but not the cafe **`app/knowledge/`** package that feeds it (loader + assembler + cache + seeds). Without it the bot has no store voice and no policy knowledge — it could answer order status from Shopify but would improvise on "when will it arrive?" / "what's your return policy?". Now added: `app/knowledge/` (brand_voice.md, faq.json, business.json, patterns.json — **no menu.csv**, we don't sell in chat) + **`app/channels/copy.py`** for deterministic reply strings (confirm-success, cancel-confirm, not-found, refusals, error fallbacks) in 4 languages — these follow button taps/mutations so they must be fixed, never LLM-generated. **Client dependency: the FAQ/policy CONTENT must come from the client → new question Q14** in `client-decisions-all.md` (delivery times, returns, COD rules, damaged-goods, support contact, tone). Content is admin-editable at runtime (no redeploy). |
| Phase 5 — Flow C Gemini Q&A + knowledge base | PENDING (gate: Phase 4 + D-2 + Q14 content) | LiteLLM/Gemini provider layer [copy from cafe] · order-intent JSON engine w/ hardened parser [adapt] · `order_resolver` ownership chain [NEW] · `app/knowledge/` loader+assembler+cache+seeds [adapt] · windowed memory [copy] · hi/en/hinglish/gu. Implementation plan not yet written (written one phase ahead, by design). |
| Phase 6 — Parallel run + cutover | PENDING (gate: client Q8) | Run alongside current tool, then switch same-day. |

| Deploy workflow — GitHub → Vercel CI/CD | **✅ VERCEL IS CONNECTED AND AUTO-DEPLOYING (confirmed 2026-08-13) — corrects the stale "deferred" note below** | Project `tavas/thetavas-bot` (Vercel CLI confirms, logged in as `c0bhavesh`), production alias `thetavas-bot.vercel.app` live for 14+ days, auto-deploys on every push to `main` (deployment history shows continuous Production deploys going back 6+ days at minimum, i.e. this has been true across multiple recent sessions, not just today). Verified 2026-08-13 by hitting `/health` on the production alias → `200 {"status":"ok","service":"thetavas-order-bot"}`, matching the just-pushed commit. `DATABASE_URL` and `ADMIN_PASSWORD` are set as Vercel project env vars (owner-confirmed). **CLAUDE.md rule 7 is in full effect for every push from now on — treat every `git push origin main` as an immediate production deploy, not a staging step.** Open question: whether Shopify/Meta webhooks are actually pointed at this deployment yet, or whether WATI is still the live traffic handler (cutover per Q8 not yet confirmed done) — deployment being live does not by itself mean it's receiving real customer webhook traffic. — Original repo-move history (2026-07-28, still accurate): repo created under `C0bhavesh/watsapp` (private) after a different-GitHub-account gap blocked using the original `Devpanchal37/thetavas-order-bot`; owner approved pushing under `C0bhavesh` anyway. |
| **First WhatsApp template message DELIVERED (2026-07-28)** | ✅ Meta channel E2E proven pre-code | `order_confirmation_cod` **hi + gu APPROVED ~minutes; en PENDING at check** (retry shortly). Hindi template sent to owner's verified test number (+91 7575072795) with sample data + both quick-reply button payloads (`order:confirm/cancel:gid://...TEST123`) — Meta accepted (`message_status: accepted`). NOTE: button taps currently go nowhere (no webhook yet — that's Phase 2/3); payload format already matches the v1.1 design. |
| Meta dev setup — WABA verified + templates created | **DONE / LIVE-VERIFIED (2026-07-28)** | App ID `771818472295971` ("Demo") · Test number `+1 555-651-8147` (quality GREEN, CLOUD_API) · Phone Number ID `1298805403309058` · WABA `2454816495000045` ("Test WhatsApp Business Account"). **Permanent system-user token working** (never expires; system user = "Conversions API System User" — WABA asset assignment was initially missing, fixed by owner; a dedicated `thetavas-bot` system user is a later nice-to-have). App secret received. Secrets in session scratchpad only → go into Vercel env/Fernet vault in Phase 1. **All 3 templates created via API: `order_confirmation_cod` en/hi/gu — category accepted as UTILITY (₹0.12 pricing assumption validated), status PENDING** (owner says ≈2–3 min to approve; re-check next session). Remaining for first live send: owner adds 1–5 verified test-recipient numbers on the API Setup page + shares one to message. |

## Deferred — logged for later

- **🟠 Phase 5 deferred / later (2026-08-10)** — items intentionally OUT of the confirm/cancel v1,
  none of which block the flow working:
  1. ~~**Self-invoke-after-webhook latency**~~ **RESOLVED via cron, NOT self-invoke (2026-08-13,
     corrected):** the 2026-08-13 self-invoke design (`send_outbound_now` via `BackgroundTasks`) was
     REVERTED — both code-review and security-review rejected it (Vercel Python `BackgroundTasks`
     run in-request, delaying the ack; contradicts ADR-001 + the 2026-07-28 error_learning). The
     actual fix: a **1-minute Vercel Cron** (`vercel.json` `crons` → `GET /internal/jobs/outbox_drain`)
     is now the sole sender. The webhook is back to pure queue-writer (persist → 200, nothing else).
     `dedupe_key UNIQUE` still guarantees exactly-once; push latency ≤ ~1 min.
  2. ~~**`FOR UPDATE SKIP LOCKED` multi-instance drain**~~ **DONE (2026-08-13):** `claim_queued_outbound`
     is now atomic — a `WITH claimed AS (SELECT id ... WHERE state='queued' ... FOR UPDATE SKIP
     LOCKED) UPDATE ... SET state='processing' ... RETURNING ...` single statement, so two overlapping
     cron ticks can never both claim the same row. `bump_outbound_attempt` returns a transient-failed
     row `processing → queued` for a later tick (or `→ failed` at the cap), never leaving it stuck.
     Required now that the cron (not a one-shot self-invoke) is the sender. Real-DB concurrency proof:
     `tests/store/test_outbox_drain_pg.py::test_concurrent_claims_never_return_the_same_row`.
  3. **Literal-YES free-text cancel fallback** — v1 is buttons-only. A `pending_actions` TTL row +
     a "reply YES to confirm" free-text path (for customers who type instead of tapping) is
     deferred; the `pending_actions` table already exists for it.
  4. **Proactive shipped/cancelled push topics** (`orders/fulfilled`/`orders/cancelled`) — each is
     a paid template send and a future client decision (see the existing deferred note below).
  5. **`reconcile_cancels` cron wiring on Vercel** — `outbox_drain` is now scheduled (2026-08-13:
     `vercel.json` `crons` → `* * * * *` GET, Bearer-authed via `CRON_SECRET`). `reconcile_cancels`
     is registered + CRON_SECRET-authed but has NO Vercel cron schedule yet; until scheduled it is
     invoked manually via `POST /internal/jobs/reconcile_cancels` (`X-Cron-Secret`). Note
     `vercel.json` now carries a top-level `crons` key alongside `builds`/`routes` — additive and
     NOT mutually exclusive (unlike `functions`, see the `maxDuration` item below); confirm the
     schedule actually fires after the next deploy.

- **⏳ Vercel dashboard: set function `maxDuration` (Task 14 security-review CRITICAL fix,
  2026-08-06)** — `backend/vercel.json` is UNCHANGED (still just `builds`/`routes`/`regions`);
  an attempt to add `functions["api/index.py"].maxDuration=60` there was reverted the same day
  because Vercel treats `functions` and `builds` as mutually exclusive (hard deploy-time error:
  "The functions property cannot be used in conjunction with the builds property"), and this
  repo's `builds`/`routes` config is what makes the catch-all ASGI routing work. **Owner
  decision: set `maxDuration` via the Vercel dashboard UI instead (Project Settings →
  Functions) once the project is connected — this is a deploy-time step, NOT a code change, so
  it is tracked here rather than as a code TODO.** The code-side defense-in-depth is already
  shipped regardless: `app/core/conversation.py`'s `run_turn` wraps the whole conversation turn
  in `asyncio.timeout(TURN_TIMEOUT_SECONDS=55.0)`, catching `TimeoutError` and logging a
  WARNING instead of leaving a near-deadline turn to an uncaught platform 504 (which would
  silently drop the customer's reply — `MessageStore.record_if_new` already marks the message
  seen, so Meta's retry of a timed-out delivery is dropped as a duplicate). 55s was chosen to
  sit comfortably under an expected ~60s dashboard setting; **must close before cutover** (same
  urgency as the other pre-deploy items below) since without a platform limit the internal
  guard's assumption ("comfortably under the platform limit") is unverified until the dashboard
  value is actually set. See `docs/memory/error_learnings.md`'s 2026-08-06 entry for the
  Vercel `functions`/`builds` schema conflict.

- **`processed_messages` residual PII on erasure** — tracked, no decode mechanism built. The
  Meta wamid (message_id PK) embeds the sender's phone in cleartext base64, but the table has no
  `phone_e164` column, so `delete_by_phone` cannot phone-scope it without decoding wamids (real
  scope creep, deliberately NOT built). It is only aged out blindly by `purge_older_than`'s
  `received_at` cutoff. Documented in `PostgresIngestStore.delete_by_phone`'s docstring.

- **🔒 Admin-panel pre-deploy security hardening (from Phase 3.5 security review, 2026-07-30)** —
  five items intentionally NOT fixed in Phase 3.5 because they only bite once the panel is on a
  public Vercel URL (Vercel deliberately not connected yet). MUST be closed before the admin panel
  is exposed / before cutover:
  1. **Login rate-limit backing store** (MEDIUM) — `slowapi` uses in-process `memory://`; on
     serverless it resets per cold start and isn't shared across instances, and `get_remote_address`
     returns the proxy IP → the `5/min` guard on the single `ADMIN_PASSWORD` is weak AND an attacker
     could lock the owner out. Fix = Postgres-backed (or Upstash) attempt store + escalating lockout,
     keyed on Vercel's trusted `x-vercel-forwarded-for`. **Owner decision needed at deploy: (a)
     Supabase-backed lockout [recommended, no new vendor] / (b) Upstash Redis / (c) accept for
     single-admin v1 with a mandatory high-entropy password.**
  2. **Session revocation / logout** (MEDIUM) — token payload is only `<exp>`, signed with
     `APP_MASTER_KEY`; no logout, and invalidating a stolen 12h cookie needs rotating
     `APP_MASTER_KEY` (which would destroy all Fernet-encrypted creds). Fix = separate signing
     secret + `session_epoch` in the payload + `POST /admin/logout`.
  3. **`public_base_url` host pinning** (LOW) — accepts any https host and becomes the Shopify
     webhook callback (`jobs/ensure_subscription`); post-compromise, one admin password redirects
     all order PII. Fix = pin to the deploy host from env / allowlist.
  4. **Audit logging** (INFO) — ✅ CLOSED (2026-08-06, REVIEW — ready for code-reviewer).
     Actor-free `_audit(action, outcome, resource)` on `logger("app.admin")` (Vercel function
     logs, no new infra) at: login success/failure/not_configured, credential_set
     (shopify/whatsapp), provider_set (llm:{provider}), knowledge_set (knowledge:{kind}),
     controls_set, erasure (resource=phone). Logs action/resource-NAME/outcome ONLY — never a
     value (passwords, keys, credential contents, knowledge bodies, or the erased phone).
  5. **DPDP retention/deletion** (INFO) — ✅ CLOSED (2026-08-06, REVIEW — ready for
     code-reviewer). Built (safe-without-Q15): (a) **delete-by-phone** `POST /admin/erasure`
     (require_admin, audit-logged, number never logged) → `ingest.delete_by_phone` purges
     order_mappings + outbound_messages (by phone_e164) + conversations/messages (by user_id,
     children-first for FK, defensive — no Phase-4 writer yet) in one txn; (b) **`retention_days`
     config** DEFAULT **0 = disabled** (no policy invented — exact period is pending client Q15);
     (c) **purge-by-age** `ingest.purge_older_than(cutoff)` + `run_retention_purge` job wired to
     `GET|POST /internal/jobs/retention_purge` (CRON_SECRET-authed; default no-op, flip on by
     setting retention_days once client answers Q15). +16 tests (327 passed, 6 skipped;
     2 new gated Postgres tests); ruff + mypy strict (46 files) + secrets grep clean.
     NOT committed yet.
  Also two Phase-4 code follow-ups (NITs): broad `except` in `providers/`; dormant
  `accept_on_rate_limit` (wire a runtime reader when a provider needs it). ~~NOTE: `send_mode` kill
  switch is stored but has NO runtime reader yet~~ **DONE (Phase 5, 2026-08-10):** the outbox drain
  hard-gates on `send_mode` in one place (`off` returns before the queue is touched; `shadow`/
  `allowlist`/`live` decided per-row by `send_policy.send_decision`), and the confirm/cancel
  button-tap dispatch is gated the same way at the webhook — so the panel's "off = nothing is
  sent / nothing mutates" claim is now enforced by code.

- **Proactive order-update notifications (`orders/cancelled` / `orders/fulfilled` topics)** —
  design locked as additive plug-ins (topic-agnostic receiver, see architecture-plan §1.3
  webhook topic strategy, 2026-07-28). v1 = `orders/create` only + re-fetch-on-demand for all
  update needs; `orders/updated` deliberately not subscribed (noise + self-echo from our own
  tagsAdd). Turning on proactive shipped/cancelled pushes = future client decision (each is a
  paid template message).

- **Address change flow (CHANGE ADDRESS)** — client: future requirement, not v1. orderUpdate research already done.
- **a2ship integration / tracking status in chat** — client: "no idea" yet. If tracking answers are wanted, need to know whether tracking lands back in Shopify fulfillments or only in a2ship (would need a2ship API).
- **Cancel-after-fulfillment/shipping behavior** — client: on hold. v1 rule pending decision (Q6 in client-decisions).
- **What the bot may reveal** — currently order id + email only; items/amount/payment status later (client point 8).
- **Human handoff alerts** — client: yes but details later (cafe D6 pattern ready to reuse).
- **App ownership + protected-data audit** (was open-questions #10) — verify app lives under client's org, scopes `read_orders, write_orders, read_customers`, protected customer data toggle ON. Must happen before live webhook test.
- **Existing 3rd-party confirm/cancel app switchover** — replicate first, run in parallel or hard cutover? Ask client when v1 is testable. Identify which app it is (theme scan running).

## Pending client verification — ON HOLD

> All client questions are consolidated in `client-decisions-all.md`. Send that one doc.

- ~~Q16 (2026-08-12) — phone-match scope for the database-first order-Q&A lookup.~~ **ANSWERED
  + IMPLEMENTED (2026-08-12): use the shipping mobile number.** Matches a real data constraint —
  checkout doesn't always capture the buyer's own phone on the order record, but shipping phone is
  reliably present (same reason the order-confirmation push already falls back to it). Billing
  stays out of scope. See `client-decisions-all.md` Q16. `find_mirrored_orders_by_phone`
  (`app/store/postgres.py` + `memory.py`) now matches `o.phone OR o.shipping_phone`; the earlier
  conservative `o.phone`-only default (commit `26e3d9e`) is replaced. Tests in
  `tests/store/test_order_mirror.py` updated (shipping-only now returns; billing-only still
  excluded). 712 passed / 24 skipped, ruff + mypy strict + secrets grep clean;
  `order_actions.py` diff vs `e13d645` confirmed empty. Committed locally, NOT pushed — Main Claude
  routes through review + pushes after approval.

| Module / Feature | Status | Notes |
|---|---|---|
| **Phase 5.5 — Database-first order reads for chat Q&A (`MirrorOrderSource`)** | **BUILT + REVIEWED (2026-08-12)** — code review APPROVED (after 1 fix round); security review found 2 code-level MEDIUMs (fixed) + 1 MEDIUM routed to client as Q16 (ON HOLD, shipped conservative default) + several LOW/INFO items | Design: `docs/superpowers/specs/2026-08-12-mirror-first-order-reads-design.md`. Plan: `docs/superpowers/plans/2026-08-12-mirror-first-order-reads.md` (5 tasks, all executed). Adds `MirrorOrderSource` (`app/core/mirror_order_source.py`), a database-first `OrderSource` adapter used ONLY by the order-tracking Q&A pipeline (`conversation.py`'s `_agent_reply`) — reads the `customers`/`orders`/`order_items` mirror first, falls back to live Shopify on a miss or DB error. The Confirm/Cancel mutation path (`order_actions.py`, `resolve_by_gid`) is untouched and verified untouched by both reviews (empty diff across the whole range) — still talks to live Shopify directly, per Critical Rule 3. Code review round 1: 1 MAJOR (unbounded/N+1 `find_mirrored_orders_by_phone` — the primary read path for backfilled customers since the backfill script never populates the `order_mappings` fast-path table) + 2 MINOR, all fixed (capped `LIMIT 10` + batch line-item fetch). Security review: MEDIUM (missing mutation-path regression test proving `order_actions.py` can't be repointed at the mirror — needs a fix), MEDIUM (phone-normalization mismatch between mirror-sourced (`_e164`-normalized) and live-Shopify-sourced (raw) phones weakens the `AuthorizedOrder` ownership-check semantics), MEDIUM → **Q16 above**, plus LOW items (bare-`Exception` swallow could log PII via exception text, missing order-name format guard on the mirror lookup for parity with the live client, `ORDER BY` NULLS-FIRST-on-nullable-column bug). **Status at time of writing: fix round in progress before push.** |
