# Error Learnings

> Append an entry whenever a non-obvious issue is encountered and solved, or when the human corrects a mistake. Agents read this before starting new work to avoid repeating known mistakes.

## Format
## [YYYY-MM-DD] Short Title
**Mistake/Issue:** what went wrong or was non-obvious
**Correct:** what should be done instead
**Pattern:** the general rule going forward

---
<!-- entries below -->

## [2026-07-28] Shopify orders are NOT searchable by phone
**Mistake/Issue:** `orders(query:"phone:...")` silently ignores the phone filter — it looks like it works but returns unfiltered results.
**Correct:** maintain our own `order_mappings` (phone_e164 → order_gid) filled from the `orders/create` webhook; fallback: `customers(query:"phone:...")` → `orders(query:"customer_id:...")`.
**Pattern:** verify every Shopify query filter against shopify.dev's supported-filter list before designing around it.

## [2026-07-28] Two different webhook HMAC schemes in one app
**Mistake/Issue:** Meta and Shopify both send HMAC-SHA256 signatures but encode them differently — copying the cafe verifier for Shopify would silently reject every webhook.
**Correct:** Meta `X-Hub-Signature-256` = **hex**; Shopify `X-Shopify-Hmac-Sha256` = **base64**. Two verifiers, both on the RAW body, constant-time compare.
**Pattern:** never reuse a signature verifier across providers without checking the encoding.

## [2026-07-28] Template quick-reply taps are message type "button", NOT interactive.button_reply
**Mistake/Issue:** A tap on a TEMPLATE quick-reply button arrives as `type:"button"` with `button.{text,payload}` + `context.id`; the cafe inbound parser only handles `type:"interactive"` (`interactive.button_reply`) and returns `None` for unknown types — so copying it wholesale makes every Confirm/Cancel tap fail SILENTLY (found by architecture review F4, before any code was written).
**Correct:** add an `InboundButton` variant to the typed union; template sends must attach an explicit per-button `payload` (≤128 chars, e.g. `order:confirm:{gid}`) or Meta returns only the language-dependent button text.
**Pattern:** when copying a channel parser across a message CLASS (template vs interactive), re-verify the inbound webhook shape against the provider's reference — not just the outbound payload.

## [2026-07-28] Vercel Python has no reliable "process after response" — use a durable outbox
**Mistake/Issue:** The plan said "ack Shopify 2xx <5s, process after" — but FastAPI BackgroundTasks on Vercel's buffered Python runtime run INSIDE the request cycle (delaying the response), and post-response work can be killed on instance reclaim. Combined with webhook-id idempotency, a lost send is never retried: customer silently gets no message (review F1).
**Correct:** webhook handler = one short DB transaction (dedupe insert + mapping upsert + `outbound_messages` queue row) → 200. A separate cron/self-invoked drain sends from the outbox with retries; `dedupe_key` UNIQUE makes "one push per order ever" a DB invariant.
**Pattern:** on serverless, anything that must survive the request needs a durable queue/table — never in-process background work.

## [2026-07-28] Cafe project is pip-installed editable and hijacks our `app.*` imports
**Mistake/Issue:** The dev machine has the cafe reference project (`beyond_loaf_agent`, `D:\ai_whatsapp_agent\backend`) installed as an editable package. Its `_EditableFinder` on `sys.meta_path` maps the top-level `app` package to the cafe dir. Because it runs AFTER the standard `PathFinder`, `import app` resolves to OUR `backend/app` — BUT any `app.<child>` submodule that does NOT yet exist in our package falls through to the cafe project (e.g. `import app.deps` pulled in `D:\ai_whatsapp_agent\backend\app\deps.py` and blew up on `from app.config.crypto import Crypto`). This produced a confusing RED (cafe ImportError instead of a clean `ModuleNotFoundError`).
**Correct:** it resolves itself as soon as our `app/<module>.py` exists (PathFinder finds ours first). No action needed if you create the module before importing it. If ever isolation is required: `pip uninstall beyond-loaf-agent` in this env, or run tests with `PYTHONPATH` scoping — but do NOT modify the shared env as part of feature work.
**Pattern:** a confusing cross-project ImportError under a same-named top-level package usually means an editable install is shadowing missing submodules — check `site-packages/__editable__*.pth` + `*_finder.py` before suspecting your own code.

## [2026-07-28] A docstring is not an invariant — `AuthorizedOrder` (ADR-004) forged in a PoC
**Mistake/Issue:** `AuthorizedOrder` only *documented* "verified_phone matches the order" — security review forged one for a victim order and `cancel_order` accepted it; the mutation-safety gate was enforcement-in-name-only.
**Correct:** add `__post_init__` that raises `ValueError` unless `verified_phone` is truthy and equals one of the order's phones; fix ripple fixtures (tests/smoke) to build genuinely consistent orders instead of relaxing the check.
**Pattern:** a type that asserts an invariant must validate it at construction — if the guarantee lives only in a docstring/convention, treat it as unenforced.

## [2026-07-28] pydantic-settings `Settings()` no-arg call trips mypy strict
**Mistake/Issue:** `Settings()` (values loaded from env/.env at runtime) fails mypy strict with `Missing named argument "app_master_key" [call-arg]`, because the required field has no default and the pydantic mypy plugin isn't enabled.
**Correct:** annotate the call site with `# type: ignore[call-arg]` (the plan already does this in `test_settings.py` for `Settings(_env_file=None)`). Used in `app/deps.py` and `scripts/smoke_shopify.py`.
**Pattern:** required-without-default BaseSettings fields are "required" to mypy even though env supplies them — expect a `call-arg` ignore at every no-arg `Settings()` construction.

## [2026-07-28] asyncpg ships no py.typed — mypy strict rejects `import asyncpg` in app code
**Mistake/Issue:** asyncpg (0.31.0 here) has no `py.typed` marker, so `mypy --strict` flags `import-untyped` on `import asyncpg` in `app/store/pg_factory.py` and `postgres.py`, breaking the `mypy app` gate.
**Correct:** add a per-module override to `backend/pyproject.toml`: `[[tool.mypy.overrides]]` `module = "asyncpg.*"`, `ignore_missing_imports = true`. Added alongside the dependency in Task 2.
**Pattern:** when adding an untyped third-party dep under a `strict` mypy config, expect to add an `ignore_missing_imports` override for it — the dep being installed is not enough.

## [2026-07-28] Plan-verbatim code can exceed the repo's ruff line-length; lint tests too, not just app/
**Mistake/Issue:** Several lines copied verbatim from the Phase 2 plan (a docstring, a ternary, and inline test comments) exceeded ruff `line-length = 100`, and one plan test imported `get_container` unused (F401). Linting only the per-task `app/` files (as the compliance step lists) let the test-file violations reach a later whole-project `ruff check .`.
**Correct:** behaviour-preserving reflow (split the line / move the comment above the statement) and drop the unused import. Run `ruff check .` (whole project, incl. `tests/`) per task, not just on the app files touched.
**Pattern:** a plan's inline code is not guaranteed lint-clean against this repo's config — run the full-project linter each task, and treat test files as first-class lint targets.

## [2026-07-28] hmac.compare_digest raises TypeError on non-ASCII str — compare bytes for header values
**Mistake/Issue:** `hmac.compare_digest(a, b)` with two `str` raises `TypeError` if either contains a non-ASCII char. Starlette decodes HTTP headers latin-1, so an attacker header byte `\xe9` reaches the verifier as a non-ASCII str → 500 on the Shopify HMAC check and the cron `X-Cron-Secret` check.
**Correct:** encode both sides to bytes before comparing — `candidate.encode("ascii")` inside `try/except UnicodeEncodeError: return False` (fail closed), then `compare_digest(expected.encode("ascii"), provided)`.
**Pattern:** any secret/signature compare on a value that originated in an HTTP header must be a bytes compare guarded against non-ASCII input — never `compare_digest` on two raw strs.

## [2026-07-28] Webhook payload fields must be type-coerced — a 500 on a signed delivery deletes the subscription
**Mistake/Issue:** JSON fields were read with the wrong shape assumed (`payload.get("phone")` an int → `normalize_phone` TypeError; `customer` a str → `.get` AttributeError; `payment_gateway_names` an int → not iterable; unvalidated `email`/`financial_status` into asyncpg text params → DataError). Each is a 500 on a signature-valid delivery, and Shopify deletes a webhook subscription after 19 consecutive failures — so one poison-but-signed payload can silently unhook order ingestion.
**Correct:** coerce every field read with `_s`/`_d`/`_seq` helpers (str/dict/list-or-empty) before use, guard `normalize_phone`/`choose_language` against non-str, and route optional DB fields through the parser so a bad value becomes `None`, not a crash.
**Pattern:** on a signed webhook, treat every payload field as attacker-typed — a 500 is not a transient error, it burns the provider's retry budget and can tear down the integration.

## [2026-07-28] Empty stored secret ≠ absent secret — they hit different fail-closed branches
**Mistake/Issue:** To cover the untested "client secret not configured → 403" path I first set the raw config value to `""` (`config_repo.set("shopify:client_secret", "")`). That does NOT exercise the `not secret` short-circuit — `ConfigService.get_secret` sees a non-None raw value and calls `vault.decrypt("")`, which raises `VaultError` (Fernet `InvalidToken` on empty input). So it lands on the SAME branch as the corrupt-secret test, adding no new coverage.
**Correct:** to hit the genuine "unset" branch (`get_secret` returns `None`), rebuild a fresh container WITHOUT seeding the key (`reset_container(); get_container()`), not an empty stored value.
**Pattern:** with encrypt-at-rest config, an empty stored value routes through decrypt (→ VaultError), while a truly-absent key returns None — they take different code paths even though both fail closed to 403. Choose the fixture that targets the branch you mean to cover.

## [2026-07-28] No project venv on this machine — use the codex-runtimes Python interpreter
**Mistake/Issue:** `python`/`python3`/`py` on PATH all resolve to the Windows Store alias stub in `WindowsApps` (no real interpreter, no `pip`), and there is NO `.venv`/`venv` in the repo or the worktree — so `pytest`/`ruff`/`mypy` cannot run via the obvious commands. This blocks all TDD until a real interpreter is found.
**Correct:** the working interpreter is `C:\Users\cbbha\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe` (Python 3.12.13) — it already has every project dep installed (fastapi, pydantic, httpx, cryptography, asyncpg, pytest, pytest-asyncio, ruff, mypy). Run everything as `"$PY" -m pytest|ruff|mypy` from `backend/`. It resolves `import app` to the worktree's `backend/app` correctly (no cafe-editable-install shadowing observed).
**Pattern:** on this Windows machine, when the PATH `python` is a WindowsApps stub and there's no repo venv, reach for the codex-runtimes bundled interpreter before trying to bootstrap a new venv.

## [2026-07-28] Plan code can lag the repo — PostgresMessageStore uses the existing `_rows_affected` helper, not `.endswith("0")`
**Mistake/Issue:** the Phase 3 plan's `PostgresMessageStore.record_if_new` returned `not result.endswith("0")` — but `.endswith("0")` mis-parses any command tag ending in a multi-digit count whose last char is 0 (e.g. `INSERT 0 10`), the exact fragility flagged as a Phase 2 code-review follow-up. `app/store/postgres.py` already ships `_rows_affected(tag)` (parses the numeric suffix) used by `PostgresIngestStore`.
**Correct:** implement `return _rows_affected(result) > 0`, reusing the existing helper, rather than copying the plan's `.endswith` verbatim.
**Pattern:** when a plan's code duplicates logic that already exists in the repo as a hardened helper, port to the helper — the plan is a snapshot and may predate the repo's fixes.

## [2026-07-28] A None-returning "is it ours?" extractor makes the tenant guard fail OPEN
**Mistake/Issue:** WhatsApp `_incoming_phone_number_id` returned `None` for BOTH "metadata absent" and "phone_number_id not a str", and the caller only enforced the tenant check `if value is not None`. So a payload omitting `metadata`, or sending a non-str id, skipped the check and was processed as ours (signed-envelope PoC returned 200 as a real event). Same envelope-parsing also read only `messages[0]`/`entry[0]`/`changes[0]` — Meta batches multiple messages per delivery, so everything after the first was silently dropped while the whole batch was 200-acked (Meta never retries an acked delivery = permanent loss of e.g. a "cancel my order").
**Correct:** fold the tenant check INTO extraction and fail CLOSED — `extract_events(payload, expected_phone_number_id)` processes a change only if `metadata.phone_number_id` is present, a `str`, and `==` ours; iterate ALL entries/changes/messages, dedupe each via `record_if_new`, return aggregate `{processed, duplicate, results:[...]}`. Enforcement must not branch on "malformed vs mismatched" — both reject.
**Pattern:** a guard whose sentinel (`None`) collapses "absent" and "wrong-type" into the same value as "checked-and-ok-to-skip" fails open. Decide present-str-and-equal in one predicate; and any per-index (`[0]`) webhook parse silently drops a provider's batch — iterate all and ack an aggregate.

## [2026-07-30] A router-level Depends cannot cap request-body size — FastAPI parses the body first
**Mistake/Issue:** The Phase-3.5 plan enforced the admin 1 MiB body cap with a router dependency `enforce_body_cap(request)` on `admin_router`. It does not work: FastAPI reads and JSON-parses the request body (for the pydantic body param) BEFORE it runs path/router `Depends`, so an oversized *invalid-JSON* body returns **422 `json_invalid`** before the cap check ever runs. The plan's `test_body_cap_413` asserts 413 → failed.
**Correct:** enforce the cap at the ASGI layer, before routing/body-parse — a tiny pure-ASGI middleware (`AdminBodyCapMiddleware`) that 413s `/admin` requests whose `Content-Length` exceeds the cap. This is exactly how the existing webhook edges do it (`app/channels/{whatsapp,shopify_webhook}.py` read the raw body and check `content-length` + `len(raw)` themselves, taking `request: Request` instead of a parsed body param). Register with `app.add_middleware(...)` at import time (before first request, else Starlette raises "Cannot add middleware after start").
**Pattern:** any cross-cutting check that must beat body parsing (size caps, raw-HMAC on the exact bytes) belongs in middleware or a raw-body handler — NOT a FastAPI dependency on a body-param route. Dependencies run after the body is already parsed.

## [2026-07-30] New machine (e:\ drive, user devnp) — `python` on PATH is a real 3.13.5 with all deps; the old codex-runtimes path is dead
**Mistake/Issue:** The 2026-07-28 entry pins the interpreter to `C:\Users\cbbha\.cache\codex-runtimes\...python.exe` — that path is from the OLD machine and does not exist here. Blindly reusing it blocks all tooling.
**Correct:** On this machine (`e:\bhavesh_new\watsapp`, user `devnp`) `python` on PATH IS a real interpreter: `C:\Python313\python.exe` (Python 3.13.5), and it already has every dep + tool installed (fastapi, pydantic, httpx, cryptography, asyncpg, pytest 9.1.1, pytest-asyncio, ruff 0.15.18, mypy 2.1.0, and the Phase-3.5 deps slowapi + litellm). `py -3.12` is NOT installed (only 3.13/3.11/3.10) — 3.13 satisfies the repo's "3.12+" (pyproject `target-version=py312` / `python_version=3.12` are *targets*, not the runtime). Run everything as `python -m pytest|ruff|mypy` from `backend/`. `import app` resolves to `backend/app` correctly (no cafe editable-install shadowing on this machine — `beyond-loaf-agent` is not installed here).
**Pattern:** interpreter/paths in error_learnings are machine-specific — on a new machine, re-discover with `python --version` / `py -0` before trusting a hardcoded path; a real PATH `python` (not a WindowsApps stub) with the deps present is the simplest answer.

## [2026-07-30] Cafe-copied auth verifier re-introduced the non-ASCII compare_digest 500 — re-apply THIS repo's hardening when porting reference code
**Mistake/Issue:** `admin/auth.py:verify_token` did `hmac.compare_digest(sig, _sign(...))` on two raw `str`s, where `sig` is split from the attacker-controlled `admin_session` cookie. A non-ASCII cookie byte (Starlette decodes headers latin-1) makes `compare_digest` raise `TypeError` → pre-auth 500 on every `require_admin` route. This is the exact anti-pattern already logged for the Shopify/cron header verifiers (2026-07-28 "hmac.compare_digest raises TypeError on non-ASCII str") — but the Phase-3.5 plan copied the auth primitive verbatim from the cafe project, which never had this fix, so the hardening was silently dropped on the way in.
**Correct:** encode the header-derived candidate to ASCII bytes inside `try/except UnicodeEncodeError: return False` (fail closed), then `compare_digest(expected.encode("ascii"), candidate)` — identical to `channels/shopify_signature.py` and `jobs/router.py`. `check_password` was already safe (it `.encode()`s both sides to bytes).
**Pattern:** when copying auth/signature/webhook code from the reference project (`D:\ai_whatsapp_agent`), re-apply THIS repo's accumulated hardening learnings before accepting it — a "cafe-verbatim" port is a snapshot that predates our security fixes; grep error_learnings for the primitive (compare_digest, body cap, type-coercion) and diff against our channel edges.

## [2026-07-30] FastAPI's DEFAULT 422 handler echoes `input` — over-length creds leak; and `RequestValidationError.errors()` takes NO kwargs
**Mistake/Issue:** Two distinct traps found in the Phase-3.5 security review. (1) FastAPI ships NO custom `RequestValidationError` handler by default — its built-in serializes each pydantic error dict WITH `input`, so POSTing an over-length `api_key`/`client_secret`/`access_token`/`password` (any `Field(max_length=…)`-capped secret) returns a 422 whose body echoes the submitted secret verbatim (→ browser devtools/HAR, proxies, error monitors = plaintext-secret leak, CLAUDE.md Rule 1). (2) The obvious fix — `exc.errors(include_url=False, include_input=False, include_context=False)` — RAISES `TypeError: ValidationException.errors() got an unexpected keyword argument 'include_url'`, because FastAPI's `RequestValidationError.errors()` is NOT pydantic's `ValidationError.errors()`; it returns the pre-computed list and accepts no kwargs. (Those kwargs DO work on a raw pydantic `ValidationError`, e.g. the `put_knowledge` path that catches `validate_and_serialize`'s error — different class, different method.)
**Correct:** register a global `@app.exception_handler(RequestValidationError)` that filters the dicts by hand — `[{k: v for k, v in err.items() if k not in ("input", "url", "ctx")} for err in exc.errors()]` → `JSONResponse(422, {"detail": …})`. Drop `ctx` too (a custom validator's raw `ValueError` object there is non-JSON-serializable → 500 on render) and `url` (noise doc link). For code paths that catch a raw pydantic `ValidationError` directly, the `errors(include_url=False, include_input=False, include_context=False)` kwargs are correct and available.
**Pattern:** on any FastAPI app, add a `RequestValidationError` handler that strips `input` before deploying a route with a secret-bearing body field — the default handler is a credential-echo vector. And never assume `RequestValidationError.errors()` == pydantic `ValidationError.errors()`: the FastAPI wrapper takes no kwargs; strip keys manually.

## [2026-08-04] Vercel Python runtime: asyncpg dual-stack getaddrinfo → OSError [Errno 16] EBUSY
**Mistake/Issue:** In production on Vercel the app could not open a DB connection to the Supabase transaction pooler (`aws-1-ap-south-1.pooler.supabase.com:6543`) even with a correct host and correctly percent-encoded password. The traceback died at `pg_factory.py acquire -> asyncpg pool -> connect_utils._create_ssl_connection -> loop.create_connection -> _ensure_resolved -> loop.getaddrinfo -> socket.getaddrinfo -> OSError: [Errno 16] Device or resource busy`. Root cause: asyncpg resolves the host through asyncio's threaded executor with `AF_UNSPEC` (dual-stack IPv4+IPv6); that threaded getaddrinfo path is broken on the Vercel Python runtime and fails with EBUSY — the DSN/password are fine, it never gets past DNS resolution.
**Correct:** in `LazyPool._get_pool`, ONLY when the DSN hostname contains `supabase.com`, resolve IPv4 synchronously in-process (`socket.getaddrinfo(host, None, family=socket.AF_INET, type=socket.SOCK_STREAM)[0][4][0]`) and call `asyncpg.create_pool(dsn, host=<ipv4>, ssl="require", min_size=0, max_size=5, statement_cache_size=0)`. The explicit `host=` kwarg overrides the DSN host (asyncpg precedence: kwarg wins), avoiding asyncio's broken dual-stack resolver; `ssl="require"` encrypts without hostname verification (mandatory because connecting by IP defeats hostname verification). Wrap the resolution in `try/except OSError` → fall back to the plain `create_pool(dsn, ...)` so it never hard-fails worse than today. Non-supabase DSNs keep the EXACT current behavior (no host override, no `ssl` kwarg) — local/test DBs may be plain non-SSL Postgres. Only `app/store/pg_factory.py` changed.
**Pattern:** on Vercel's Python serverless runtime, asyncio's threaded AF_UNSPEC getaddrinfo can EBUSY — force `AF_INET`, resolve synchronously, connect by the resolved IP, and add `ssl="require"` to restore encryption lost by connecting to an IP. Scope such a runtime-specific workaround to the exact host family (`supabase.com`) so other DBs are untouched. This class of bug only reproduces on the runtime — tests can validate the connect-kwargs wiring (mock `socket.getaddrinfo` + `asyncpg.create_pool`) but not the fix itself.

## [2026-08-04] Exact-substring redaction can't scrub a secret a library may reformat — discard the raw error, don't redact it
**Mistake/Issue:** `LiteLLMProvider._redact` scrubbed the Vertex service-account JSON from an error via `msg.replace(vertex.credentials_json, "***")`. That only removes the EXACT stored string. litellm/vendor code can re-serialize the SA JSON (different whitespace/key order) or echo a lone `private_key`/`client_email` field — none match the stored bytes, so the credential leaks past the redact once `complete()` is wired into the Phase-4 engine. A test embedding a reformatted copy proved it (the exact copy was scrubbed, the reformatted one and the lone `private_key` were not).
**Correct:** for the secret-bearing path (`vertex_ai/*`), do NOT surface the raw error text at all — raise `ProviderError("Vertex AI request failed", _classify(exc))`, discarding the string entirely and keeping only the classified kind (mirrors the env-verify path). Keep `_redact(str(exc), api_key)` ONLY for the api_key path, where the single opaque key string IS an exact match. Env failure wording likewise gets its own `_ENV_KIND_MESSAGES` (service-account/project/location, never "the API key").
**Pattern:** a secret that a downstream library can reformat/re-serialize/split into fields cannot be reliably redacted by substring replacement — on any error path that could carry it, drop the raw text and surface a fixed safe message + classified kind. Redaction is only safe for an opaque token you compare verbatim.

## [2026-08-06] Third machine (d:\bhvaesh_automation, user cbbha) — codex-runtimes Python is back, but missing slowapi + litellm
**Mistake/Issue:** On THIS machine `python`/`py` on PATH are the WindowsApps stub (no real interpreter, no launcher). The only real interpreter is the codex-runtimes one again — `C:\Users\cbbha\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe` (Python 3.12.13) — but it is NOT fully provisioned: `fastapi/pydantic/httpx/cryptography/asyncpg/pytest/ruff/mypy` are present, yet `slowapi` and `litellm` (both declared `[project].dependencies` in `backend/pyproject.toml`) are ABSENT. Every admin/main test errors with `ModuleNotFoundError: No module named 'slowapi'` at import (`app/ratelimit.py` imports slowapi at module top; `app.main` pulls it in) until they are installed. The 2026-07-30 entry pins `C:\Python313\python.exe` on machine `e:\...` — that path does not exist here.
**Correct:** run everything as `"$PY" -m pytest|ruff|mypy` from `backend/` with `PY=` the codex-runtimes path above; first install the two missing declared deps into it: `"$PY" -m pip install "slowapi>=0.1.9" "litellm>=1.89,<2"` (pulls openai/tiktoken/etc. transitively). This is installing the project's OWN declared runtime deps into the only available interpreter, not modifying the env for a workaround — different from the 2026-07-28 "don't uninstall the cafe editable install" note. After that: 327 passed / 6 skipped, ruff + mypy strict (46 files) clean. `import app` resolves to `backend/app` correctly (no cafe editable-install shadowing observed on this machine).
**Pattern:** interpreter paths in error_learnings are machine-specific AND provisioning-specific — on a new machine re-discover the interpreter, then verify EACH declared dep imports (not just the common ones); a codex-runtimes interpreter may lack the newer deps (slowapi/litellm) added in a later phase. Install the declared deps before concluding the code is broken.

## [2026-08-06] `str.isdigit()` is not a safe int-parsing gate — Unicode digits pass isdigit() but disagree with int()
**Mistake/Issue:** `controls.load_controls` gated the `_INT_KEYS` parse with `if raw.isdigit(): data[key] = int(raw)`. `str.isdigit()` is Unicode-aware and does NOT agree with `int()` in either direction: `"²".isdigit()` is `True` but `int("²")` raises `ValueError` (→ uncaught 500 that bricks GET /admin/controls), while other Unicode-decimal forms (`"٣٠"`) pass isdigit() and int() alike but were never a real operator input. A corrupted/attacker-written stored value could thus either crash the endpoint or silently enable an unintended retention period. The same class of bug lived unguarded on the SIGNED Shopify webhook hot path: `float(staleness_raw)` in `channels/shopify_webhook.py` — a non-numeric stored `push_staleness_hours` → `ValueError` → 500 on a signature-valid delivery, burning Shopify's 19-failure retry budget before it deletes the subscription (the higher-blast-radius version).
**Correct:** never use `str.isdigit()`/`str.isdecimal()` as a pre-check for `int()`/`float()`. ⚠ **Round-1 note (superseded — see 2026-08-06 "ASCII-digit gate" entry below):** the first fix used `try: value = int(raw) except ValueError: pass` / `try: return float(raw) except ValueError: return DEFAULT`. That closes the *crash*, but `int()`/`float()` are themselves Unicode-permissive (`int("٣٠") == 30`, `float("٣٠") == 30.0`), and `float()` also accepts `nan`/`inf`/`1e400` without raising — so the Unicode-bypass and a fail-open non-finite value were still open. The COMPLETE gate is `raw.isascii() and raw.isdigit()` **then** parse (+ `math.isfinite` + range clamp on the float path). Same "treat every stored/typed value as attacker-typed, degrade to a safe default, never crash" posture as the 2026-07-28 "Webhook payload fields must be type-coerced" entry.
**Pattern:** `isdigit()`-then-`int()` is a latent 500 (and a silent-misconfig) vector — the two functions do not share a definition of "a number." Neither does `int()`/`float()` alone close it (Unicode-permissive; `float` also fail-opens on nan/inf). Require `str.isascii() and str.isdigit()` before parsing, then bound the result. Reserve `isdigit()` for genuinely display-only checks. (Related: `re`/pydantic `\d` is Unicode-aware too — pin phone/E.164 patterns to `[0-9]`, not `\d`; and Python `re`'s `$` matches before a trailing `\n`, so anchor with `\Z`.)

## [2026-08-06] Round-2 fix: `int()`/`float()` alone don't close the Unicode-digit bypass; `float` fail-opens on nan/inf; and `$` matches before a trailing newline
**Mistake/Issue:** The round-1 fixes above were re-verified and found INCOMPLETE (a two-round fix, not a one-shot close). (1) `int("٣٠") == 30` and `float("٣٠") == 30.0` — the try/except-only guard still silently accepted an Arabic-Indic stored `retention_days`/`push_staleness_hours`, enabling an unintended retention purge / staleness window. (2) On the signed Shopify webhook staleness path, `float("nan"/"inf"/"Infinity"/"1e400")` all succeed WITHOUT raising, so a `ValueError` guard can't catch them; a nan/inf makes `age > staleness*3600` always False, **fully disabling the staleness guard** → every historical order looks fresh and push-eligible again (mass unwanted WhatsApp template re-sends). (3) Separately, `load_controls` validated the whole `AdminControls` at once and fell back to `AdminControls()` on ANY field error — one corrupt field silently reset every other, e.g. re-widening a deliberately narrowed `reveal_fields` back to the PII-exposing default. (4) A NEW MEDIUM introduced by round 1: `require_admin` logged the raw `request.url.path` in an audit line BEFORE auth — uvicorn percent-decodes `%0A` to a literal newline in `scope["path"]`, so an unauthenticated attacker could split the log line and forge a fake `admin_audit ... outcome=success` record (or flood a 4000+ char line).
**Correct:** (1)/(2) gate with `raw.isascii() and raw.isdigit()` BEFORE `int()`/`float()`; on the float path also require `math.isfinite(value)` and clamp to the field's own `Field(ge=1, le=168)` range, else fall back to the module default. (3) validate per top-level field (`_validate_per_field`: drop the keys pydantic reports invalid, re-validate) so one bad field defaults only itself. (4) audit the matched route TEMPLATE (`request.scope["route"].path`, attacker-uncontrollable) — fall back to a control-char-stripped, length-bounded raw path only when no route matched, sanitizing BEFORE the value reaches the logger (never rely on the logging call to sanitize). (5) anchor the Python `re` E.164 pattern with `\Z` not `$`.
**Pattern:** for any stored/typed numeric, `isascii()+isdigit()`-then-parse is the real gate; on a float path add `math.isfinite` + a range clamp (a non-finite value that flows into a `>`/`<` comparison silently disables the guard, worse than a crash). For fail-safe defaulting, default the offending FIELD, never the whole document. For audit/log lines, never log an attacker-controlled raw path — log a fixed route template, and sanitize any raw fallback (strip C0 controls + DEL, bound length) before it reaches the logger.

## [2026-07-28] Meta error bodies echo the bearer token — never store `resp.text` verbatim, and guard 2xx JSON shape too
**Mistake/Issue:** `SendResult.error = resp.text[:500]` for >=400 responses copied Meta's OAuth error bodies verbatim into a field bound for a DB column; those bodies routinely echo the offending token (`{"error":{"message":"Invalid OAuth access token EAA..."}}`) = plaintext-secret-at-rest (CLAUDE.md rule 1). Separately, the 2xx path did `(data.get("messages") or [{}])[0].get("id")` — a 2xx body that is non-JSON, a list, or `{"messages":"x"}`/`{"messages":[1]}` raised `AttributeError`, so a future outbox-drain cron would 500 on a malformed-but-2xx Meta response.
**Correct:** `_safe_error` keeps only structured `code/type/error_subcode`, redacts `EAA[A-Za-z0-9]+` and the configured access token from the message, and falls back to `"non-JSON error response"` for unexpected shapes. `_extract_wamid` guards the JSON decode + every nested `.get` with `isinstance` and degrades to `wamid=None`. Also `WhatsAppConfig` secret fields → `field(repr=False)` so a traceback/Sentry locals capture never prints them (repr=False preserves `__eq__`).
**Pattern:** the inbound "treat every field as attacker-typed" lesson applies OUTBOUND too — a provider's 2xx/4xx body is untrusted shape AND a secret-leak vector; parse to structured fields, redact token-shaped substrings, never persist raw response text.

## [2026-08-06] Vercel `vercel.json`: `functions` and `builds` are mutually exclusive — cannot add per-function `maxDuration` alongside legacy `builds`/`routes`
**Mistake/Issue:** To close a Task-14 security-review CRITICAL (no `maxDuration` set, risking an uncaught 504 that silently drops a WhatsApp reply — `MessageStore.record_if_new` marks the message seen before the turn runs, so Meta's retry of a timed-out delivery is dropped as a duplicate with nothing logged), `backend/vercel.json` was edited to add `"functions": {"api/index.py": {"maxDuration": 60}}` alongside the existing `"builds"`/`"routes"` config (needed for this FastAPI app's catch-all ASGI routing — every path forwards to the single `api/index.py` entrypoint). This combination is REJECTED by Vercel at deploy time: "The functions property cannot be used in conjunction with the builds property" — a hard build failure, not a warning. Since `builds`/`routes` cannot be dropped (it IS the routing), the `functions` block could not coexist with it in this repo.
**Correct:** revert `vercel.json` to `builds`/`routes`/`regions` only (verify byte-for-byte via `git diff` against the pre-change commit, not just eyeballing — CRLF/LF noise can make a raw `diff` look non-empty even when `git diff` shows zero change). Set `maxDuration` via the Vercel dashboard instead (Project Settings → Functions) once the project is connected/deployed — a deploy-time operational step, not a `vercel.json` change, and not trackable as a code TODO (tracked in `_pipeline_status.md`'s deferred list instead). Keep an INTERNAL `asyncio.timeout(...)` guard in the code regardless (`app/core/conversation.py`'s `TURN_TIMEOUT_SECONDS`) as defense-in-depth — it should self-abort and log a clean `WARNING` well under whatever platform limit ends up configured, rather than depending entirely on the platform default/dashboard setting.
**Pattern:** before adding a `functions` block to a `vercel.json` that already uses `builds` (common in legacy/ASGI-catchall Python deployments), check whether the two are mutually exclusive for the CURRENT Vercel schema — they were as of this fix. When genuinely uncertain about a deploy-config syntax and no way to verify it in-sandbox (no network access to the platform's schema validator), flag it explicitly as unverified rather than presenting it as done; a wrong-but-silently-accepted config is far worse than a wrong-and-loudly-rejected one, but "flag and let the human verify" beats guessing either way when the tool to check is unavailable.

## [2026-08-06] A rejecting input filter that returns the same value as "no results" fabricates a negative answer
**Mistake/Issue:** `ShopifyClient._is_safe_product_query` rejected any product query containing a quote, colon, `*`, paren, or a standalone `and`/`or`/`not` — so ordinary customer phrasing ("black and white saree", "women's kurti", "size: M", "cotton or silk?") never reached Shopify. Worse, `search_products` returned `[]` for BOTH "query rejected as unsafe" and "searched, catalog has no match", so the agent confidently told the customer the store has nothing — a fabricated negative produced by the very guard meant to prevent hallucination. Two latency variants of the same "pay for it whether or not it is used" shape lived alongside it: `resolve_by_phone` (one live Shopify `get_order` per mapped order, awaited SEQUENTIALLY) ran before intent classification on every inbound "hi", though only `order_tracking` reads the result; and `TURN_TIMEOUT_SECONDS` was applied per turn while Meta batches N messages per delivery, giving an unbounded N x 55s request.
**Correct:** sanitize, don't reject — strip the metacharacters (quotes DELETED so "women's" -> "womens"; `*:()\` -> a SPACE so two words never fuse into a nonsense token) plus standalone boolean tokens, and search the remainder; return a DISTINGUISHABLE sentinel (`None`) when nothing searchable survives, so callers can tell "no searchable term" from "no match". For the latency shape: classify first and resolve orders only for the intent that reads them; `asyncio.gather(..., return_exceptions=True)` the per-order re-fetches (re-raise anything that is not the expected domain error); hold ONE deadline for the whole webhook delivery and pass the REMAINING budget into each turn — `min(budget, TURN_TIMEOUT_SECONDS)` read at call time keeps the existing monkeypatch-based timeout test working.
**Pattern:** any validation guard whose rejection value equals a legitimate empty result is a silent-wrong-answer generator, not a safety feature — sanitize where possible, and always give "rejected/unusable input" its own sentinel. In a per-turn hot path, never pay for data an intent-dependent branch may not read: classify first, fan out concurrently, and budget the REQUEST, not the iteration.

## [2026-08-06] Two silent no-ops: an identity written under one key and deleted under another, and a prompt promise the response schema cannot express
**Mistake/Issue:** Phase 4's project code+security review found two failures that no test could catch because nothing *errored*. (1) `core/conversation.py` stored the conversation via `get_or_create(event.wa_id)` — the raw Meta `wa_id` `"919876543210"` — while the DPDP erasure delete binds `phone_e164` `"+919876543210"` to `DELETE FROM conversations WHERE user_id = $1`. Same customer, two strings: the delete matched zero rows, `POST /admin/erasure` returned `ok: true` with real counts from the *other* tables, and the full chat history survived a "successful" erasure. (2) Four of five specialist agents' system prompts instructed the model to "offer to connect the customer with the team", but only `customer_support` asked for a `{"reply", "handoff"}` JSON contract and parsed the flag — the other four requested only `{"reply"}`, so `AgentReply.handoff` was structurally always `False`. The model said "I'll connect you with our team", the customer believed it, nothing paused, and the AI answered normally on the next turn. Related third: even the one working handoff path notified nobody — `AdminControls.owner_alert_number` was a config field no code read. And `AdminControls.reveal_fields` was an admin disclosure control that `order_tracking._order_context` never consulted.
**Correct:** (1) normalize FIRST and key the conversation on the same E.164 value the erasure path deletes by (`phone or event.wa_id`), and pin BOTH sides with tests — the write key (engine, via a signed webhook turn) and the bound parameter (`DELETE ... WHERE user_id = $1`) — so neither can drift again. (2) any prompt instruction that promises an ACTION must have a matching field in the response contract AND a parse step; shared `HANDOFF_JSON_CONTRACT` + `base.model_asked_for_handoff` rather than four copies. Do NOT hand off on `ProviderError` in the specialists — a transient LLM blip would pause the AI 24h. (3) fire the owner alert inside the SAME `send_mode` gating as the customer reply (off/shadow/allowlist all suppress it), degrade silently when unconfigured. (4) render only the admin-approved `reveal_fields` into the prompt — omit withheld fields entirely rather than telling the model not to mention them; what the model never sees, it cannot leak.
**Pattern:** two recurring classes. **Key-identity drift:** when one subsystem WRITES a record under an identifier and another DELETES/looks it up by a *derived form* of the same identifier (raw wa_id vs E.164, id vs slug), the mismatch is invisible — the delete succeeds with 0 rows. Pin both sides to one canonical form and test the write key and the bound parameter together; a "0 rows affected" success is not a success. **Prompt-promise without a schema slot:** an LLM system prompt telling the model to *do* something (escalate, hand off, flag) is dead text unless the response contract has a field for it and the code parses that field. Audit every "offer to…/tell them you will…" instruction against the JSON schema the same prompt requests. Both bugs pass every test that only asserts a 200/a reply string.

## [2026-08-09] A fully-tested function with ZERO app callers is a silent no-op — grep for callers, not just tests
**Mistake/Issue:** Phase-4 code review found `core/order_resolver.resolve_by_order_name` (order-name lookup + injection-guard + live ownership check) was fully implemented AND had its own passing unit tests, but had zero callers anywhere in `app/`. So a customer whose WhatsApp number is mapped to no order could type their order number ("tavas3733") and never get it looked up — the order-tracking pipeline only ever called `resolve_by_phone`, and the empty result just handed off to a human. Green tests + green suite hid it: nothing errored, the function simply was never wired into `_agent_reply`'s resolve step. (Same "passes every test that only asserts a 200/a reply" class as the 2026-08-06 silent-no-op entries above.)
**Correct:** wire the fallback into `core/conversation._agent_reply` — only for `intent == "order_tracking"` and only when `resolve_by_phone` returns empty, extract an order-name token from the customer's own message (`re.compile(r"tavas[0-9]+", re.IGNORECASE)` — Thetavas names have NO `#`, use `[0-9]` not `\d` to avoid Unicode-digit surprises, single `.search` so it stays linear on huge/hostile input) and pass it to `resolve_by_order_name`, folding any returned `AuthorizedOrder` into `context.orders`. Preserve every guarantee: ownership + live re-fetch + non-enumerable uniform-None all already live inside `resolve_by_order_name` (do NOT weaken it); the extractor only surfaces a candidate. Prove the wiring with an integration test that captures `context.orders` AND asserts the extracted token reached `resolve_by_order_name` — a unit test on the resolver alone would still have passed with zero callers.
**Pattern:** "has tests" ≠ "is reachable." When a capability is built + tested but the feature it enables is missing in practice, grep the codebase for CALLERS of the function, not just its tests. A public/tested resolver with no call site is dead code that fabricates a degraded UX (here: an unnecessary human handoff) while every gate stays green.

## [2026-08-10] A Meta send-error CODE lives in the response BODY, not the HTTP status — the outbox drain can't classify undeliverable-vs-retryable off `SendResult.status_code`
**Mistake/Issue:** The Phase-5 plan's outbox drain wrote `elif r.status_code in _UNDELIVERABLE_CODES: # {131026, 131047, 131049}` to route "recipient not reachable / re-engagement" sends to a terminal `undeliverable` state instead of retrying. But those 131xxx numbers are META error CODES that live in the JSON error body (`{"error": {"code": 131026, ...}}`), NOT the HTTP status — Meta returns HTTP **400** for ALL of them, so `r.status_code` (the HTTP status) is never 131026 and the branch would never fire; every undeliverable send would instead be `bump`ed and retried to the attempt cap, then marked `failed`, wasting the whole retry budget on a permanent failure. `SendResult(ok, status_code, wamid, error)` exposes only the HTTP `status_code` and the redacted `error` string — the structured Meta code is not a field.
**Correct:** extract the Meta code from `SendResult.error` — `whatsapp_sender._safe_error` renders the body's `code` as `"code=<n>"`, so a `re.compile(r"code=(\d+)")` search recovers it reliably (our own formatter produces it). Route a code in `_UNDELIVERABLE_CODES` → `mark_outbound_undeliverable`; anything else (incl. 5xx/429 with no code) → `bump_outbound_attempt` (stays queued until the cap). Documented as a deliberate deviation from the plan's literal `r.status_code` check in the pipeline status.
**Pattern:** a provider's HTTP status and its in-body error `code` are DIFFERENT axes — a 400 can carry a permanent "undeliverable recipient" code OR a transient/bad-param code. When a `SendResult`-style wrapper only surfaces the HTTP status + a formatted error string (not the structured code), derive the terminal-vs-retryable decision from the code carried in that string; never assume the numeric provider error code equals the HTTP status.

## [2026-08-10] Phase-5 review: an unanchored `code=` regex matched inside `error_subcode=`, and an async mutation needs its idempotency marker written BEFORE it fires
**Mistake/Issue:** Two non-obvious Phase-5 review findings on the confirm/cancel surface. (1) The follow-up to the entry above extracted the Meta send-error code with `re.compile(r"code=(\d+)").search(error)` — but `_safe_error` renders BOTH `code=<n>` (top-level send error) AND `error_subcode=<n>` in the SAME `"; "`-joined string, and an UNANCHORED `code=` happily matches the tail of `error_subcode=131026`. So an error carrying only a benign `error_subcode` (no top-level `code`) was misclassified as UNDELIVERABLE and terminated instead of retried — the guard meant to spot permanent failures was fabricating them. (2) The two-phase cancel wrote the provisional `cancel_requested` tag AFTER `cancel_order(auth)` returned; two near-simultaneous cancel-confirm taps (distinct Meta message-ids, so message-id idempotency doesn't dedupe them) both re-fetched the order, both saw no provisional tag, and both fired the irreversible async `orderCancel`. (3) Related audit gap: a FAILED confirm (`add_tags` error) and a transport-level cancel error wrote NO `order_actions` row — only the GraphQL-userErrors cancel path was audited — so a failed mutation left no trail.
**Correct:** (1) anchor the field regex to the top-level field only: `re.compile(r"(?:^|; )code=(\d+)")` — require `^` or the `"; "` separator before `code=`, so a substring inside another field NAME (`error_subcode`) can't match; a no-top-level-code error now falls through to retry. (2) advance state FIRST: write the provisional tag + `set_mapping_status("cancel_requested")` BEFORE calling `cancel_order`, so the racing second tap's live re-fetch sees the marker and short-circuits (`cancel_order` called at most once). Accept the tradeoff that a subsequent cancel FAILURE leaves the order in `cancel_requested` (reply cancel_failed; reconcile leaves it until Shopify reports `cancelledAt`) — a stuck-pending is safer than a double orderCancel. (3) make the audit symmetric: the outer `except ShopifyError` records an "error" `order_actions` row for the attempted mutation (mapping the parsed action → its audit label) across ALL ShopifyError subtypes, while the handler's own GraphQL branch records-and-returns so a GraphQL failure never double-writes. (4) the kill switch belongs in the deterministic dispatcher too, not only the drain/inbound path — `send_decision(...)` first in `dispatch_button`, `suppress` → no resolve/mutation/reply — so a shadow/allowlisted-out tap can't perform a REAL mutation + send.
**Pattern:** a field-name regex (`code=`, `id=`, `error=`) MUST be anchored (`^`/separator/`\b`) or it matches inside a longer sibling field (`error_subcode=`, `user_id=`) — the same "anchor the pattern" lesson as `$` vs `\Z` on E.164. For an ASYNC/irreversible mutation guarded by a re-fetched marker, write the marker BEFORE firing the mutation, never after — otherwise the guard has a race window exactly as wide as the mutation's in-flight time. And every kill-switch/authorization gate that guards a side effect must live at the side-effect boundary itself (one source of truth), with any earlier gate treated as defense-in-depth, not the enforcement point.

## [2026-08-12] Green mocked-GraphQL tests hid a schema-invalid mutation — `webhookSubscription{apiVersion}` is not an INPUT field
**Mistake/Issue:** `shopify/subscriptions.py`'s `_CREATE_MUTATION`/`_UPDATE_MUTATION` declared `$apiVersion: String!` and interpolated it inside the `webhookSubscription: {callbackUrl, apiVersion, format}` INPUT object. Shopify's `WebhookSubscriptionInput` has NO `apiVersion` field — a subscription's delivered-payload version is implicitly the API version of the request that created/updated it (the endpoint URL's version, i.e. `ShopifyClient.api_version`), not a settable input. Against the LIVE Admin API (2026-07) both mutations return a SCHEMA-VALIDATION error ("InputObject 'WebhookSubscriptionInput' doesn't accept argument 'apiVersion'; Variable $apiVersion is declared … but not used") — a hard failure BEFORE any userErrors, so `_ensure_one_topic` swallowed it into a flat `"error"` string and the self-heal had NEVER once created a real subscription (live `webhookSubscriptions(first:20)` returned zero). The whole test suite was green because every subscription test MOCKS the GraphQL response (`httpx.MockTransport`) rather than validating the query against Shopify's real schema — the mock happily accepts any query string. Note `apiVersion { handle }` IS valid to READ back on a `WebhookSubscription` node (used correctly in `_LIST_QUERY` for F20 drift detection) — only passing it as INPUT is wrong.
**Correct:** drop `apiVersion` from both mutations' input object AND the `$apiVersion` variable declaration AND the `apiVersion` key from the variables dicts passed to `client._graphql(...)`. The created subscription's version is whatever `ShopifyClient.api_version` (per `Settings.shopify_api_version`) the request already uses — no explicit input needed or possible. Keep the F20 `current_version == version` read-back comparison untouched (`version = client.api_version` still drives it). Tightened the tests to assert the EXACT variables dict sent (`{topic, callbackUrl}` / `{id, callbackUrl}`, no `apiVersion`) and that `"apiVersion"` is absent from the mutation query string — not just the mocked response.
**Pattern:** "has tests" ≠ "matches the real schema" (cf. the 2026-08-09 "has tests ≠ is reachable" entry). A test that MOCKS a provider's response validates your request-handling/parsing but NEVER validates that the request itself is accepted by the provider — a schema-invalid GraphQL mutation passes every mock forever. For provider mutations, assert the exact query shape + variables the code sends (so a review can diff it against the provider's schema docs), and treat any capability that has only mock-backed tests and zero real-API confirmation as UNVERIFIED against the live schema until proven otherwise.

## [2026-08-12] Plan test snippets missed two harness prerequisites — get_container() needs APP_MASTER_KEY set first, and exercising the order-name resolve path needs an order-number token in the message
**Mistake/Issue:** The mirror-first-order-reads plan's Task 4 `_agent_reply` test (verbatim from the plan) failed twice for reasons unrelated to the code under test. (1) It called `reset_container(); c = get_container()` WITHOUT first setting `APP_MASTER_KEY` — there is no autouse fixture that seeds it (`tests/conftest.py` only offers an on-demand `master_key`/`settings` fixture), so `get_container()` builds `Settings()` from env and raises `pydantic ValidationError: app_master_key Field required` before the test body runs. The established repo idiom (`tests/test_whatsapp_webhook.py:52-54`) is `monkeypatch.setenv("APP_MASTER_KEY", master_key); reset_container(); get_container()` (with the `master_key` fixture as a param) — the plan snippet dropped the `setenv` line and the fixture param. (2) With that fixed, the test asserted `ingest.mirror_calls == ["get_mirrored_order", "find_mirrored_order_by_name"]` but the message text was `"where is my order"` — which has NO order-number token, so `_recover_order_by_name` short-circuits at `_extract_order_number_candidate(text) is None → return [], None` and never reaches `resolve_by_order_name`/`find_order_by_name`, so only `get_mirrored_order` (via `resolve_by_phone`) was ever called.
**Correct:** (1) add the `master_key: str` fixture param + `monkeypatch.setenv("APP_MASTER_KEY", master_key)` before `reset_container()` — the same bootstrap every container-monkeypatching test uses (no DATABASE_URL → the in-memory container, whose `ingest`/`shopify` are then monkeypatched). (2) put an order token in the message (`"where is my order tavas3733"`) so BOTH mirror reads actually run — the plan's own assertion (two methods) and its `_PoisonedShopify` design (`get_order` raises to prove the phone-path hit, `find_order_by_name` returns a benign `None` so the name-path can fall through past the mirror miss) both signal the token was intended; the assertion is the source of truth for intent, the token-less text was the defect. Both are behaviour-preserving test-harness fixups, applied inline (cf. the 2026-07-28 "Plan-verbatim code can lag the repo" entries).
**Pattern:** two reusable gotchas. `get_container()` has NO autouse env seeding — any test that constructs/monkeypatches the container singleton must `monkeypatch.setenv("APP_MASTER_KEY", <fernet key>)` (or use the `master_key` fixture) BEFORE `reset_container()`, or it dies in `Settings()` validation, not in the code under test. And `_recover_order_by_name` (and `_extract_order_number_candidate` behind it) only calls its `OrderSource` when the message contains an order-number-shaped token of the right digit count — a test that asserts the name-resolve path was exercised MUST feed it such a token, else it silently short-circuits and the OrderSource is never touched.

## [2026-08-12] A mirror-read fallback was unbounded + N+1 because the backfill fills the mirror but NOT the fast-path mapping table
**Mistake/Issue:** `PostgresIngestStore.find_mirrored_orders_by_phone` scanned every matching order (no `ORDER BY`/`LIMIT`) and then round-tripped once per order for its line items (`_fetch_mirror_line_items` in a loop), all on one held connection from the 5-connection pool the app shares with the live WhatsApp reply path. It looked low-traffic because `resolve_by_phone` tries the fast path (`find_mappings_by_phone`, already capped `LIMIT 20`) first — but the one-time 12-month backfill script only calls `upsert_order_mirror`; it NEVER writes `order_mappings` rows. So for the entire backfilled order history the fast path returns empty and every routine order-tracking turn falls through to this exact unbounded/N+1 method. A backfilled customer with many historical orders thus hit the worst path on the hot pool, invisibly (green tests, the two sibling precedents already capped: `find_mappings_by_phone` `LIMIT 20`, and the Shopify fallback it wraps `first: 10`).
**Correct:** cap the phone query `ORDER BY o.updated_at DESC LIMIT 10` (match the Shopify fallback's cap) and batch-fetch line items for all returned gids in ONE query (`WHERE order_gid = ANY($1)`), grouping into `items_by_gid` in Python — never loop a per-row fetch. Match the cap in the in-memory impl so the two don't diverge. Extract a single `_line_item_from_row` builder shared by the per-order and batched reads (also fixed a latent NIT there: `if r["price_amount"] is not None` not truthiness, so a stored `"0"` is kept).
**Pattern:** when a backfill/import populates a mirror table but NOT the fast-path index/mapping table that a resolver checks first, the "rare" fallback read is actually the PRODUCTION path for all backfilled data — bound and batch it exactly like the live path it wraps (same `LIMIT`, one `= ANY` fetch, no N+1), and grep the backfill script to see which tables it does/doesn't write before assuming a fallback is cold. A phone/owner-scoped read on a pool shared with the reply hot path must always be `ORDER BY ... LIMIT`-capped and single-round-trip.

## [2026-08-12] An ownership/identity CHECK must normalize both sides — raw-vs-normalized string equality silently changes semantics per data source
**Mistake/Issue:** `AuthorizedOrder.__post_init__` (the ADR-004 mutation/disclosure gate) did exact string-equality — `verified_phone in (order.phone, order.shipping_phone, order.billing_phone)`. Once a SECOND `OrderSource` (the `MirrorOrderSource` database mirror) started feeding the same gate, the two sources supplied phones in different shapes: live Shopify returns them RAW (customer free-text, e.g. `"98765 43210"` or a non-E.164 address phone), while the Postgres mirror stores them `_e164`-normalized and the in-memory store stored them verbatim (a third behavior). So whether an ownership check passed depended on which store answered — a source-dependent security invariant. It "worked" in production only because Shopify happens to E.164-normalize the customer's primary phone; a shipping/billing-only or free-text number would false-REJECT the true owner (routes to handoff — safe but wrong), and the mismatch made the guard's real semantics invisible. Related: `store/memory.py`'s `upsert_order_mirror` didn't normalize on write at all, so only Postgres normalized — the two `IngestStore` impls diverged on what a stored phone looked like.
**Correct:** normalize BOTH sides inside the check — `normalize_phone(verified) == normalize_phone(order_field)` — so it's identical whether the order came from live Shopify, the Postgres mirror, or the in-memory store (two representations of the same number ARE the same number for an ownership test). Guard the None edge: an unparseable `verified_phone` (`normalize_phone -> None`) must be REJECTED, and a `None` must never match a `None` order phone (don't let "both unparseable" authorize). Kept the existing `not verified_phone` empty-string reject. Also aligned `memory.upsert_order_mirror` to normalize phones on write with the SAME `_e164` helper Postgres uses, so the two stores no longer diverge. Verified every existing `AuthorizedOrder` caller/test (both the mutation path `order_actions.py`/`resolve_by_gid` and the Q&A path `order_resolver.py`) still behaves — all used already-E.164 phones, which normalize to themselves.
**Pattern:** any equality-based authorization/identity check (phone, email, id, slug) that can be fed the "same" value in two normalization shapes MUST normalize both operands at the comparison, not rely on the callers to pre-normalize — otherwise adding a second data source silently forks the invariant's meaning. Same family as the 2026-08-06 "key-identity drift" entry (write-key vs delete-key), but here it's a read-time COMPARISON, not a stored key: normalize at the choke point (the `__post_init__`/predicate) so no upstream source can weaken it, and pin the None/unparseable case so "can't normalize" never collapses into "matches."

## [2026-08-13] ⛔ RETRACTED — WRONG: "webhook self-invoke send" (superseded by the queue + 1-minute cron entry below)
**⚠ This entry was WRONG and its recommended fix was reverted the same day. Do not follow it.** It concluded the `orders/create` webhook should send its own queued row via a FastAPI `BackgroundTask` (`send_outbound_now`). That directly contradicts the standing, correct 2026-07-28 entry "Vercel Python has no reliable 'process after response'" (and binding ADR-001): on Vercel's buffered Python runtime `BackgroundTasks` execute INSIDE the request cycle (the security reviewer measured a 2s simulated send delaying the ack by the full 2s) and post-response work can be killed on instance reclaim — so a background-task send is neither low-latency NOR durable, and it burns the 5s Shopify-ack budget. The entry also failed to check itself against that older entry + ADR-001 before being written, which is how the mistake reached code. **Standing correct guidance = the 2026-07-28 entry + the queue+cron entry immediately below.** Original (wrong) text preserved for the audit trail:
**Mistake/Issue:** Flow A queued an `outbound_messages` row on `orders/create` but nothing ever sent it — the only sender was `run_outbox_drain`, and there is NO Vercel cron on the live deployment (confirmed by checking production directly; owner chose webhook-self-invoke over cron polling). The naive fix — call `run_outbox_drain` (or its `claim_queued_outbound` path) from the webhook's `BackgroundTasks` — is a duplicate-send bug: `claim_queued_outbound` is a plain `SELECT ... WHERE state='queued'` with NO `FOR UPDATE SKIP LOCKED` / `UPDATE ... RETURNING`, so two orders arriving close together spin up concurrent Vercel instances that could BOTH select and BOTH send the SAME queued row. The non-atomic claim was a pre-existing, already-documented gap that only became actively dangerous once sends could happen concurrently (which self-invoke introduces).
**Correct:** the self-invoke path must NOT touch `claim_queued_outbound` at all — it already knows the exact row it just created. Thread the new row's id out of the write: extend `IngestResult` with `outbound_id: int | None` (Postgres `INSERT ... ON CONFLICT DO NOTHING RETURNING id` via `fetchval` → `None` on conflict = "no fresh row"; in-memory the new `_OutboundRow.id`), and have the webhook send ONLY that id (`send_outbound_now` builds a one-off `OutboundClaim` from the `OutboundDraft` it already has + the id). Extract the per-row state machine (`send_one_outbound`) so the backstop drain and the self-invoke send byte-for-byte identically, and keep `run_outbox_drain` intact as a future backstop. Register the task via a real `background_tasks: BackgroundTasks` route param so it runs AFTER the 200 is constructed (Vercel's ASGI runtime waits for it before freezing — safe, unlike bare fire-and-forget). Honour the `send_mode` kill switch exactly like the drain (`off` → leave queued; shadow/allowlist-miss → suppress, zero Meta calls), and wrap the whole task in `try/except Exception` (log + swallow) so a BackgroundTask can never crash the worker.
**Pattern:** when moving a queue-drain side effect onto a per-item trigger that can now run CONCURRENTLY, a non-atomic "claim" (`SELECT WHERE state=…` with no lock) turns from a latent single-worker gap into a live double-processing bug. The fix is not "add locking to the claim" (deferred) but "don't claim at all" — carry the freshly-written row's own id out of the write transaction and process exactly that one, so two triggers can never contend. Same key-identity discipline as the drift entries: the id that was WRITTEN is the id you PROCESS, no shared re-query in between.

## [2026-08-13] Flow A send trigger = queue + 1-minute Vercel Cron + ATOMIC claim (the correct resolution; supersedes the retracted self-invoke entry above)
**Mistake/Issue:** Flow A queued an `outbound_messages` row on `orders/create` but nothing sent it — no Vercel cron existed and the only sender (`run_outbox_drain`) was never triggered. The FIRST attempted fix (self-invoke via `BackgroundTasks` — the retracted entry above) was rejected by both code-review and security-review: it violated ADR-001 + the 2026-07-28 "no reliable process-after-response" learning, and the reviewer measured a 2s simulated send delaying the Shopify ack by the full 2s (BackgroundTasks run in-request on Vercel Python). Separately, the pre-existing `claim_queued_outbound` was a plain `SELECT ... WHERE state='queued'` with NO row lock and NO state write — so two overlapping cron executions (one still in flight when the next 1-minute tick fires) could both select and both send the SAME row (a real [MEDIUM] duplicate-send race, latent until a real scheduler existed).
**Correct:** (1) **Add a real 1-minute Vercel Cron** (`vercel.json` `"crons":[{"path":"/internal/jobs/outbox_drain","schedule":"* * * * *"}]` — `crons` is a separate top-level key that DOES coexist with `builds`/`routes`, unlike `functions` which is mutually exclusive with `builds`). Cron fires `GET` + `Authorization: Bearer <CRON_SECRET>`, so `jobs/router.py` accepts GET (Bearer) in addition to the existing POST (`X-Cron-Secret`), each method reading only its own header. The webhook goes back to pure queue-writer: persist → 200, nothing else. (2) **Make the claim atomic**: one statement, inside an explicit transaction — `WITH claimed AS (SELECT id ... WHERE state='queued' ORDER BY created_at LIMIT $1 FOR UPDATE SKIP LOCKED) UPDATE ... SET state='processing' ... WHERE id IN (SELECT id FROM claimed) RETURNING ...`. `SKIP LOCKED` + the `queued→processing` flip means two concurrent callers can never receive the same row. (3) **Retry across runs**: because each row is now `processing` after claim, `bump_outbound_attempt` must move it `processing → queued` (re-claimable by a later tick) unless `max_attempts` is hit → `failed`; it must NEVER leave a row stuck in `processing` (nothing re-claims a processing row). `send_one_outbound` (kept from the reverted commit — extracting it was correct regardless) transitions `processing → sent`/`→ undeliverable`/(via bump) `→ queued|failed`. (4) Log only `type(exc).__name__` on the send path, never the rendered traceback (it touches the WhatsApp token + a customer phone) — mirror `mirror_order_source._safe`.
**Pattern:** on Vercel Python, "send after ack" is ALWAYS queue + external scheduler (cron), never an in-request BackgroundTask — re-confirmed twice now; check ADR-001 + the 2026-07-28 entry before ever proposing a background-task send again. Any claim that a scheduler can invoke concurrently must be atomic (`FOR UPDATE SKIP LOCKED` + state flip in one statement), and a claimed-then-failed row must return to a re-claimable state, never a terminal in-flight one. And when a durable-queue side effect gains a real periodic trigger, re-audit the claim's concurrency safety — a lock-free claim that was "fine for the single-instance backstop" becomes a live race the moment two ticks can overlap.

## [2026-08-13] The atomic `queued→processing` claim needs a stale-`processing` RECLAIM, or a killed invocation strands the row forever
**Mistake/Issue:** The atomic-claim fix above (`SELECT ... WHERE state='queued' ... FOR UPDATE SKIP LOCKED` → flip to `processing`) closed the double-send race but opened a permanent-message-loss HIGH: the claim predicate was `WHERE state = 'queued'` ONLY. If the Vercel function running the drain is killed AFTER a row is flipped to `processing` but BEFORE the send completes (platform timeout / instance reclaim — realistic: `_CLAIM_LIMIT` was 25 rows/tick × `send_template`'s 20s per-call timeout, and `maxDuration` is still unset on the legacy-`builds` config so the platform default ~10-15s applies), that row is stranded in `processing` FOREVER — nothing re-claims it: `bump_outbound_attempt` only fires on a reached send, `retention.purge_older_than` explicitly excludes `outbound_messages`, no admin requeue exists. The first cron tick after deploy faces the live store's accumulated backlog (the single most likely moment to hit the limit and strand rows).
**Correct:** widen the claim predicate to also reclaim stale in-flight rows — `WHERE state = 'queued' OR (state = 'processing' AND updated_at < now() - interval '10 minutes')`. The 10-minute threshold is two orders of magnitude above the 20s send timeout, so a genuinely-in-flight row (claimed moments ago, fresh `updated_at`) can NEVER collide with the reclaim; only rows truly abandoned by a dead invocation are recovered — the `queued→processing` and `processing→reclaimed` transitions stay mutually exclusive at the boundary. Also cut `_CLAIM_LIMIT` 25→5 so a run comfortably fits any per-invocation budget (5/min = 7200/day ≫ the 100-500 orders/day target; a leftover row waits for the next 1-minute tick). Mirror the predicate in the in-memory store (`_OutboundRow` gains an `updated_at`, stamped on write + on claim; reclaim if `now - updated_at >= 10min`) so it doesn't regress behind Postgres. Proven by a gated real-Postgres test (stale `processing` → reclaimed, recent `processing` → not) + a fast in-memory unit test; the SQL-shape assertion test pins the literal new WHERE clause.
**Pattern:** an atomic `queued→processing` claim is only half a queue — the OTHER half is a reclaim path for rows a crashed worker left in the in-flight state. Whenever a claim flips a row to a non-terminal in-flight state and the only thing that advances it out is the worker that claimed it, add a staleness-based reclaim (threshold ≫ the operation's own timeout, so a live op is never stolen), or a killed worker turns "process-after-claim" into permanent loss. Verify no OTHER writer re-queues that state (purge, admin, retry) before assuming a stranded row is recoverable.

## [2026-08-14] A new dedupe_key namespace on a shared queue silently dies if the consumer parses the OLD prefix only
**Mistake/Issue:** The Q17 reminder feature queues a second kind of `outbound_messages` row — `dedupe_key = "order_reminder:{gid}"` — onto the SAME outbox the original push (`order_created:{gid}`) already flows through, on the (owner-brief-stated) assumption that "once queued the row flows through the EXISTING claim → `send_one_outbound` pipeline automatically, no changes needed there." It does NOT. `send_one_outbound` recovers the order gid (for the `order:confirm/cancel:{gid}` button payloads AND the `set_mapping_status` call) by stripping a prefix in `_gid_from_dedupe_key`, whose constant was `_DEDUPE_PREFIX = "order_created:"` ONLY — so a `order_reminder:` row returns `gid=None`, is treated as a CORRUPT dedupe_key, and is terminally marked `failed` via `bump_outbound_attempt(max_attempts=1)`. The reminder never sends and no error surfaces (the drain's "one poison row never aborts the loop" design swallows it). Every unit/store test was green because they asserted the row was queued with the right key; only the mandated E2E test (queue a reminder → run the real drain → assert it SENT with the confirm/cancel buttons) caught it.
**Correct:** widen the extractor to accept every prefix producers write — `_DEDUPE_PREFIXES = ("order_created:", "order_reminder:")`, loop and strip whichever matches. Both carry the same gid and send the same template + buttons, so the drain treats them identically; only the key prefix (the reminder's exactly-once guarantee, distinct from the original) differs. No other drain change was needed. Note the `OutboundClaim` shape carries NO separate gid field — the dedupe_key IS the only gid carrier the sender has, which is exactly why the consumer's prefix parser is load-bearing.
**Pattern:** when a durable queue's CONSUMER derives routing/identity data by parsing a structured key, adding a new key namespace on the PRODUCER side is not additive-for-free — the consumer's parser must recognize the new prefix or the new rows fail closed (here, silently, because the drain deliberately never aborts on a bad row). A brief that says "flows through the existing pipeline, no changes needed" is a hypothesis to TEST end-to-end, not a fact: always drive a new queue-row type through the REAL consumer in a test (not just assert it was enqueued), and grep the consumer for every place it `.startswith(<the one old prefix>)`.

## [2026-08-14] A "1 hour after X" reminder clock must age off X's ACTUAL timestamp, not a proxy column — and needs an upper ceiling
**Mistake/Issue:** Q17's `find_stale_template_sent` aged orders off `order_mappings.created_at` (webhook-ingest time), not off when the confirmation template was actually SENT. Two live-customer HIGHs. (1) Because the outbox cron had never run in production, a real backlog of `queued` rows sat for hours; on first deploy the 1-minute drain flushes it (finally sending the original templates + stamping `status='template_sent'`), and since those mappings' `created_at` was already ≫1h old, the VERY NEXT 5-minute reminder sweep queued a duplicate for every one — two identical templates back-to-back to a real customer, not "after 1 hour of silence." (2) No upper bound + `order_mappings` kept indefinitely (Q15) meant a 30-hour / 45-day-old unanswered mapping was swept and reminded on the first run — a reminder-blast of the store's entire history of untapped orders. (3) Same class as the 2026-08-12 mirror-read finding: `find_stale_template_sent` was the ONLY unbounded, N+1 (one `find_outbound_by_dedupe_key` per swept row) SELECT on the shared pool while every sibling was `ORDER BY ... LIMIT`-capped.
**Correct:** anchor the window on `updated_at` — `set_mapping_status` already writes `updated_at = now()` on every status change, and the drain calls it with `'template_sent'` right after a successful send, so `updated_at` on a `template_sent` row IS the send moment. Filter `status='template_sent' AND updated_at <= now()-make_interval(mins=>$1) AND updated_at > now()-make_interval(mins=>$2)` (min 60m, ceiling 24h, both parameters) `ORDER BY updated_at LIMIT 25`; add index `(status, updated_at)`; mirror in-memory with a `_mapping_updated_at` re-stamped on ingest + `set_mapping_status`; batch the payload lookups into one `find_outbound_by_dedupe_keys(list) -> dict` (`= ANY($1)`). **Verify the anchor holds against ALL `set_mapping_status`/`updated_at` writers:** the confirm/cancel/reconcile taps all move status OFF `template_sent`, and the only other `updated_at` writer (the `orders/create` ingest `ON CONFLICT DO UPDATE ... updated_at=now()`) leaves status alone — a duplicate ingest only moves `updated_at` FORWARD, which DELAYS a reminder, never fires one early or twice. So `status='template_sent' AND <updated_at window>` is exactly "last thing that happened to this row was becoming template_sent, N–M minutes ago." Surfaced in the E2E test only after seeding age BEFORE the original drain — the drain re-stamps `template_sent` (correctly, as prod does), so the age must be simulated AFTER that send, mirroring the real timeline.
**Pattern:** a "T minutes after event E" scheduler must age off the column that E actually writes, never a proxy that merely correlates with E under normal timing (here `created_at` ≈ send time ONLY when the drain runs promptly — the exact assumption that was false in prod). When the anchor column is written by several code paths, enumerate every writer and prove the (status-filter AND time-window) predicate still means "E happened in the window" — a forward-only bump of the anchor that leaves the status intact is safe (delays), but any writer that could move it while the status still qualifies must be checked. And any owner/order-scoped sweep on a pool shared with a hot path gets the same treatment as the mirror-read precedent: `ORDER BY ... LIMIT`-cap it, batch the fan-out reads (`= ANY`), and add a max-age ceiling whenever the table is retained indefinitely, so "first run after deploy" can't process the whole history.

## [2026-08-14] A fulfillment webhook carries a numeric order_id, not an order gid — the one place F20's "never reconstruct a gid" cannot apply; and the child FK needs an existence guard, not a caught FK error
**Mistake/Issue:** Two non-obvious points building the fulfillment-tracking mirror (Q10). (1) Every other Shopify webhook parser here takes the resource gid straight from `admin_graphql_api_id` (F20: never reconstruct a gid). A `fulfillments/create`/`fulfillments/update` payload's `admin_graphql_api_id` is the FULFILLMENT's own gid — its ONLY reference to the parent order is the numeric `order_id` (NOT a gid). So the order gid genuinely has to be reconstructed (`gid://shopify/Order/{order_id}`) — the single case F20's rule can't cover, because Shopify gives no order gid to copy. `order_id` must be guarded: `bool` is an `int` subclass (reject `True`→`gid://shopify/Order/True`) and a string id must be ASCII-digits-only so nothing path-like is interpolated. (2) `fulfillments.order_gid REFERENCES orders(gid)` — a fulfillment can legitimately arrive for an order not (yet) mirrored (a stale pre-mirror order, or a failed `orders/create` sync). Relying on the FK to raise means a scary `ForeignKeyViolationError` traceback on the soft-fail path of a SIGNED delivery. Better: guard on order existence FIRST (`SELECT 1 FROM orders`) and skip quietly — the exact "known only" posture `customers/update` already uses (`customer_exists` before `upsert_customer`). (3) `Order.fulfillments` in the live GraphQL is a plain LIST (`[Fulfillment!]!` with a `first:` arg), NOT an edges/node connection like `lineItems` — parse it directly, no `edges`/`node` unwrap. `trackingInfo` is itself a list (a fulfillment can rarely carry >1 tracking number) — keep the first, matching the one-tracking-per-row mirror schema. Also selected `id` on the live fulfillment (not in the brief's literal field list) so the live-path `Fulfillment` carries its real gid, consistent with the mirror path (negligible cost).
**Correct:** `_order_gid_from_id(raw)` (bool-reject, int→gid, ASCII-digit-str→gid, else None); the parser returns `(order_gid, Fulfillment) | None`. `upsert_fulfillment(order_gid, f)` checks order existence then `INSERT ... ON CONFLICT (gid) DO UPDATE`; in-memory mirrors the skip (`order_gid in self.orders`). The three mirror reads attach `Order.fulfillments`, phone path batched `WHERE order_gid = ANY($1)` (no N+1, same discipline as the 2026-08-12 line-item fix). `read_fulfillments` scope: added FULFILLMENTS_CREATE/UPDATE to `REQUIRED_TOPICS` — `_ensure_one_topic` already isolates a per-topic userError, so a missing scope reports `"error"` for just those two topics and never blocks the other three (no special-casing needed).
**Pattern:** when a child webhook references its parent only by a numeric id (not a gid), reconstructing the parent gid is correct and unavoidable — but treat the numeric id as attacker-typed (reject bool, require ASCII digits) before interpolating. For a child row under a FK, guard on parent existence and skip quietly rather than letting the FK raise on a signed-delivery soft-fail path (matches the "known only" mirror posture). And re-check a GraphQL field's shape against the real schema before assuming it mirrors a sibling: `Order.fulfillments` is a bare list, `Order.lineItems` is a connection — a wrong `edges`/`node` unwrap would parse to empty and silently drop all tracking.

## [2026-08-14] A scope-gated sub-field inlined in a shared fragment ACCESS_DENIEDs the WHOLE query — decouple it; and a same-tx child write can't use the standalone existence guard
**Mistake/Issue:** The fulfillment feature (above) added `fulfillments(first: 5) { ... }` directly into the shared `ORDER_FIELDS` GraphQL fragment used by `get_order`/`find_order_by_name`/`find_customer_orders_by_phone`/`list_orders_created_since`. Reading fulfillments needs the `read_fulfillments` scope, which the live app does NOT have — and Shopify returns ACCESS_DENIED for the ENTIRE query when a selected sub-field lacks scope, not just that sub-tree. So the moment this shipped, `get_order`/`find_order_by_name` would return `None`/raise `ShopifyGraphQLError`, which `resolve_by_gid` swallows into "order not found" — i.e. EVERY Confirm/Cancel button tap, the reconcile sweep, and the live Q&A fallback would fail ("we couldn't find your order"), a total outage of the CURRENTLY-WORKING v1 flow. Every test was green because the `httpx.MockTransport` handlers returned a full node regardless of scope — a mock never enforces field-level auth (same "has tests ≠ matches real schema/scope" class as the 2026-08-12 `apiVersion` entry). Second trap found while fixing MEDIUM #3: the plan said "have `upsert_order_mirror` call `upsert_fulfillment` after the order row is written — the existence guard makes it ordering-safe." But `upsert_fulfillment` acquires its OWN pool connection; called from inside `upsert_order_mirror`'s open transaction (on a different connection), its `SELECT 1 FROM orders` cannot see the still-uncommitted order row → it would skip every fulfillment. The existence guard only works AFTER commit.
**Correct:** decouple the scope-gated sub-field into its own isolated, best-effort query — `ShopifyClient.get_order_fulfillments(gid) -> tuple[Fulfillment, ...]` runs `{ order(id:) { fulfillments(...) } }` alone and catches ANY `ShopifyError` (ACCESS_DENIED/throttle/outage) → `()`, never propagating. `get_order`/`find_order_by_name`/`find_customer_orders_by_phone` call it AFTER the core order fetch succeeds and attach via `replace(order, fulfillments=...)`, so a missing/revoked scope can only withhold tracking, never break lookup. The core order query no longer selects fulfillments at all — structural safety independent of scope state. Regression test drives the REAL client (MockTransport ACCESS_DENIED on the fulfillments sub-query only) through `resolve_by_gid` and asserts a valid `AuthorizedOrder` with `fulfillments == ()`. For the same-tx child write: extract `_upsert_fulfillment_on_conn(conn, ...)` (no existence guard) and call it on the SAME connection inside `upsert_order_mirror`'s transaction (the order row it just INSERTed is visible on that conn); keep the existence guard only in the standalone `upsert_fulfillment` (own connection, order already committed).
**Pattern:** any GraphQL sub-field (or REST expansion) that requires a scope/permission the app might not hold must be fetched by its OWN query, isolated behind a catch that degrades to empty — never inlined into a shared fragment that critical read paths depend on, because provider field-level auth fails the WHOLE request, not just the unauthorized branch. A mock transport can't catch this (it enforces no auth) — write a test that simulates the exact ACCESS_DENIED and assert the critical path still returns. And a child-row write that must run in a parent's open transaction has to execute on the SAME connection (a helper taking `conn`), because a method that acquires its own connection sees only committed data — an "existence guard makes ordering safe" claim silently becomes "skips everything" across a transaction boundary.

## [2026-08-14] The isolated best-effort sub-fetch is still pure latency when it can't matter — gate it on the parent's own state, at the SHARED choke point
**Mistake/Issue:** The decoupled `get_order_fulfillments` (above) made a missing `read_fulfillments` scope SAFE, but every call still paid for it: `find_customer_orders_by_phone` awaited `_with_fulfillments(o)` in a SEQUENTIAL list comprehension (up to 10 orders → 10 round trips one at a time on the customer-facing Q&A path), and while the scope is ungranted all 10 are guaranteed ACCESS_DENIED — pure added latency. `jobs/reconcile.py` (which never even READS fulfillments) doubled its call count the same way, one extra fetch per `get_order` in its sweep. An order whose `fulfillment_status` is `None`/`UNFULFILLED` has NO fulfillments yet, so issuing the query for it can never return anything.
**Correct:** two fixes. (1) Fan out concurrently — `list(await asyncio.gather(*(self._with_fulfillments(o) for o in orders)))` instead of the sequential comprehension. (2) Skip the sub-fetch entirely at the shared choke point: `_with_fulfillments` returns the order unchanged when `order.fulfillment_status in (None, "UNFULFILLED")`. Placing the skip in the shared helper (not just the phone path) is what makes `get_order` — and therefore the `reconcile` sweep — benefit too; reconcile has no fan-out list to `gather`, so the skip IS its whole fix. NON-OBVIOUS TEST FALLOUT: several existing client tests drove fulfillment-PARSING through an `UNFULFILLED` node (`ORDER_NODE.displayFulfillmentStatus == "UNFULFILLED"`) — a semantically impossible fixture (an unfulfilled order carrying tracking) that only "worked" because the old code fetched unconditionally. The skip made those tests stop issuing the follow-up query; fix is to drive them through a realistic `_FULFILLED_ORDER_NODE` (only a (partially) fulfilled order can have fulfillments), plus a new test asserting an UNFULFILLED order issues NO `fulfillments(first: 5)` query and returns `()`.
**Pattern:** making a scope-gated/optional sub-fetch SAFE (degrade-to-empty) is not the same as making it CHEAP — a best-effort call that can't return anything for the parent's current state is still a full round trip. Gate it on a field the parent already carries (here `fulfillment_status`), at the single shared helper so every caller inherits the skip, and fan out any per-item loop with `asyncio.gather` rather than awaiting serially on a customer-facing path. Watch for fixtures that only pass because the old code fetched unconditionally — a state-gated skip exposes any test whose parent state contradicts the child data it asserts.

## [2026-08-15] Deleting the webhook self-heal — `subscriptions.py` was NOT dead: the live receiver imports `REQUIRED_TOPICS`
**Mistake/Issue:** Task to remove the app-managed Shopify webhook self-heal (`ensure_subscription` + the `webhookSubscriptionCreate`/`Update` job, superseded by Admin-created webhooks) assumed `app/shopify/subscriptions.py` "should have zero remaining purpose once removed from the job router" — i.e. delete the whole module. But `app/channels/shopify_webhook.py` (the LIVE, deployed receiver, explicitly out of scope / must-not-change) does `from app.shopify.subscriptions import REQUIRED_TOPICS` and derives its `HANDLED_TOPICS` set from it; `tests/test_shopify_webhook.py` imports it too. Deleting the module wholesale would have broken the one file the task forbade touching. The self-heal (subscription CREATION) and the topic list (receiver-side ACCEPT list) live in the same file but are unrelated concerns.
**Correct:** keep the `REQUIRED_TOPICS` tuple in `subscriptions.py` (rewrote the module to that constant + an explanatory comment, no imports), and remove only the creation code (`ensure_subscription`, `_ensure_one_topic(_or_raise)`, `_raise_on_user_errors`, the `webhookSubscription*` GraphQL strings), the `_job_ensure_subscription` handler + its `JOBS` entry + import in `jobs/router.py`, and `tests/test_subscriptions.py`. In `test_jobs_router.py`, delete only the ensure_subscription-specific case and repoint the auth-path tests (which just needed *a* job name) at a live job. Renaming the file to move the constant was rejected — it would force an edit to the forbidden receiver. 786 passed/34 skipped (was 795/34: −9); `git diff` of `shopify_webhook.py` and `order_actions.py` both empty.
**Pattern:** before deleting a module during a "remove capability X" cleanup, `grep -rn` every export, not just the one the task named — two unrelated concerns can share a file, and a constant/type consumed by an out-of-scope live file survives the deletion. When the task's own framing ("this module should have zero purpose") collides with a hard "don't touch file Y" constraint, honour the constraint: keep the still-referenced symbol in place, strip only the dead code around it, and flag the retained part in the report.

## [2026-08-15] Owner-directed reversal to an INLINE webhook send — bounded `await` + single-row atomic claim, NOT BackgroundTasks
**Mistake/Issue:** N/A (informed, owner-directed architecture reversal, not a mistake). The external scheduler (cron-job.org) the owner relied on to trigger `/internal/jobs/outbox_drain` stopped working, so queued order confirmations were not being sent. The owner accepted the tradeoffs (including that the earlier same-day `BackgroundTasks` "send inline" attempt was rejected for being unreliable on Vercel's buffered Python runtime — see the two 2026-08-13 entries + ADR-001) and chose to move the PRIMARY send back into the webhook request as a genuine inline `await`, rather than keep depending on an external cron. The non-obvious hazards to avoid while doing so: (a) reintroducing the shared-queue-claim double-send race the atomic `queued→processing` claim fixed; (b) blowing Shopify's <5s ack budget with the sender's default 20s timeout; (c) letting an inline send failure turn the 200 ack into a 5xx (which burns Shopify's 19-failure retry budget and can tear down the subscription); (d) violating the `channels/` layering rule by importing `admin.controls` into the webhook edge.
**Correct:** (a) do NOT touch `claim_queued_outbound`/the generic drain path — carry the freshly-written row's own id OUT of the write (`IngestResult.outbound_id`, re-added: Postgres `INSERT ... ON CONFLICT DO NOTHING RETURNING id` via `fetchval`, in-memory the `_OutboundRow.id`; None on conflict/no-draft) and claim EXACTLY that row via a new `claim_outbound_by_id(id)` — an atomic single-statement `WHERE id=$1 AND state='queued' FOR UPDATE SKIP LOCKED` flip to `processing`. Whichever of the inline path / a racing backstop drain flips it first wins; the other gets None and sends nothing → never double-sends. (No stale-`processing` reclaim in the by-id claim: the inline path only ever targets a just-queued row; reclaiming an abandoned in-flight row stays `claim_queued_outbound`'s job, and a killed inline invocation's `processing` row is recovered by the drain's existing 10-min reclaim, while a retryable failure/timeout bumps it back to `queued`.) (b) add an OPTIONAL `timeout: float | None = None` to `send_one_outbound` — only the inline caller supplies it (`_INLINE_SEND_TIMEOUT_SECONDS = 3.0`); when None the cron path keeps `send_template`'s 20s default UNTOUCHED (pass the kwarg only in the `else` branch so the default call is byte-identical — a cron test asserting no timeout kwarg still passes). (c) wrap the whole inline attempt in the same defensive posture the drain uses — `send_one_outbound` already swallows `WhatsAppSendError`+Meta errors and never raises; add an outer `except Exception: log + return None` around the controls/config load + claim so nothing can propagate past the webhook boundary; the ack stays `{"ok", "duplicate", "queued"}` regardless of send outcome. (d) put the orchestration (`send_inline_outbound`) in `jobs/outbox_drain.py` (which already imports `admin.controls`/`channels`) NOT in `channels/shopify_webhook.py` — the webhook just `await`s the one function, keeping the edge free of `admin` imports (same layering fix as the Phase-4 `core.conversation` extraction). Enforce the `send_mode` kill switch exactly as elsewhere: `off` short-circuits before claiming (row stays queued for the backstop, mirroring `run_outbox_drain`), shadow/allowlist-miss suppress inside `send_one_outbound` with zero Meta calls. Keep `/internal/jobs/outbox_drain` alive as a manual/backstop tool; `reminders.py` out of scope (still needs external triggering). `core/order_actions.py` untouched (`git diff` empty), `test_button_dispatch.py` green.
**Pattern:** to move a durable-queue send onto a per-item inline trigger that can run concurrently with a still-existing drain, the fix is "don't claim from the shared queue at all" — carry the just-written row's id out of the write and atomically claim exactly it (`WHERE id=$1 AND state='queued' FOR UPDATE SKIP LOCKED`), so the two paths are mutually exclusive on that row and neither double-sends (same "the id that was WRITTEN is the id you PROCESS, no shared re-query" discipline as the key-identity-drift entries). A genuine inline `await` with an EXPLICIT short timeout is predictable and bounded (safe under a hard ack deadline) where `BackgroundTasks` on Vercel Python is silently unreliable — the timeout, not the mechanism, is what makes "send before ack" safe. When adding an optional tightening param (a shorter timeout) to a shared helper, pass it through ONLY when supplied so every existing caller's behaviour is provably unchanged. And keep cross-cutting orchestration (controls/config load + kill-switch) out of the `channels/` edge — host it in the job/orchestration layer the edge calls into.

## [2026-08-15] A bare-float httpx timeout is PER-OP, not total — and a `datetime.now()`-bearing signed webhook body re-derived twice self-invalidates its HMAC (test asserts nothing)
**Mistake/Issue:** Two review findings on the inline-send commit (243efd3). (1) The inline send's `_INLINE_SEND_TIMEOUT_SECONDS = 3.0` was passed as a bare float into `send_template(..., timeout=3.0)` → httpx expands it to `Timeout(connect=3, read=3, write=3, pool=3)` — PER-OPERATION limits, NOT a total-elapsed bound. A slow-but-not-hung Meta endpoint (slow connect, then a trickled response where no single read gap exceeds 3s) could take 6+ seconds total, blowing Shopify's <5s ack budget — the entire reason the timeout exists. (2) `test_inline_send_touches_only_its_own_row` (the ONLY webhook-level test proving "inline send touches only its own row, unrelated queued row untouched") called `payload(gid)` TWICE per order — once to build the body it SIGNED (`headers(json.dumps(payload(gid)).encode())`) and once to build the body it POSTED (`post(json.dumps(payload(gid)).encode())`). `payload()` embeds `created_at: datetime.now(UTC).isoformat()`, which differs by microseconds between the two calls, so the signed bytes never match the posted bytes → the Shopify HMAC verifier correctly 403s → the test dies on its first status assert and proves NOTHING. It was non-deterministic (order A's two calls sometimes landed in the same microsecond; order B's didn't), so it read as "flaky" but was deterministically broken.
**Correct:** (1) wrap the send in `asyncio.wait_for(send_one_outbound(...), timeout=_INLINE_SEND_TIMEOUT_SECONDS)` for a REAL total-elapsed bound regardless of how httpx splits connect/read/write; the resulting `TimeoutError` (an `Exception` subclass) is caught by the existing outer `except Exception` (never propagates past the webhook boundary; the timed-out row stays in `processing`, recovered by the drain's 10-min stale reclaim). Keep the per-op float too as defense-in-depth. Prove it with a sender that just `asyncio.sleep(8)`s (bypasses httpx entirely, so NO sub-timeout could fire) and assert the ack returns in <5s, not ~8s. (2) compute the signed body ONCE per order (`body = json.dumps(payload(gid)).encode()`) and reuse that same variable for both `headers(body, ...)` and `post(body, ...)` — the pattern every other test in the file already uses. Verified the isolation property genuinely holds by a mutation test: swapping `claim_outbound_by_id` for the shared `claim_queued_outbound` makes the test fail (the unrelated seeded row goes `queued -> undeliverable`), so the assertion has teeth.
**Pattern:** a bare-float timeout handed to httpx is four independent per-operation deadlines, never a wall-clock cap — when a hard total budget matters (a webhook ack window), enforce it with `asyncio.wait_for` around the whole call, not the transport's own timeout. And any test that SIGNS one serialization of a body and POSTS a freshly re-serialized copy will silently 403 whenever the payload carries a non-deterministic field (`datetime.now()`, a random id, a re-ordered dict) — serialize the signed bytes ONCE and reuse the exact bytes for both the signature and the request; a "flaky HMAC 403" in a webhook test is almost always a re-serialized-body bug, not transport flakiness.

## [2026-08-15] ⚠ DELIBERATE, OWNER-APPROVED EXCEPTION — the Shopify webhook logs the FULL raw payload incl. PII; do NOT "fix"/narrow it
**Mistake/Issue:** N/A — this is NOT a bug and NOT an oversight. `channels/shopify_webhook.py:shopify_webhook()` intentionally `logger.info(...)`s a `"SHOPIFY WEBHOOK RECEIVED: topic=… shop=… webhook_id=…"` line followed by `"ORDER JSON: %s"` with `json.dumps(payload, indent=2)` — i.e. the COMPLETE raw webhook body, which for `orders/create`/`orders/updated`/`customers/update`/`fulfillments/*` includes customer PII in plaintext (name, phone, email, shipping/billing address, tracking) to Vercel's log stream. This is a real, conscious deviation from this repo's otherwise-strict no-PII-logging discipline (several 2026-08-13 fixes narrowed `_mirror_order`/`_mirror_fulfillment`/the reminder+drain error paths to log only a gid + `type(exc).__name__` precisely to keep tracking numbers/phones out of the logs). The owner was told this plainly (raw JSON, unredacted, to the log stream) across two rounds of clarification and explicitly chose to proceed for debugging visibility. This entry exists so a FUTURE session does not "helpfully" redact/scrub/narrow it the way every other PII-logging case today was narrowed — that would silently defeat the owner's stated intent.
**Correct:** keep it as-is: two `logger.info` lines (NOT `print()` — Vercel captures `logging` identically while staying consistent with the codebase convention), NO redaction/scrubbing, logging the full `payload` dict. Placement is load-bearing for security: the log sits AFTER both HMAC verifiers pass, AFTER the shop-domain check, and AFTER `json.loads(raw)` yields a `dict`, and BEFORE any topic branching — so (a) a forged/unsigned/foreign-shop probe (403) NEVER has its attacker-supplied body logged (the debug log must not become a pre-auth information source), and (b) it fires uniformly for every handled topic, not just orders. A one-line comment above the log block marks it as intentional and points here. Tests pin all four properties: fires for a valid signed webhook (orders/create AND customers/update), and does NOT fire for a bad-HMAC probe or a foreign shop domain (proving the after-verification ordering). If the owner ever wants this off, remove the two lines + this exception — do not partially redact.
**Pattern:** an intentional, owner-approved deviation from a standing safety rule (here: no-PII-logging) must be documented as such AT the code site (comment) AND in error_learnings, or a later hardening pass will "correct" it in good faith and silently undo a deliberate decision. When a debug/observability log echoes an externally-supplied body, it MUST sit after the request's authentication/authorization gates — a pre-auth body log is an information-disclosure vector that logs forged input as if legitimate. Same "place the check at the right point in the pipeline" discipline as the HMAC-before-processing edges, applied to logging.

## [2026-08-15] The debug log above emitted NOTHING in prod (no logging config) — and `caplog` masked it; `json.dumps(indent=2)` is a ~4900x eager amplifier
**Mistake/Issue:** Two real bugs in the owner-approved webhook debug logging (above), both hidden by green tests. (1) The app had NO `logging.basicConfig`/`dictConfig`/`setLevel` anywhere in `app/` or `backend/api/index.py`, so at runtime the root logger sat at its default WARNING with zero handlers. The two new `logger.info(...)` lines were therefore silently discarded by `logging.lastResort` (which only surfaces WARNING+) — the owner paid the full PII-logging cost they accepted and got ZERO visibility in Vercel logs. The file's OTHER log calls survived only because they are `logger.error`/`logger.exception` (already above the threshold). **Why every test still passed:** the four existing debug-log tests call `caplog.set_level(logging.INFO, logger="app.channels.shopify_webhook")`, and pytest's `caplog` injects its OWN handler AND forces that logger's level to INFO — capturing the record regardless of the app's real runtime config. So the tests validated "the code calls `logger.info`", never "the deployed app is configured so INFO actually emits." A legitimate test-infra-masks-a-prod-config-bug gap. (2) `json.dumps(payload, indent=2)` on a validly-signed body within `MAX_WEBHOOK_BODY_BYTES` renders ~quadratically in nesting depth (reviewer measured 1MB in -> ~5.1GB out, ~4900x), built EAGERLY as a function argument on every delivery regardless of whether the level is enabled — an OOM-induced 500 on the signed hot path (burns Shopify's 19-failure retry budget).
**Correct:** (1) add a `_configure_logging()` in `app/main.py`, called at MODULE IMPORT time (Vercel's ASGI entrypoint is `api/index.py -> from app.main import app`, so import-time runs on every cold start; NOT gated behind `if __name__ == "__main__"`, which serverless never hits). Scope it to the `app` NAMESPACE logger only — `logging.getLogger("app").setLevel(INFO)` + a `StreamHandler(sys.stderr)` (Vercel captures stderr) — NOT a global `logging.basicConfig`, which would also promote httpx/litellm/asyncpg to INFO verbosity. Guard the handler add so a re-import never stacks handlers. (2) drop `indent=2` -> `json.dumps(payload)` (compact, ~1.49x, still the COMPLETE payload, just not pretty-printed). Verify the logging fix the way the reviewer did — a REAL (non-caplog) process import then `logging.getLogger("app.channels.shopify_webhook").isEnabledFor(logging.INFO) is True` + an actual `.info()` emit landing on stderr. The regression test (`tests/test_main.py`) asserts `isEnabledFor(INFO)` on a concrete app logger (pytest does NOT change root's level by default — no `log_level`/`log_cli` in `pyproject.toml` — so this reads the effective level caplog can't mask) AND a `StreamHandler` on the `app` logger (the emit gate; on `app`, not root, so pytest's own root handler can't falsely satisfy it).
**Pattern:** `caplog`/`assertLogs` prove your code CALLS the logger; they do NOT prove the deployed app is configured so that level actually emits — they inject their own handler+level. For any log line that must be VISIBLE in production (especially one whose whole purpose is observability), add a test that reads the real runtime config (`isEnabledFor(level)` on a concrete logger + a non-harness handler in the namespace chain), and empirically emit in a fresh process. A serverless app needs its logging configured at MODULE IMPORT of the ASGI entrypoint, scoped to your own namespace, or every `logger.info` below WARNING is silently dropped by `lastResort`. And never build a pretty-printed/expensive representation as an EAGER log argument (`json.dumps(x, indent=2)`) — it is paid on every call even when the level is disabled, and `indent` is a super-linear size amplifier on attacker-shaped nested input; log compact, and prefer `%`-style lazy args.

## [2026-08-15] Shopify's REST fulfillment webhook payload has NO delivery-date field — deliveredAt is GraphQL-only
**Mistake/Issue:** Adding capture of a shipment's actual delivery date (`Fulfillment.deliveredAt`, confirmed on the Admin GraphQL schema) tempts a symmetric change to the REST webhook parser (`fulfillment_from_webhook_payload`) — reading a `delivered_at`/`shipment_delivered_at`-style field off the `fulfillments/create`+`fulfillments/update` payload. That field does NOT exist: the REST Fulfillment webhook body carries `shipment_status` (a string enum — `label_printed`/`in_transit`/`out_for_delivery`/`delivered`/`failure`/…) which tells you delivery HAPPENED but not WHEN — there is no delivery TIMESTAMP anywhere in the REST payload. Inventing one (`payload.get("delivered_at")`) would silently always return None and mislead a future reader into thinking the webhook path captures it.
**Correct:** capture `deliveredAt` ONLY on the live Admin GraphQL read path — add it to the `fulfillments{…}` selection (`FULFILLMENT_FIELDS`) and parse it in `_fulfillments_from_node`. Leave the webhook parser's `Fulfillment.delivered_at` at its None default with an in-code comment stating the REST payload has no such field (do NOT read a phantom field). Because the webhook write path (`upsert_fulfillment`) therefore always carries `delivered_at=NULL`, and it legitimately wins the `shopify_updated_at` freshness guard when it is newer, a naive column overwrite would WIPE a delivery date a prior GraphQL/backfill write captured. Fix: COALESCE it in the DO UPDATE (`delivered_at = COALESCE(EXCLUDED.delivered_at, fulfillments.delivered_at)`) and mirror the same keep-existing-when-incoming-None in the in-memory store — a delivery date is monotonic (once delivered, never un-delivered), so keeping the known value is the fail-safe direction. Net: in the steady-state webhook-driven mirror `delivered_at` populates only via a live GraphQL read / one-time backfill, never the webhook push — a real, documented limitation, not a bug.
**Pattern:** a field confirmed to exist on Shopify's GraphQL schema is NOT guaranteed to appear on the corresponding REST webhook payload — verify the REST body shape before parsing it, and when it genuinely lacks the field, document the limitation rather than reading a phantom key. When a value can only ever be supplied by one of several write paths (here: GraphQL, not webhook), any per-column overwrite in a shared upsert must COALESCE-protect it, or the path that always carries NULL will silently clobber the path that carries the real value.

## [2026-08-15] Q19 new WABA template — REST order line item has NO image URL, Meta named params need `parameter_name`, and an en-only template must pin its send language
**Mistake/Issue:** Swapping the COD-confirmation template from `order_confirmation_cod` (3 positional params, no header) to `cod_confirmation` (IMAGE header + 6 NAMED params + 2 quick-reply buttons) on the new WhatsApp number surfaced three non-obvious traps. (1) The IMAGE header is meant to be the live product photo, but the Shopify REST `orders/create` webhook line item carries NO image URL — only `product_id`/`variant_id`/`variant_title`/`title`/`sku` (verified against a real order payload). So the photo cannot come from the webhook body; it needs a LIVE Admin GraphQL `product(id){featuredImage{url}}` fetch keyed on a product gid BUILT from the numeric `product_id`. (2) Meta's template send REJECTS a named-placeholder template if each body parameter object is sent positionally — a named template requires `{"type":"text","parameter_name":"<name>","text":"<v>"}` on EACH body param (Meta matches by name, not order); the old positional `{"type":"text","text":p}` shape silently mismatches. Meta also rejects an EMPTY named body param, so any missing product/order field must degrade to a placeholder (`"-"`), never `""`. (3) `cod_confirmation` is Meta-approved in `en` ONLY on the new WABA — keeping the old code's per-locale `choose_language()` (hi/gu) as the TEMPLATE send language would make Meta reject every non-en order (`#132001` template-does-not-exist-in-locale), silently reintroducing the exact "every order fails to send" bug the change fixes.
**Correct:** (1) parse the FIRST line item's `product_id` into `gid://shopify/Product/{id}` (bool-rejected, ASCII-digit-guarded — same guard as the fulfillment `order_id` gid builder) and split `variant_title` ("Cream / M") on `" / "` into colour/size (Shopify joins option values that way); add a best-effort `ShopifyClient.get_product_image_url(gid) -> str|None` that mirrors `get_order_fulfillments` (swallow any `ShopifyError`→None, return only a public `https` URL — Meta rejects non-https). **Resolve the image at INGEST time, cache the URL in the outbox `payload_json`** — NOT at send time — because the primary send is now INLINE in the webhook under a tight 3s budget AND the same payload is replayed by the backstop drain + the 1-hour reminder; resolving once keeps the tight-budget send path doing zero Shopify work, makes the reminder (which replays payload_json verbatim) need no change, and keeps `send_template` a pure formatter with no Shopify coupling. Bound the one added ingest-time Shopify call with `asyncio.wait_for` (best-effort; a failure/timeout → no `image_url` → send with no header, Q19a graceful degradation; a rare ack-budget overrun is a SAFE idempotent Shopify retry via webhook_id dedupe + claim-by-id no-double-send). (2) change `send_template`'s `body_params` to a `Mapping[str,str]` and emit `parameter_name` per body param; add an optional `header_image_url` → an `{type:"header",parameters:[{type:"image",image:{link}}]}` component ordered header→body→buttons. (3) pin the template send to `TEMPLATE_LANGUAGE="en"` (keep the customer's detected language only for free-form replies). A legacy outbox row still queued under the OLD payload shape now fails the stricter `_parse_payload` → terminal `undeliverable` (the old template is gone regardless), which is correct — no infinite retry.
**Pattern:** when a provider template gains a media header + named params, three separate things must all change together or the send silently fails: the DATA SOURCE (a REST webhook body often lacks media URLs that only the GraphQL/product API has — verify the real payload, don't assume symmetry), the WIRE SHAPE (named templates need `parameter_name`; empty named params are rejected — degrade to a placeholder), and the LOCALE (a template approved in one language must have its send language pinned, or every other-locale send is rejected). And decide media resolution by WHERE the time budget is: cache a resolved URL in the durable queue payload at ingest when the send path is tight/replayed, rather than re-fetching per send. Same "treat the send path like the inbound one — every field/shape/locale is a hard provider contract" posture as the outbound-leak entries.

## [2026-08-15] The template header image URL is an SSRF-adjacent sink — validate at the SOURCE (host allowlist), and Meta media-fetch codes (131052/131053) must retry image-less, not fail the message
**Mistake/Issue:** Two Q19 security-review MEDIUMs on the `cod_confirmation` product-image header. (F1) `ShopifyClient.get_product_image_url` accepted the URL on a bare `url.startswith("https://")` — no host allowlist, no length cap — the ONE new field pulled from a Shopify response not held to this repo's "cap/validate everything untrusted" posture (`MAX_FIELD_LEN=256` clip pattern). Not currently exploitable (the URL traces verbatim from Shopify's own `product.featuredImage.url` GraphQL response, never a webhook-payload-controlled string), but it's an SSRF-adjacent sink — Meta's servers fetch whatever URL lands in the header — and the value is persisted to `payload_json` and replayed up to 24h later by the reminder job. (F2) Meta returns codes 131052/131053 when a template header image can't be fetched (merchant deleted/replaced the product photo between order-time and a ≥1h-later reminder replay, which replays `payload_json` verbatim). Those codes were NOT in `_UNDELIVERABLE_CODES`, so the row exhausted its retry attempts and landed `failed` — the customer got NO confirmation at all, directly contradicting Q19a ("a Shopify hiccup must never block the confirmation message itself").
**Correct:** (F1) validate at the source with `urllib.parse.urlsplit`: `scheme == "https"`, `hostname` ends with `.shopify.com` (dot-anchored so `cdn.shopify.com.evil.com` / `evil-shopify.com` are rejected, not a substring match), and `len(url) <= 2048`; else `None` (same graceful-degrade as every other failure in that fn). Leave the two downstream `startswith("https://")` checks (`outbox_drain._parse_payload`, `whatsapp_sender.send_template`) as defense-in-depth with a comment that the real validation is now at the source. (F2) add `_MEDIA_ERROR_CODES = {131052, 131053}`; in `send_one_outbound`, when a send fails with one of those codes AND the row HAD an image, retry the SAME row once immediately with `header_image_url=None` (image-less) — the retry's result flows through the existing classification, so an image-less success = SENT (attempts NOT bumped), a subsequent non-media failure bumps normally. **Design decision on surfacing the Meta code:** did NOT add a structured field to `SendResult` — there was ALREADY a structured code path (`_meta_error_code(result.error)`, a `(?:^|; )code=(\d+)` regex on the rendered `_safe_error` string) that `_UNDELIVERABLE_CODES` classification already relies on. Reused it for the media-error check so both terminal-undeliverable and media-retry decisions read the code the SAME way; adding a parallel `SendResult.code` field would have created two divergent code-extraction mechanisms. Guarded the retry on `payload.image_url is not None` so it runs at most once (no infinite loop). Extracted a nested `_send(header_image_url)` closure so the original + retry send share the timeout-branching (cron 20s default vs inline short timeout) byte-for-byte.
**Pattern:** any URL pulled from an upstream response that becomes a header/link a THIRD party (Meta) will fetch, and that gets persisted+replayed, is an SSRF-adjacent sink — validate it at the source with a scheme + dot-anchored host allowlist + length cap, degrade to none on failure, and keep downstream checks as defense-in-depth. When a provider has a "the media/attachment failed but the message is otherwise valid" error class, treat those codes as retry-without-the-attachment, never terminal — a header that can't be fetched must not sink the whole message. And when a structured value (a provider error code) already has ONE extraction path the codebase relies on for another decision, reuse it rather than adding a second field — one code-reading mechanism, not two.

## [2026-08-15] Meta's template-listing `example.header_handle` URL is preview-only — using it as a real send's header image silently kills delivery
**Mistake/Issue:** While manually verifying the new `cod_confirmation` template via direct curl (before the app code was deployed), reused the `scontent.whatsapp.net/...` URL returned in `GET /{waba-id}/message_templates`'s `components[].example.header_handle` field as the actual `header.parameters[0].image.link` for a real send. Every send returned `message_status: accepted` with a valid `wamid` — normal-looking success — but NONE of 4 sends across 2 real, verified WhatsApp recipients ever arrived. Spent a long diagnostic cycle checking (all clean, none were the cause): phone number quality/verification/review status, WABA business-verification/account-review status, app-to-WABA webhook subscription + callback URL config, a second recipient number to rule out a per-contact block, and WhatsApp Manager's UI for any restriction banner. The account was never broken — sending Meta's own `hello_world` template via their dashboard's "Send message" button, from the same number to the same recipient, delivered instantly, which was the actual signal that isolated the fault to the IMAGE HEADER specifically (not the account/number/template-approval/app-config). Confirmed by swapping the header image to a verified-reachable public URL (`https://placehold.co/600x400.png`) — delivered immediately with everything else unchanged.
**Correct:** `header_handle` from the template-listing API is a short-lived/referrer-restricted URL for rendering the template PREVIEW inside Meta's own Template Manager UI — it is not a stable link WhatsApp's delivery servers can refetch when actually sending a message. `accepted`+`wamid` only means the request was syntactically valid and queued; it does NOT mean the header image was later fetched successfully — a failed async media fetch (Meta codes 131052/131053, see the entry above) can silently drop the whole message with no visible error on the send call itself, and no status webhook reached this app either (nothing logs the inbound `/webhook/whatsapp` body, a real observability gap). For manual/API testing of an image-header template, always use a genuinely public, freshly-verified-reachable HTTPS URL — never copy a URL out of the template-listing response's `example` block. The DEPLOYED app was never at risk from this specific failure mode: `ShopifyClient.get_product_image_url` sources the header image from Shopify's own live `product.featuredImage.url` GraphQL field, a real, stable, always-public `cdn.shopify.com` link — a structurally different, valid URL source from the broken preview handle.
**Pattern:** when a provider's read/listing API returns an `example`/preview URL alongside real config, verify by checking that API's docs (or testing) whether that URL is safe to reuse operationally — many are deliberately preview/audit-scoped and will look correct on every synchronous check while failing silently downstream. When a "delivery accepted but never arrives" symptom survives ruling out every account/number/app-config cause, isolate by testing the SAME send path with one variable removed/swapped at a time (here: swap only the header image) rather than continuing to re-check config that already checked out clean.

## [2026-08-16] COD-vs-prepaid template routing is gated by push_policy — a prepaid-routing test needs a prepaid-permitting policy, not just a prepaid payload
**Mistake/Issue:** The order-type-routing plan's Task 2 test (`test_prepaid_order_routes_to_prepaid_template`) built a prepaid `orders/create` payload (empty tags + a non-COD gateway) and expected the queued outbound's `template` to be `prepaid_order`, predicting the pre-fix RED would be `cod_confirmation`. But `shopify_webhook` reads `push_policy` (default `"cod_only"`) and calls `is_eligible_for_push`, which for a `cod_only` policy returns `order.is_cod()` — so a prepaid order is filtered out BEFORE the template-selection branch and no outbound row is queued at all. The test therefore RED-failed with `KeyError` on the outbound lookup, not the predicted wrong-template assertion — and would have stayed red even after the (correct) routing implementation, because the branch that picks the template is never reached for a prepaid order under the default policy.
**Correct:** the routing branch only runs for push-eligible orders; to exercise prepaid routing the test must first set a prepaid-permitting policy (`config.set_plain("push_policy", "all")`, or `"all_prepaid_no_buttons"`) — mirroring `is_eligible_for_push`'s `push_policy in ("all", "all_prepaid_no_buttons")` return. The `shopify_webhook.py` routing change itself (`TEMPLATE_NAME_COD if incoming.is_cod() else TEMPLATE_NAME_PREPAID`) was correct as planned; only the test fixture needed the policy set. The `outbox_drain` button-gating half of the task keys off the row's `payload.template == "cod_confirmation"` (not the push policy), so its test was unaffected.
**Pattern:** a plan's RED prediction can be wrong when an upstream eligibility/guard gate short-circuits the code path the test targets — re-verify each RED actually fails for the stated reason before implementing, and when a feature's behavior sits behind a config gate (here `push_policy`), the test must set that gate to reach the branch, not just supply data that "looks like" the case. Grep the guard (`is_eligible_for_push`) for its real predicate rather than assuming the payload alone selects the branch.

## [2026-08-16] Confirm-tap reply goes to `event.wa_id` (raw, no `+`), not the order's E164 — and a shared webhook-integration fake must patch the NEW sender too
**Mistake/Issue:** Two plan-vs-reality gaps while switching `order_actions._handle_confirm` from a plain-text reply to the `cod_confirmmsg` template send. (1) The plan's unit-test assertion pinned the template send's recipient to `OWNER_E164` (`"+919664290413"`), but every reply in `order_actions.py` sends to `event.wa_id` — the raw Meta sender id, which arrives WITHOUT a leading `+` (`"919664290413"`). The old `send_text`-based tests only captured the message BODY (`sends.last_text`), never the `to`, so the discrepancy was invisible until the new test asserted the full `(to, template, lang, params)` tuple. (2) The plan listed only `tests/core/test_button_dispatch.py`, but two integration tests in `tests/test_whatsapp_webhook.py` (`test_post_button_tap_{live_mode,allowlist_hit}_full_confirm`) assert "a confirm reply goes out" via `len(sends) == 1`, and their shared `_wire_real_dispatch` helper only monkeypatched `send_text`/`send_buttons`. After the switch the confirm reply is a `send_template` call, so the real (unpatched) `send_template` fired and its `WhatsAppSendError` was swallowed by `_safe_send_template` — `sends` stayed empty and both tests went red on the full-suite run (never in the per-file run the plan checked).
**Correct:** (1) assert `OWNER` (the raw wa_id) as the template recipient, not `OWNER_E164` — the reply target is the tapper's `event.wa_id`, consistent with all sibling handlers. (2) extend `_wire_real_dispatch` to also `monkeypatch.setattr("app.core.order_actions.send_template", fake_send_template)` and append `(to, template_name)` to the shared `sends` list, keeping each test's `len(sends)` intent intact. Both are test-only fixes; the `_handle_confirm` implementation (send to `event.wa_id`, params `[customer_display_name(order), order.name]`, pinned `en`) was correct as planned.
**Pattern:** when a change swaps the OUTBOUND primitive a handler calls (`send_text` -> `send_template`), grep the WHOLE test tree for every fixture that monkeypatches the old primitive to capture sends — a per-file test run hides breakage in a different file's shared helper that patches only the old function. And never trust a plan's literal for a value the old assertions never checked (here the reply `to`): confirm it against the actual code path (`event.wa_id`, no `+`) before locking the assertion.

## [2026-08-17] Any NEW customer-facing send must resolve its recipient via the mapping phone, NOT Order.best_phone() — best_phone() omits customer.phone
**Mistake/Issue:** The fulfillment shipped/delivered notification (`shopify_webhook._notify_fulfillment_events`) resolved its recipient as `order.best_phone()` and passed it straight into the `OutboundDraft`. `Order.best_phone()` is `phone or shipping_phone or billing_phone` — it does NOT consider `customer.phone`, whereas the ORIGINAL order-confirmation push (`parse_order_created`) ranks `customer.phone` ABOVE shipping/billing (`payload.phone or customer.phone or shipping.phone or billing.phone`). For any order where the checkout customer's own number differs from the shipping-address number (gift order, a relative's/reception number — routine for this store), the confirmation went to the CUSTOMER but this new notification went to the SHIPPING contact — silently sending someone's name + order number + tracking link to a person who never messaged the bot. Second issue: `best_phone()`'s value isn't guaranteed E.164 for a GraphQL-backfilled order (a raw unparseable string could reach `phone_e164`), which also breaks `delete_by_phone`'s exact-E.164 match (a DPDP right-to-erasure gap). This is the EXACT class already fixed one commit earlier in `reconcile._notify_cancelled` (commit 584b4af) — it regressed straight back in the next feature.
**Correct:** resolve `to = await c.ingest.get_mapping_phone(order_gid)` first (the original confirmation's actual recipient — already correctly ranked, already E.164, the number that held the WhatsApp conversation), fall back to `normalize_phone(order.best_phone())` only if no mapping row exists, and skip the send entirely (gid-only log, no PII, still ack 200) if neither resolves. `normalize_phone` on an already-E.164 value is idempotent, so the fallback is safe. Mirror `reconcile._notify_cancelled` verbatim.
**Pattern:** every NEW customer-facing send must resolve its recipient the SAME way the original confirmation did — via `get_mapping_phone(order_gid)`, never `Order.best_phone()` directly (best_phone omits `customer.phone`, the top-ranked source, and is not guaranteed E.164). When a security review fixes a recipient-routing bug on one send path, grep every OTHER send path for `best_phone()` before it regresses into the next feature. Related: give each independent best-effort notification (shipped vs delivered) its OWN try/except so one failing never drops a sibling — a single shared `try` spanning both means an early failure skips the later branch entirely (its dedupe key never written = permanently lost), and put the try INSIDE the per-notification send helper so isolation is structural, not positional.
