# Component Registry

> Reusable building blocks (services, adapters, pydantic models, FastAPI dependencies, helpers). Grep before creating anything new — never duplicate.

## Format
## [ComponentName]
- **File:** app/path/to/file.py
- **Purpose:** one line
- **Public API:** key functions/classes + signatures
- **Used in:** module names
- **Notes:** gotchas

---
<!-- entries below -->

## Settings
- **File:** backend/app/config/settings.py
- **Purpose:** pydantic-settings config; fail-fast on missing `APP_MASTER_KEY`.
- **Public API:** `Settings(BaseSettings)` — `app_master_key: str` (required, from env/.env), `database_url: str = ""`, `shop_domain: str = "thetavas.myshopify.com"`, `shopify_api_version: str = "2026-07"`, `request_timeout_seconds: float = 20.0`, `app_env: str = "dev"`, `cron_secret: str = ""` (env `CRON_SECRET`; empty disables the jobs endpoint), `admin_password: str = ""` (env `ADMIN_PASSWORD`; empty → admin login returns 503, never grants access — Rule 1 third env exception, approved 2026-07-30), `vertex_credentials_json: str = ""` (env `VERTEX_CREDENTIALS_JSON` — service-account JSON, secret, env-only, never returned to any UI), `vertex_project: str = ""` (env `VERTEX_PROJECT`), `vertex_location: str = "us-central1"` (env `VERTEX_LOCATION`).
- **Used in:** deps.Container, TokenManager, ShopifyClient, scripts/smoke_shopify.
- **Notes:** Shopify API version is read ONLY from `shopify_api_version` — never hardcode in URLs. Calling `Settings()` with no args needs `# type: ignore[call-arg]` (mypy strict; value loaded from env at runtime).

## SecretVault
- **File:** backend/app/config/crypto.py
- **Purpose:** Fernet encrypt/decrypt for secrets at rest.
- **Public API:** `SecretVault(master_key: str)`; `.encrypt(plaintext: str) -> str`; `.decrypt(token: str) -> str`; `VaultError(Exception)`.
- **Used in:** ConfigService, deps.Container.
- **Notes:** invalid master key → `VaultError` at construction; decrypt of garbage → `VaultError`. Error strings never contain the plaintext.

## ConfigRepo (Protocol) + InMemoryConfigRepo + PostgresConfigRepo
- **File:** backend/app/store/base.py (Protocol), backend/app/store/memory.py (in-memory), backend/app/store/postgres.py (Postgres)
- **Purpose:** async key→value string store for config/secrets.
- **Public API:** `ConfigRepo.get(key) -> str | None`, `.set(key, value) -> None`; knowledge-override methods (Phase 3.5): `.get_knowledge_override(kind) -> str | None`, `.set_knowledge_override(kind, content) -> None`, `.get_knowledge_overrides(kinds: list[str]) -> dict[str, str | None]`, `.bump_config_int(key) -> None`; `InMemoryConfigRepo()` (dict-backed); `PostgresConfigRepo(pool: LazyPool)` (UPSERT into `app_config` on set).
- **Used in:** ConfigService, deps.Container, knowledge.KnowledgeLoader, admin.router (knowledge PUT).
- **Notes:** `core` depends on the Protocol, never the concrete repo. deps picks Postgres when `database_url` is set, else in-memory. Knowledge overrides live in a separate `knowledge_overrides(kind PK, content, updated_at)` table (survives Vercel deploys, unlike seed files); `bump_config_int` writes a decimal-int string to `app_config` (`knowledge_version` → cache invalidation), created at "1" then `+1` (Postgres uses `(value::bigint + 1)::text`).

## IngestStore (Protocol) + InMemoryIngestStore + PostgresIngestStore + dataclasses
- **File:** backend/app/store/base.py (Protocol + dataclasses), backend/app/store/memory.py (in-memory), backend/app/store/postgres.py (Postgres)
- **Purpose:** ADR-001 atomic order ingest — dedupe (processed_webhooks) + mapping upsert (order_mappings) + outbox row (outbound_messages) in ONE transaction.
- **Public API:** `IngestStore.ingest_order_created(webhook_id, topic, mapping: MappingUpsert, outbound: OutboundDraft | None) -> IngestResult`; read-only views (Phase 3.5): `.recent_mappings(limit) -> list[MappingView]`, `.recent_outbound(limit) -> list[OutboundView]`; DPDP erasure/retention (2026-08-06): `.delete_by_phone(phone_e164) -> DeletionResult`, `.purge_older_than(cutoff: datetime) -> DeletionResult`; Phase 4 (2026-08-06): `.find_mappings_by_phone(phone_e164, limit=20) -> list[MappingView]`, `.count_orders_by_phone(phone_e164) -> int`. **Phase 5 outbox-drain + mutation-audit + mapping-status methods (2026-08-10):** `.claim_queued_outbound(limit=20) -> list[OutboundClaim]` (**atomic claim, 2026-08-13**: in ONE statement/transaction selects the oldest `state='queued'` rows AND flips them to `state='processing'`, via a CTE + `FOR UPDATE SKIP LOCKED` in Postgres so two overlapping cron runs can never both claim the same row; in-memory flips each returned row to `processing` too so the state machine matches), **`.claim_outbound_by_id(id) -> OutboundClaim | None` (2026-08-15 inline-send path)** — atomically claims ONE specific row by id (`WHERE id=$1 AND state='queued' FOR UPDATE SKIP LOCKED` → flip to `processing`; in-memory the same flip on the one row), so the inline order-confirmation send touches ONLY the row `ingest_order_created` just wrote and can never collide with the backstop `claim_queued_outbound`/drain; returns None if the row is missing or not `queued` (already claimed/sent) → the caller sends nothing. No stale-`processing` reclaim here (that stays `claim_queued_outbound`'s job), `.mark_outbound_sent(id, wamid)` (`processing -> sent`), `.mark_outbound_suppressed(id)`, `.mark_outbound_undeliverable(id, code)` (`processing -> undeliverable`), `.bump_outbound_attempt(id, code, max_attempts=5) -> 'queued'|'failed'` (increments attempts + last_error_code; moves `processing -> queued` for a later cron re-claim, or `-> failed` at the cap — **never left stuck in `processing`**), `.set_mapping_status(order_gid, status)`, `.record_order_action(order_gid, action, actor_wa_id, source_wamid, result, user_errors_json)` (INSERT into `order_actions`), **`.orders_awaiting_cancel_reconcile(limit=50) -> list[str]` (2026-08-16, atomic claim — was a plain status filter)**: atomically CLAIMS each returned order — flips it out of `cancel_requested` into a transient `cancel_reconciling` status in ONE statement (Postgres: CTE + `FOR UPDATE SKIP LOCKED`, `ORDER BY updated_at LIMIT $1`), so two overlapping reconcile runs can never both claim the same order (no double-send of `cod_cancel`). Also RECLAIMS a `cancel_reconciling` row abandoned by a killed invocation once `updated_at` is older than 10 minutes (same idiom as `claim_queued_outbound`'s stale-`processing` reclaim). The caller MUST finalize every claimed gid — advance it to `cancelled` on success, or release it back to `cancel_requested` (via `set_mapping_status`) on any skip/failure; in-memory mirrors the same state transition + staleness window for parity. **`.get_mapping_phone(order_gid) -> str | None`** (2026-08-16) — returns `order_mappings.phone_e164` (already E.164, written by the signed `orders/create` webhook) for one order gid, or `None` if there's no mapping/stored phone; used by the reconcile job to prefer the conversation's real phone over the order's raw (not-guaranteed-E.164) Shopify phone when sending `cod_cancel`. **Q17 reminder-sweep methods (2026-08-14):** `.find_stale_template_sent(older_than_minutes: int, max_age_minutes: int) -> list[MappingView]` (mappings still at `status='template_sent'` whose **`updated_at`** — the template-sent moment, NOT `created_at` — is between the two bounds: `updated_at <= now()-older_than AND updated_at > now()-max_age`, `ORDER BY updated_at LIMIT 25`; Postgres uses `make_interval(mins => $1/$2)` so the window is parameterizable/injection-safe, in-memory tracks a per-mapping `_mapping_updated_at` re-stamped on every ingest upsert + `set_mapping_status`; a Confirm/Cancel tap moves status off `template_sent` so a tapped order is auto-excluded. **2026-08-14 security fix**: was anchored on `created_at` + unbounded — see the reminder-sweep job entry for the HIGH#1/HIGH#2/MEDIUM#1 rationale), `.find_outbound_by_dedupe_key(dedupe_key: str) -> OutboundDraft | None` (narrow single lookup returning the ORIGINAL push's `{dedupe_key,kind,phone_e164,payload_json}` so the reminder reuses the exact payload verbatim, no Shopify re-fetch), `.find_outbound_by_dedupe_keys(dedupe_keys: list[str]) -> dict[str, OutboundDraft]` (**batched** sibling, one `WHERE dedupe_key = ANY($1)` round-trip; the sweep uses this to avoid a per-row N+1 — 2026-08-14 MEDIUM#1 fix; `[]` → `{}`), `.enqueue_outbound(outbound: OutboundDraft) -> bool` (standalone queue insert with the SAME `ON CONFLICT (dedupe_key) DO NOTHING` idempotency `ingest_order_created` uses — the UNIQUE `dedupe_key` IS the exactly-once guarantee; in-memory shares the extracted `_enqueue` helper with `ingest_order_created`). **Mirror-read methods (2026-08-12, database-first Q&A):** `.get_mirrored_order(gid) -> Order | None`, `.find_mirrored_order_by_name(raw_name) -> Order | None` (normalizes bare digits via `normalize_order_name` before matching `orders.name`), `.find_mirrored_orders_by_phone(phone_e164) -> list[Order]` (matches `phone` OR `shipping_phone` — billing intentionally excluded, per Q16 answered 2026-08-12; narrower than `delete_by_phone`'s three-column erasure predicate — **`ORDER BY updated_at DESC LIMIT 10`** — matching the Shopify fallback's `first: 10` — with line items **batch-fetched for all returned gids in ONE `= ANY($1)` query, never N+1**; in-memory mirrors the 10 cap; review fix 2026-08-12) — read the `orders`/`customers`/`order_items` mirror tables and hydrate full `Order` objects (with `line_items` + LEFT-JOINed embedded `Customer`); consumed only by `MirrorOrderSource` (see its entry). Postgres converts the `timestamptz` `cancelled_at`/`updated_at` columns back to ISO strings for the `str | None` dataclass fields. Frozen dataclasses: `MappingUpsert(...)`; `OutboundDraft(dedupe_key, kind, phone_e164, payload_json)`; **`OutboundClaim(id, dedupe_key, phone_e164, payload_json, attempts)`** (the queued row handed to the drain — by the time the sender sees it the row is already in state `processing`, see `claim_queued_outbound`); `IngestResult(duplicate, queued, outbound_id=None)` (**2026-08-15**: `outbound_id: int | None` carries the freshly-queued outbound row's id — None when nothing was queued/`ON CONFLICT DO NOTHING` — so the inline send can claim exactly that row; Postgres `INSERT ... RETURNING id` via `fetchval`, in-memory the `_OutboundRow.id`); `MappingView(order_gid, order_name, phone_e164, status, is_cod, created_at)`; `OutboundView(dedupe_key, state, kind, phone_e164, attempts, last_error_code, created_at)`; `DeletionResult(order_mappings, outbound_messages, conversations, messages, pending_actions, order_actions)` (per-table row counts; `processed_messages` intentionally NOT a field — see below). In-memory exposes `.webhooks/.mappings/.outbound` + (Phase 5) `.order_actions: list[dict]` for assertions.
- **Used in:** channels.shopify_webhook, deps.Container, admin.router (mappings/outbox views + POST /erasure), jobs.retention (purge job), core.order_resolver + core.conversation (Phase 4 — mapping fast-path + VIP threshold), core.mirror_order_source (2026-08-12 — the 3 mirror-read methods, database-first Q&A).
- **Notes (Phase 4 additions):** `find_mappings_by_phone(phone_e164, limit=20) -> list[MappingView]` — the fast path `order_resolver.resolve_by_phone` tries first (our own `order_mappings`, built by the Phase 2 webhook) before falling back to Shopify's customer-by-phone search; every result is still re-fetched live from Shopify and re-verified through `AuthorizedOrder`'s ownership invariant before use, never trusted as-is. `count_orders_by_phone(phone_e164) -> int` — backs the VIP/repeat-customer check (`AdminControls.vip_order_count_threshold`, default 3); `core.conversation` compares the count against the threshold to set `AgentContext.is_vip`, which agents may use to inform tone (e.g. a warmer welcome-back) but per the client decision must never announce the number itself.
- **Notes:** duplicate `(webhook_id, topic)` → `duplicate=True, queued=False` (short-circuit). `dedupe_key` UNIQUE (`order_created:{order_gid}`) = one push per order ever; re-seen dedupe_key → `queued=False`. `outbound=None` (ineligible/backfill) maps without queueing. Postgres detects rowcount via `_rows_affected(tag)`. **DPDP (2026-08-06):** `delete_by_phone` removes rows keyed to one phone from order_mappings + outbound_messages (both by `phone_e164`) + conversations/messages (by `user_id`, children-before-parents for FK) + **pending_actions (by `wa_id`) + order_actions (by `actor_wa_id`)** in ONE Postgres transaction; `purge_older_than` (the AUTOMATIC age-based job) deletes rows older than `cutoff` ONLY from the client-approved-for-deletion tables: pending_actions/order_actions on `created_at`, conversations/messages on `last_active_at`, **and blanket-ages `processed_messages` on `received_at`**. conversations/messages have NO writer yet (Phase 4) but are cleaned defensively. **⚠ Q15 conflict fix (2026-08-06):** `purge_older_than` NO LONGER touches `order_mappings`/`outbound_messages` — the client decided customer/order data is kept INDEFINITELY (round 3, client-decisions-all.md Q15), so the age-based job excludes those two tables entirely (its `DeletionResult` always reports `order_mappings=0, outbound_messages=0`). Only `delete_by_phone` (right-to-erasure-on-request) may remove order/outbound rows, and only for a specific number. Docstrings on both impls + `retention.py` + `DeletionResult` state this so a future editor does not re-add the DELETEs; a regression test asserts the age-based purge never even queries those tables. **Security-review completeness fix (2026-08-06):** pending_actions/order_actions/processed_messages were previously MISSED (incomplete erasure). `processed_messages` has NO `phone_e164` column — its message_id is a Meta wamid that embeds the sender number in cleartext base64, so it cannot be phone-scoped without decoding wamids (out of scope, documented as a known residual in `delete_by_phone`'s docstring + `_pipeline_status.md` deferred note); it is only aged out blindly by `purge_older_than`, hence NOT a `DeletionResult` field. **In-memory** models only mappings/outbound → `delete_by_phone` prunes those two dicts, all other counts (conversations, messages, pending_actions, order_actions) stay 0; in-memory `purge_older_than` is a documented no-op (in-memory rows carry no timestamp — the real age filter is Postgres-only). **Fulfillment mirror (2026-08-14, Q10 tracking):** `upsert_fulfillment(order_gid, Fulfillment) -> None` — upserts one fulfillment (courier/tracking) keyed by fulfillment gid; guards on order existence FIRST (Postgres `SELECT 1 FROM orders`, in-memory `order_gid in self.orders`) and SKIPS quietly if the order isn't mirrored, rather than hitting the FK (`fulfillments.order_gid REFERENCES orders(gid) ON DELETE CASCADE`) — same "known only" posture as `customers/update`. The three mirror-read methods (`get_mirrored_order`/`find_mirrored_order_by_name`/`find_mirrored_orders_by_phone`) also attach `Order.fulfillments`: the phone path batch-fetches them in ONE `WHERE order_gid = ANY($1)` query alongside line items (never N+1); the two single-order paths fetch via `_fetch_mirror_fulfillments`. Fed by the `fulfillments/create`+`fulfillments/update` webhooks (see api_registry). **Correction pass (security review):** (a) the DO UPDATE is now guarded on the fulfillment's OWN Shopify `updated_at` stored in a dedicated `fulfillments.shopify_updated_at` column (distinct from `updated_at` = our sync time) — `WHERE fulfillments.shopify_updated_at IS NULL OR EXCLUDED.shopify_updated_at >= fulfillments.shopify_updated_at`, so a replayed `fulfillments/create` (empty tracking) can't revert a newer `fulfillments/update`; shared Postgres helper `_upsert_fulfillment_on_conn`, mirrored in-memory via `_fulfillment_update_wins`. (b) `upsert_order_mirror` now PERSISTS any `order.fulfillments` it carries (capped `MAX_MIRROR_FULFILLMENTS=10`), so the one-time backfill — which attaches them via `get_order_fulfillments` — gives already-shipped historical orders their tracking instead of a permanently empty row. (c) in-memory `delete_by_phone` now pops `self.fulfillments[gid]` (Postgres already cascades) so DPDP erasure clears courier/AWB data. **delivered_at capture (2026-08-15, Q18 groundwork):** the `fulfillments` table + `Fulfillment` now carry `delivered_at` (Shopify `deliveredAt`, live-GraphQL only — the REST webhook payload has no such field, so the webhook write path always stores NULL). Both the batched phone read and `_fetch_mirror_fulfillments` round-trip it. The write path COALESCEs it (`delivered_at = COALESCE(EXCLUDED.delivered_at, fulfillments.delivered_at)` in Postgres; in-memory keeps `existing.delivered_at` when a winning update carries None) so a later `fulfillments/update` webhook — which legitimately wins the freshness guard but carries no delivery date — cannot wipe an already-captured one (a delivery date is monotonic). Pure capture/storage: NO deletion/pruning/retention logic is wired to it (that half is gated behind pending client decision Q18).

## LazyPool (asyncpg)
- **File:** backend/app/store/pg_factory.py
- **Purpose:** asyncpg connection pool created on FIRST `acquire()`, never at import (serverless cold-start rule).
- **Public API:** `LazyPool(dsn: str)`; `async with pool.acquire() as conn:`; `async close()`. Double-checked `asyncio.Lock` guards single pool creation. `create_pool(min_size=0, max_size=5, statement_cache_size=0)`.
- **Used in:** PostgresConfigRepo, PostgresIngestStore, deps.Container.
- **Notes:** asyncpg has no py.typed marker — `[[tool.mypy.overrides]] module="asyncpg.*" ignore_missing_imports=true` in pyproject.toml. **Supabase-host IPv4 pin (Vercel getaddrinfo EBUSY workaround, 2026-08-04):** when the DSN hostname contains `supabase.com`, `_get_pool` calls `_create_supabase_pool(host)` — it synchronously resolves IPv4 (`socket.getaddrinfo(host, None, family=AF_INET, type=SOCK_STREAM)[0][4][0]`) and passes `host=<ipv4>` + `ssl="require"` to `create_pool` (asyncpg kwarg overrides the DSN host; `ssl=require` encrypts without hostname verification since we connect by IP). This dodges Vercel's Python runtime raising `OSError [Errno 16] EBUSY` inside asyncio's threaded dual-stack (AF_UNSPEC) getaddrinfo. On resolution failure (`except OSError`) it falls back to the plain `create_pool(dsn, ...)` — never hard-fails worse than before. **Non-supabase DSNs are UNCHANGED** (no host override, no `ssl` kwarg) so local/other plain non-SSL Postgres still works. `statement_cache_size=0` kept on every path (transaction pooler breaks prepared statements). Wiring is unit-tested with mocks (`tests/store/test_lazypool_ipv4.py`); the bug itself only reproduces on the Vercel runtime.

## normalize_phone (E.164)
- **File:** backend/app/core/phone.py
- **Purpose:** normalize any phone/wa_id to E.164 with `+91` default for bare 10-digit Indian numbers.
- **Public API:** `normalize_phone(raw: str | None) -> str | None`.
- **Used in:** channels.shopify_orders (parser). core layer, no external deps.
- **Notes:** strips non-digits, drops leading `00`; 10-digit → `+91…`; 11-digit `0…` → `+91…`; 11–15 digits → `+…`; else None.

## Shopify order webhook parsing (IncomingOrder)
- **File:** backend/app/channels/shopify_orders.py
- **Purpose:** pure parse of an `orders/create` payload + language + push eligibility.
- **Public API:** `IncomingOrder(gid, name, order_number, email, phone_e164, customer_name, tags, gateways, created_at, locale, financial_status, product_name=None, product_color=None, product_size=None, product_gid=None)` with `.is_cod()`; `parse_order_created(payload) -> IncomingOrder | None` (None if gid/name missing); `choose_language(locale, default="en") -> str` (supported `{en,hi,gu}`, first 2 letters); `is_eligible_for_push(order, now, push_policy, staleness_hours) -> bool`. **`customer_display_name(order: Order) -> str`** (2026-08-16) — `"First Last"` from `order.customer` (stripped, joined with a space), or `EMPTY_PARAM_PLACEHOLDER` (imported from `channels.copy`) when there's no customer or both name fields are blank; the shared `{{1}}` name builder for the `cod_confirmmsg`/`cod_cancel` positional-param template sends (`core.order_actions`, `jobs.reconcile`).
- **Used in:** channels.shopify_webhook.
- **Notes:** GID taken from `admin_graphql_api_id` (never reconstructed — F20). Phone chain: order→customer→shipping→billing, each normalized. COD = gateway contains "cash on delivery" OR tag == "cod". Eligibility: created_at required + within staleness window; `cod_only` → COD only; `all`/`all_prepaid_no_buttons` → all. **Q19 (2026-08-15):** the FIRST line item's product is summarized for the `cod_confirmation` template (19b: multi-item order shows only item 1) — `product_name` (title), `product_color`/`product_size` split from `variant_title` (Shopify joins option values with `" / "`, so "Cream / M" → colour "Cream", size "M"; `_split_variant_options`), and `product_gid` built from the numeric `product_id` (`_product_gid_from_line_item`, bool-rejected/ASCII-digit-guarded) — the REST order webhook line item carries NO image URL, so the photo is fetched LIVE from that product gid. All clipped to `MAX_FIELD_LEN`. **2026-08-16:** `EMPTY_PARAM_PLACEHOLDER` is now imported from `channels.copy` (relocated there — three modules need it: this file, `shopify_webhook.py`, and `customer_display_name` above), not locally defined.

## Shopify webhook HMAC verifier (base64)
- **File:** backend/app/channels/shopify_signature.py
- **Purpose:** verify Shopify webhook signatures.
- **Public API:** `verify_shopify_hmac(raw_body: bytes, header_value: str | None, secret: str) -> bool`.
- **Used in:** channels.shopify_webhook.
- **Notes:** Shopify = **base64**(HMAC-SHA256(raw body, client secret)); constant-time compare. Distinct from Meta (hex). Missing/empty header → False.

## Meta webhook HMAC verifier (hex)
- **File:** backend/app/channels/whatsapp_signature.py
- **Purpose:** verify Meta WhatsApp Cloud API webhook signatures.
- **Public API:** `verify_meta_hmac(raw_body: bytes, header_value: str | None, secret: str) -> bool`.
- **Used in:** channels.whatsapp (Phase 3+).
- **Notes:** Meta = **hex**(HMAC-SHA256(raw body, app secret)) with `sha256=` prefix; constant-time compare. Distinct from Shopify (base64). Non-ASCII header bytes → False (fail-closed). Missing/empty header → False.

## WhatsAppConfig (Fernet-split secrets/plain)
- **File:** backend/app/channels/whatsapp_config.py
- **Purpose:** load WhatsApp Bot API credentials from ConfigService (secrets encrypted, plain values unencrypted).
- **Public API:** frozen dataclass `WhatsAppConfig(access_token, app_secret, verify_token, phone_number_id, waba_id, api_version)`; module tuples `WHATSAPP_SECRET_FIELDS = ("access_token", "app_secret", "verify_token")`, `WHATSAPP_PLAIN_FIELDS = ("phone_number_id", "waba_id", "api_version")`; `async load_whatsapp_config(config: ConfigService) -> WhatsAppConfig | None`.
- **Used in:** deps.Container, channels.whatsapp (Phase 3+).
- **Notes:** returns `None` if any of the 6 keys is unset; all 6 must be present for a valid config. Secrets are fetched via `config.get_secret(f"whatsapp:{field}")`, plain via `config.get_plain(f"whatsapp:{field}")`. No hardcoded `api_version` default (ADR-005: operational values are config, not code).

## WhatsApp inbound event parser
- **File:** backend/app/channels/whatsapp_inbound.py
- **Purpose:** parse a Meta webhook envelope into a typed inbound event.
- **Public API:** frozen dataclasses `InboundText(message_id, wa_id, text, timestamp)`, `InboundInteractive(message_id, wa_id, button_id, button_title, timestamp)`, `InboundButton(message_id, wa_id, payload, button_text, context_message_id, timestamp)`; `InboundEvent = InboundText | InboundInteractive | InboundButton`; `extract_event(payload: dict) -> InboundEvent | None`.
- **Used in:** channels.whatsapp (POST receive).
- **Notes:** F4 — template quick-reply taps arrive as `type:"button"` (→ `InboundButton`), NOT `type:"interactive"` (→ `InboundInteractive`, for buttons WE sent via send_buttons). Every field type-coerced (`isinstance` guards); status callbacks / unknown types / malformed / type-confused payloads → `None`, never an exception.

## WhatsApp sender (send_text / send_template / send_buttons)
- **File:** backend/app/channels/whatsapp_sender.py
- **Purpose:** outbound Meta Graph message sends.
- **Public API:** `SendResult(ok, status_code, wamid, error)` (frozen); `WhatsAppSendError(Exception)`; `async send_text(http, cfg, to, body, timeout=20.0) -> SendResult`; `async send_template(http, cfg, to, template_name, language, body_params: Mapping[str, str] | Sequence[str], button_payloads=(), header_image_url=None, timeout=20.0) -> SendResult`; `async send_buttons(http, cfg, to, body_text, buttons, timeout=20.0) -> SendResult` (`buttons: Sequence[tuple[id, title]]`).
- **Used in:** scripts/smoke_whatsapp; Phase 4/5 (outbox drain, confirm/cancel); `core.order_actions` (`cod_confirmmsg`), `jobs.reconcile` (`cod_cancel`).
- **Notes:** POST to `graph.facebook.com/{api_version}/{phone_number_id}/messages`, bearer `cfg.access_token`. HTTP >=400 → `SendResult(ok=False, ...)` (not raised); transport/timeout error → `WhatsAppSendError`. `send_buttons`: 1-3 buttons, title <=20 chars else `ValueError`. `send_template` quick-reply buttons carry an explicit per-button `payload` (F4) as `type:"button"` components indexed `"0".."2"`. **Q19 (2026-08-15):** `body_params` accepted a NAMED `Mapping[str,str]` (name→value) — the `cod_confirmation` template uses named placeholders, so each body parameter object carried `"parameter_name"` (Meta matches by name). **2026-08-16 widened:** `body_params` is now `Mapping[str, str] | Sequence[str]` — a `Mapping` still builds NAMED parameters (unchanged behavior, `cod_confirmation`/`prepaid_order`); a `Sequence[str]` (e.g. a `list[str]`) builds POSITIONAL parameters (`{"type":"text","text":value}`, no `parameter_name`, order matters) for templates with `{{1}}`/`{{2}}`-style placeholders (`cod_confirmmsg`, `cod_cancel`). Optional `header_image_url` (public https) adds an `{type:"header", parameters:[{type:"image", image:{link}}]}` component; omit → no header. Component order: header → body → buttons.

## Deterministic multilingual reply copy
- **File:** backend/app/channels/copy.py
- **Purpose:** fixed system reply strings (never LLM-generated) in en/hi/hinglish/gu.
- **Public API:** `SUPPORTED_LANGUAGES = ("en", "hi", "hinglish", "gu")`; `DEFAULT_LANGUAGE = "en"`; `copy_for(key: str, language: str) -> str`; **`EMPTY_PARAM_PLACEHOLDER: Final = "-"`** (2026-08-16, relocated here from a local `shopify_webhook.py` constant — Meta rejects an empty template body parameter, named or positional, so every template-sending call site substitutes this when a real value is unavailable; consumed by `channels.shopify_webhook`, `channels.shopify_orders.customer_display_name`).
- **Used in:** Phase 4 (conversation deterministic replies); Phase 5 (button-tap dispatch replies + two-phase cancel button titles); `channels.shopify_webhook`/`shopify_orders`/`jobs.reconcile` (`EMPTY_PARAM_PLACEHOLDER`).
- **Notes:** Phase 4 keys `order_confirmed`, `cancel_confirm_prompt`, `order_cancelled`, `order_not_found`, `refusal_other_order`, `error_fallback`. **Phase 5 keys (2026-08-10, all 4 languages):** `confirm_success`, `already_confirmed`, `cancel_are_you_sure`, `cancel_yes_title` (≤20), `cancel_no_title` (≤20), `cancel_requested`, `cancel_kept`, `cancel_too_late`, `already_cancelled`, `cancel_failed`, `not_found`. The two `*_title` keys are reply-button titles capped at Meta's 20-char limit (test-enforced). Unsupported language → English fallback; unknown key → `KeyError` (call sites internal, not user input). No emojis (CLAUDE.md). Wording is an OPEN owner/client review item before cutover.

## MessageStore (Protocol) + InMemoryMessageStore + PostgresMessageStore
- **File:** backend/app/store/base.py (Protocol), backend/app/store/memory.py (in-memory), backend/app/store/postgres.py (Postgres)
- **Purpose:** dedupe authority for inbound Meta message ids (`processed_messages`) — the Meta-side sibling of `processed_webhooks`.
- **Public API:** `MessageStore.record_if_new(message_id: str) -> bool` (`True` iff newly recorded); `InMemoryMessageStore()` (inspectable `.seen: set[str]`); `PostgresMessageStore(pool: LazyPool)`.
- **Used in:** channels.whatsapp (POST receive), deps.Container.
- **Notes:** Postgres: `INSERT ... ON CONFLICT DO NOTHING`, rowcount via `_rows_affected(tag) > 0` (the existing helper, not `.endswith`). deps picks Postgres when `database_url` set, else in-memory. `processed_messages(message_id PK, received_at)` table pre-exists (schema.sql).

## ConversationStore (Protocol) + InMemoryConversationStore + PostgresConversationStore + core.memory
- **File:** backend/app/store/base.py (Protocol + `StoredMessage`), backend/app/store/memory.py (in-memory), backend/app/store/postgres.py (Postgres), backend/app/core/memory.py (`load_history`/`persist_turn`)
- **Purpose:** windowed chat history + pause/handoff state per WhatsApp sender, backing the Phase 4 conversation engine.
- **Public API:** `StoredMessage(role, content, created_at: str | None)` (frozen); `ConversationStore.get_or_create(user_id) -> int`, `.recent_messages(conversation_id, limit) -> list[StoredMessage]`, `.append_message(conversation_id, role, content) -> None`, `.pause_until(conversation_id, until: datetime) -> None`, `.get_paused_until(conversation_id) -> datetime | None`, `.mark_handoff_attempted(conversation_id, at: datetime) -> None`, `.get_handoff_attempted_at(conversation_id) -> datetime | None`; `InMemoryConversationStore()`; `PostgresConversationStore(pool: LazyPool)`; `app.core.memory.DEFAULT_WINDOW = 8`, `async load_history(store, wa_id, window=DEFAULT_WINDOW) -> tuple[int, list[Message]]` (creates the conversation on first contact, replays only user/assistant turns as provider `Message`s), `async persist_turn(store, conversation_id, user_text, assistant_reply) -> None`.
- **Used in:** Phase 4 conversation engine (Tasks 12/14 — customer_support agent handoff logic, webhook wiring); wired into `deps.Container.conversations` (Task 13, picks Postgres/in-memory the same way `ingest`/`messages` do); `core.memory` depends only on the `ConversationStore` Protocol (no concrete store import), per `core` layering rule.
- **Notes:** `paused_until` = a human has already taken over; `handoff_attempted_at` (new column, this task) = one AI handoff attempt already used in the current window — deliberately a separate real column, not derived from message content, since "one attempt then immediate handoff" (client decision) needs both states to persist independently across messages. `conversations.user_id` holds the WhatsApp id; `get_or_create` picks the most-recently-active row per user and bumps `last_active_at` (no separate "close conversation" concept yet — a single ever-growing window per user, capped only by `limit`/`window` on read). `core.memory` deliberately has NO rolling-summary logic — only windowed history (last N turns), per task scope. In-memory `recent_messages` slices `list[-limit:]`; Postgres orders `DESC LIMIT` then reverses in Python to get chronological order.

## order_resolver (phone/order-name resolution + ownership)
- **File:** backend/app/core/order_resolver.py
- **Purpose:** the Phase 4 identity/ownership chain — turns a WhatsApp sender or a customer-typed order number into live, ownership-verified `AuthorizedOrder`(s), always re-fetched from Shopify (never trusts a stored mapping snapshot).
- **Public API:** `OrderSource` Protocol (`get_order`, `find_order_by_name`, `find_customer_orders_by_phone` — `ShopifyClient` matches structurally, no inheritance needed; `core` depends on the Protocol, not the concrete client, per layering rule); `async resolve_by_phone(shopify: OrderSource, ingest: IngestStore, wa_id: str) -> list[AuthorizedOrder]`; `async resolve_by_order_name(shopify: OrderSource, wa_id: str, raw_name: str) -> AuthorizedOrder | None`; **Phase 5 (2026-08-10):** `async resolve_by_gid(shopify: OrderSource, wa_id: str, gid: str) -> AuthorizedOrder | None` (deterministic button-dispatch ownership — re-fetch by gid live + ownership-check); `authorize_own_order(order: Order) -> AuthorizedOrder | None` (SYNC; wraps an order in an AuthorizedOrder from its OWN best phone for a system op with no tapper — used by the reconcile job to tag an order the bot already cancelled).
- **Used in:** `core.conversation` (Phase 4 pipeline); `core.order_actions` (Phase 5 button dispatch — `resolve_by_gid`); `jobs.reconcile` (Phase 5 — `authorize_own_order`).
- **Notes:** `resolve_by_phone` chain = `ingest.find_mappings_by_phone` (fast path) → live `shopify.get_order` re-fetch per mapping → falls back to `shopify.find_customer_orders_by_phone` only if the fast path yields nothing; every candidate is wrapped in `AuthorizedOrder(order, verified_phone=phone)`, which raises `ValueError` if the live phone no longer matches (a stale/reassigned mapping is silently dropped, never surfaced) — caught and skipped per-candidate. A `ShopifyError` mid-loop degrades to whatever was already resolved (often `[]`) rather than raising, so a transient Shopify outage doesn't break the conversation turn. `resolve_by_order_name` returns `None` uniformly whether the order doesn't exist OR exists but belongs to a different phone — so a reply can never be used to enumerate valid order numbers (Critical Rule 3, ownership-before-reveal).

## MirrorOrderSource (database-first OrderSource adapter)
- **File:** backend/app/core/mirror_order_source.py
- **Purpose:** answer the order-tracking chat Q&A from our own database mirror (`orders`/`customers`/`order_items`, kept near-real-time by the `orders/create`/`orders/updated`/`customers/update` webhooks) instead of a live Shopify call, falling back to live Shopify on a miss OR any database error (2026-08-12).
- **Public API:** `MirrorOrderSource(ingest: MirrorReadSource, shopify: OrderSource)` — structurally satisfies `OrderSource` (`get_order`/`find_order_by_name`/`find_customer_orders_by_phone`). `MirrorReadSource` is a NARROW local `Protocol` (just the 3 `IngestStore` mirror-read methods), not the full `IngestStore`, so a test double only implements those three. Each method tries the mirror first (`get_mirrored_order`/`find_mirrored_order_by_name`/`find_mirrored_orders_by_phone`) and returns the wrapped `shopify` result only on a mirror miss (`None`/empty list) or a swallowed DB error.
- **Used in:** `core.conversation._agent_reply` — constructed per-turn as `MirrorOrderSource(c.ingest, c.shopify)` and passed to `resolve_by_phone` + `_recover_order_by_name` for `order_tracking` turns ONLY.
- **Notes:** the internal `_safe(fn, *args)` wraps each mirror read in `try/except Exception → log + return None`, the same "degrade, never break the caller" boundary posture as `channels/shopify_webhook.py`'s `_mirror_order`/`_mirror_customer` — an infra hiccup on this read path costs one extra Shopify round-trip, never a failed turn. **Deliberately NOT used by the Confirm/Cancel mutation path** (`core.order_actions.dispatch_button` → `order_resolver.resolve_by_gid`), which keeps receiving the real Shopify client directly and always re-fetches live before any tagsAdd/orderCancel — Critical Rule 3 is untouched by this adapter (the mutation-path regression guard `tests/core/test_button_dispatch.py` proves nothing leaked).

## sanitize (strip_markdown)
- **File:** backend/app/core/sanitize.py
- **Purpose:** convert an LLM completion's standard Markdown into WhatsApp-safe plain text before it is sent.
- **Public API:** `strip_markdown(text: str) -> str`.
- **Used in:** `core.conversation` (applied to every agent reply, after `extract_reply_text`, before persist/send).
- **Notes:** WhatsApp renders `**bold**` literally (its own bold marker is a single `*`) — `**text**` → `*text*`; markdown heading markers (`#`.."######") stripped (WhatsApp has no heading concept); runs of 3+ blank lines collapsed to 1 blank line (keeps replies compact on a phone screen); final `.strip()`.

## Agents package (5-agent subagent architecture + shared base + router)
- **File:** backend/app/agents/{base,router,order_tracking,product_search,policy,recommendations,customer_support}.py
- **Purpose:** the Phase 4 specialist-agent layer — one narrow-scope agent per intent, a shared `AgentContext`/`AgentReply` contract, hardened LLM-JSON parsing, and a lightweight router that classifies each inbound message before dispatch. Supersedes the 2026-08-04 single-inline-prompt `engine.py` plan (never executed) per the client's round-3 (2026-08-06) decision that the bot needs specialized product search / order tracking / policy / recommendations / customer-support behavior, not one generic prompt.
- **Public API:**
  - `base.py`: `PERSONALITY: str` (shared "Friendly Fashion Advisor" system-prompt fragment, injected into every agent so tone is consistent regardless of which specialist answers); frozen `AgentContext(wa_id, phone_e164, user_text, history: list[Message], orders: list[AuthorizedOrder], is_vip: bool, knowledge: dict[str,str], provider: LLMProvider, model, api_key, extra_params, timeout=20.0)`; frozen `AgentReply(text: str, handoff: bool = False)`; `extract_json_blob(raw_text: str) -> dict[str, object] | None` (strips `<think>` blocks + ``` fences, `json.loads` then outermost-`{...}`-span fallback, never raises); `extract_reply_text(raw_text: str, fallback: str) -> str` (pulls `{"reply": "..."}`, degrades to `fallback` on JSON-with-no-usable-reply, trusts non-JSON plain text as-is, but a completion that *looks like* a failed JSON/fence attempt — starts with `{` or contains ``` — is never leaked to the customer verbatim, always `fallback` instead).
  - `router.py`: `Intent = Literal["order_tracking","product_search","policy","recommendations","customer_support"]`; `async classify_intent(provider, model, api_key, user_text, *, timeout=10.0, extra_params=None) -> Intent` (single strict-JSON LLM call; any provider error or unparseable/unrecognized completion degrades to `"customer_support"`, the safe catch-all — never leaves a message unrouted).
  - `order_tracking.py`: `async run(context: AgentContext) -> AgentReply` — answers only from `context.orders` (verified order history), states cancel-before-dispatch policy (`fulfillment_status in (None, "UNFULFILLED")` = eligible), never cancels itself (only offers to bring up the Confirm/Cancel button — actual mutation is Phase 5's deterministic button-tap dispatch, per Critical Rule 2).
  - `product_search.py`: `ProductSource` Protocol (`async search_products(query, limit=5) -> list[Product]`, matched structurally by `ShopifyClient`); `async run(context: AgentContext, shopify: ProductSource) -> AgentReply` — calls Shopify live, grounds the prompt in the real results only ("never invent a product"), `ShopifyError` degrades to an empty result list (not a crash). The raw `context.user_text` passed as `query` is validated Shopify-side by `ShopifyClient._is_safe_product_query` before it ever reaches Shopify's search DSL (see the `ShopifyClient` entry above for the injection-defense detail).
  - `policy.py`: `async run(context: AgentContext) -> AgentReply` — grounds strictly in `context.knowledge["faq"]`/`["business"]` (the KnowledgeLoader-assembled real Thetavas policy text), explicitly told published policy takes precedence even under customer pushback.
  - `recommendations.py`: `async run(context: AgentContext, shopify: ProductSource) -> AgentReply` — reuses `product_search.ProductSource` verbatim (not redefined); grounded cross-sell/upsell/complete-the-look framing, explicitly told to answer the original question first and never be pushy (one or two genuine suggestions).
  - `customer_support.py`: `HANDOFF_MESSAGE: str`; `async run(context: AgentContext, store: ConversationStore, conversation_id: int, now: datetime) -> AgentReply` — the catch-all + human-handoff owner. One AI attempt per 24h window (`_HANDOFF_WINDOW`) then immediate handoff (client decision, round 3): an explicit human-request phrase on a SECOND attempt within the window skips the LLM entirely and hands off deterministically; a first request still gets one LLM attempt (marks `handoff_attempted_at` first); any `ProviderError` or the LLM's own fallback-degraded reply is also treated as "could not help" → handoff (`store.pause_until(conversation_id, now + 24h)`, `AgentReply(handoff=True)`).
- **Used in:** `core.conversation.run_turn`'s `_run_agent` dispatcher (routes on the classified `Intent` to the matching agent; `customer_support` alone receives `conversations`/`conversation_id`/`now` for its pause/handoff side effects, the other four are stateless besides their Shopify/LLM calls).
- **Notes:** every agent's system prompt ends with the identical `Respond with STRICT JSON only, no other text: {"reply": "<your reply to the customer>"}` instruction — `extract_reply_text` is the single shared parser every agent calls, no agent redefines its own parsing. **Review fix (Task 6, not in the original brief):** an earlier version of `extract_reply_text` fell through to returning the RAW completion text (including literal `{"reply": ...}` JSON syntax or a stray code fence) whenever the JSON-shape detection missed an edge case — found during review as a customer-facing content-safety bug (the customer would see literal `{"reply": "..."}` text); fixed by tightening the "looks like a failed JSON/fence attempt" check so any such case falls back to the safe `fallback` string instead of leaking parser-internal syntax. `recommendations` is deliberately the ONLY entry point for recommendation content — other agents do not cross-sell inside their own replies (explicit brainstorming/scope decision, not an oversight).

## WhatsApp webhook router (GET verify + POST receive)
- **File:** backend/app/channels/whatsapp.py
- **Purpose:** Meta webhook edge only — GET subscribe verification + POST receive (HMAC, dedupe); dispatches each fresh `InboundText` into the conversation engine via a single narrow call.
- **Public API:** `router` — `GET /webhook/whatsapp`, `POST /webhook/whatsapp`.
- **Used in:** main.app.
- **Notes:** GET: `hub.mode==subscribe` + ASCII-safe constant-time `hub.verify_token` compare → echoes `hub.challenge`, else 403. POST: 403 on bad/unconfigured HMAC; 413 if body > 1 MiB; foreign `phone_number_id` / status callback / unparseable / non-dict / unknown-type → 200 `{"ok": true, "ignored": true}`; response shape (unchanged since Phase 3) is `{"ok": true, "processed": N, "duplicate": N, "results": [{"message_id", "duplicate", "event_type"}, ...]}` (one aggregate ack per delivery, since Meta may batch several messages). For every fresh (non-duplicate) `InboundText`, `await run_turn(c, event, budget_seconds=remaining)` (from `app.core.conversation`) runs before the response is built. **Phase 5 button wiring (2026-08-10):** a fresh `InboundButton`/`InboundInteractive` now runs `await dispatch_button(c, event)` (from `app.core.order_actions`) at the same seam, gated on the send-mode kill switch: the raw `send_mode` config key is read ONCE per delivery (only when a tap is present, so a text-only delivery pays nothing) and dispatch runs only for `shadow`/`allowlist`/`live` — `off`/unset/corrupt = disabled (whitelist fails safe). `send_mode` is read RAW via `c.config.get_plain("send_mode")` rather than `load_controls` so `channels/` stays admin-free (the layering rule below); `dispatch_button` itself is wrapped in `try/except Exception` so a tap can never 500 the signed webhook (Rule 5.5). This file's only non-`channels`/`config`/`deps` imports are now `app.core.conversation.run_turn` + `app.core.order_actions.dispatch_button` (both `core`, layering-clean). **Layering fix (2026-08-06, post-Task-14 security review):** the conversation-pipeline logic (send_mode/pause gating, order resolution, knowledge loading, router dispatch, agent invocation, persist, send) originally lived directly in this file, importing from `admin`/`agents`/`core`/`knowledge` — violating `fastapi-layering.md` (`channels/` = webhook parsing/HMAC/idempotency/sending only). Extracted wholesale (mechanical, behavior-neutral) into `app/core/conversation.py`.

## Conversation engine (Phase 4 router + 5-agent pipeline orchestrator)
- **File:** backend/app/core/conversation.py
- **Purpose:** turns one fresh inbound WhatsApp text into a persisted, conditionally-sent reply — the send_mode/pause-gated router + 5-agent pipeline. Extracted from `channels/whatsapp.py` (2026-08-06 layering fix) so `channels/` stays limited to the webhook edge.
- **Public API:** `async run_turn(c: Container, event: InboundText) -> None`; `TURN_TIMEOUT_SECONDS: float = 55.0` (module-level, deliberately mutable-at-test-time via monkeypatch). Internal: `async _run_turn(c, event) -> None` (the actual pipeline body), `async _run_agent(context: AgentContext, intent: Intent, c: Container, conversation_id: int, now: datetime) -> AgentReply`.
- **Used in:** `channels.whatsapp.receive_webhook` (the only caller).
- **Notes:** unlike `order_resolver`/`memory`/`sanitize` (which depend only on Protocols), this module is an intentional orchestrator — it imports concrete adapters (`admin.controls`, `agents.*`, `channels.copy`/`whatsapp_config`/`whatsapp_sender`, `knowledge.loader`, `deps.active_llm`) because gluing them together IS its job; this is the shape the layering fix asked for (isolate the orchestration in `core/`, not eliminate it). `run_turn` order of checks (all inside `try/except TimeoutError` then `except Exception: logger.exception(...)`, so neither a downstream failure nor a timeout ever breaks the webhook's 200 ack for an already-deduped message): (1) `load_controls`; `send_mode == "off"` returns immediately — no `load_whatsapp_config`, no conversation store touch, no Shopify call, no LLM call, nothing runs. (2) `load_whatsapp_config`; `None` (not configured) also returns immediately. (3) `load_history` (creates the conversation on first contact) then `get_paused_until` — if paused (a human has taken over), `append_message(..., "user", event.text)` is called so the human sees the message, then returns immediately, still before any Shopify/LLM call. (4) only past the pause gate: `resolve_by_phone` (ownership-checked) + `count_orders_by_phone` (VIP threshold) + `active_llm` (falls back to `copy_for("error_fallback", "en")` if no provider is active) + `classify_intent` (router) + the matched specialist agent (`_run_agent` dispatches on `Intent` to `order_tracking`/`product_search`/`policy`/`recommendations`/`customer_support`, the last one taking `conversations`/`conversation_id`/`now` for its pause/handoff side effects) + `strip_markdown` on the reply. (5) **empty-reply guard (2026-08-06):** if the stripped reply is blank (e.g. a completion that's only a code fence, or an agent's own degraded output), it is replaced with `copy_for("error_fallback", "en")` BEFORE persisting/sending — never persist or send an empty WhatsApp message. (6) `persist_turn` ALWAYS runs (both user + assistant turns), even in `shadow` mode or when `allowlist` will suppress the send. (7) send gate: `shadow` → return before `send_text`; `allowlist` → only sends if `normalize_phone(event.wa_id)` is in `controls.allowlist_phones`; `live` (and any other value) → always `send_text`. (8) **`SendResult` failure is now checked (2026-08-06):** `if not result.ok: logger.warning("whatsapp send failed: status=%s error=%s", ...)` — `SendResult.error` is already secret-redacted by `whatsapp_sender._safe_error`, safe to log directly; previously a failed send was silently discarded. `phone` (E.164, may be `None` for an unparseable wa_id) is reused for VIP lookup, `AgentContext.phone_e164` (falls back to raw `wa_id` if unparseable), and the allowlist check. **Timeout hardening (2026-08-06, CRITICAL security-review fix):** the whole turn runs inside `async with asyncio.timeout(TURN_TIMEOUT_SECONDS)`; `TURN_TIMEOUT_SECONDS=55.0` is chosen to sit comfortably under whatever Vercel function `maxDuration` is eventually configured (expected ~60s range), so a near-deadline turn is cancelled and logged as a WARNING here rather than left to an uncaught platform 504. **⚠ Correction (2026-08-06, same day):** an earlier version of this fix also added `functions["api/index.py"].maxDuration=60` to `backend/vercel.json` — REVERTED, because Vercel treats `functions` and `builds` as mutually exclusive in `vercel.json` (hard deploy-time error: "The functions property cannot be used in conjunction with the builds property"), and this repo's `vercel.json` already depends on legacy `builds`/`routes` for the catch-all ASGI routing. `vercel.json` is back to its original `builds`/`routes`/`regions`-only content (verified identical via `git diff` against the pre-fix commit). **`maxDuration` must instead be set via the Vercel dashboard (Project Settings → Functions) once the project is connected — this is a deploy-time step, not a code change; tracked in `_pipeline_status.md`'s deferred list, not as a code TODO.** The internal `TURN_TIMEOUT_SECONDS` guard below stands on its own regardless of the platform limit (defense-in-depth: it should fire and log cleanly even after the dashboard limit is raised, rather than relying on the platform default). Why this matters: `MessageStore.record_if_new` has already marked the inbound message as seen by the time this runs, so if Vercel's own timeout killed the function first (raw 504, no Python exception at all), Meta's retry of the same message would be silently dropped as a duplicate and the customer would simply never get a reply, with nothing logged — the internal timeout is what turns that failure mode into an observable, bounded one. `asyncio.timeout()`'s own deadline-triggered cancellation is caught as `TimeoutError` (builtin alias since Python 3.11) specifically (a plain `except Exception` would technically also catch it, since `TimeoutError` subclasses `OSError`/`Exception`, but a dedicated clause gives an unambiguous "timed out" log signal distinct from the generic failure log). **Database-first order-tracking reads (2026-08-12):** `_agent_reply` no longer passes the raw `c.shopify` into the `order_tracking` resolve step — it builds `order_source = MirrorOrderSource(c.ingest, c.shopify)` once per turn and passes THAT to both `resolve_by_phone` and `_recover_order_by_name`, so the Q&A reads from our own mirror tables first and only hits live Shopify on a mirror miss or DB error. This applies to `order_tracking` turns ONLY; the Confirm/Cancel mutation path (`order_actions.py`'s `resolve_by_gid`) is untouched and keeps re-fetching from Shopify directly (Critical Rule 3).

## Shopify webhook topics (REQUIRED_TOPICS) — self-heal REMOVED (2026-08-15)
- **File:** backend/app/shopify/subscriptions.py
- **Purpose:** now holds ONLY the `REQUIRED_TOPICS` tuple (`ORDERS_CREATE, ORDERS_UPDATED, CUSTOMERS_UPDATE, FULFILLMENTS_CREATE, FULFILLMENTS_UPDATE`) — the set of Shopify webhook topics this app accepts. It is receiver-side data: `channels.shopify_webhook` derives `HANDLED_TOPICS` from it, and Admin-created webhooks must be registered for exactly these topics.
- **Public API:** `REQUIRED_TOPICS: tuple[str, ...]` (module constant). No functions.
- **Used in:** `channels.shopify_webhook` (derives `HANDLED_TOPICS`).
- **⚠ Removed (2026-08-15):** the app-managed self-heal — `ensure_subscription`, `_ensure_one_topic(_or_raise)`, `_raise_on_user_errors`, and the `webhookSubscriptions`/`webhookSubscriptionCreate`/`webhookSubscriptionUpdate` GraphQL strings — plus the `ensure_subscription` job (`_job_ensure_subscription` + its entry in `jobs.router.JOBS`) and `tests/test_subscriptions.py`. Webhook delivery is now Admin-created (see api_registry "Shopify webhook subscription management — REMOVED"); recreating an app-managed subscription would double-deliver every event. Do NOT reintroduce a `webhookSubscriptionCreate` path.

## ConfigService
- **File:** backend/app/config/service.py
- **Purpose:** plain + encrypted config access over a ConfigRepo + SecretVault.
- **Public API:** `ConfigService(repo: ConfigRepo, vault: SecretVault)`; async `get_plain/set_plain(key[, value])`, `get_secret/set_secret(key[, value])` (secrets encrypted at rest).
- **Used in:** TokenManager, deps.Container, scripts/smoke_shopify.
- **Notes:** secret keys in use: `shopify:client_id`, `shopify:client_secret`, `shopify:webhook_signing_secret` (the per-store Admin→Notifications webhook signing secret; 2nd accepted secret in the `/webhooks/shopify` HMAC check — added 2026-08-14), `shopify:access_token`. Plain key: `shopify:token_expires_at`.

## Shopify models + normalize_order_name
- **File:** backend/app/shopify/models.py
- **Purpose:** frozen domain dataclasses + pure derivations.
- **Public API:** `Money(amount, currency)`; `Order(...)` with `.best_phone()`, `.is_cod()`, `.is_cancelled()`; `AuthorizedOrder(order, verified_phone)`; `CancelRequested(job_id)`; `normalize_order_name(raw, prefix="tavas") -> str`; **`Fulfillment(gid, status, tracking_company, tracking_number, tracking_url, created_at=None, updated_at=None, delivered_at=None)`** with `.has_tracking()` (2026-08-14; `updated_at` = Shopify's own last-modified stamp, added in the security-review correction pass — drives the mirror's out-of-order-delivery guard. **`delivered_at` (2026-08-15)** = Shopify's own delivery timestamp from Admin GraphQL `Fulfillment.deliveredAt` — capture-and-store only for a future retention/pruning decision (Q18); None until delivered AND for every webhook-parsed Fulfillment — the REST fulfillment webhook payload has NO delivery-date field, so only the live GraphQL read populates it).
- **Used in:** ShopifyClient (reads/mutations), core.order_resolver (Phase 2+).
- **Notes:** phone chain = order.phone → shipping_phone → billing_phone. COD = gateway contains "cash on delivery" (case-insensitive) OR tag == "cod". `normalize_order_name` maps `tavas3733`/`#tavas3733`/`3733`/`#3733` → `tavas3733`. `AuthorizedOrder` is the ADR-004 mutation gate — only core.order_resolver should construct it in production. Phase 1 security fix: `AuthorizedOrder.__post_init__` raises `ValueError` unless `verified_phone` is truthy AND matches one of the order's phones — the invariant is now enforced at construction, not just documented. **Fulfillment tracking (2026-08-14, Q10):** `Order.fulfillments: tuple[Fulfillment, ...] = ()` — split shipments modelled as a list (an order can have several fulfillments, each with its own tracking; a single fulfillment can rarely carry >1 tracking number → we keep the first, one-per-row). Populated on BOTH read paths, but tracking is fetched via a SEPARATE, isolated query — NOT inline in `ORDER_FIELDS` (security-review HIGH fix, correction pass): reading fulfillments needs the `read_fulfillments` scope, and if it is missing/revoked Shopify ACCESS_DENIEDs the WHOLE query it appears in, which would take down `get_order`/`find_order_by_name` and every Confirm/Cancel tap. Live path: `ShopifyClient.get_order_fulfillments(gid) -> tuple[Fulfillment, ...]` runs `{ order(id:) { fulfillments(first: 5) { id status trackingInfo(first: 3) { company number url } createdAt updatedAt deliveredAt } } }` on its own and swallows any `ShopifyError` (ACCESS_DENIED/throttle/outage) into `()`; `get_order`/`find_order_by_name`/`find_customer_orders_by_phone` call it best-effort AFTER the core order fetch succeeds and attach via `replace(order, fulfillments=...)`, so a missing scope only withholds tracking, never breaks lookup. Mirror path: the `fulfillments` table. Read-only Q&A enrichment; NEVER on a mutation path.

## Shopify error taxonomy
- **File:** backend/app/shopify/errors.py
- **Purpose:** typed Shopify-layer failures.
- **Public API:** `ShopifyError` (base); `ShopifyAuthError`, `ShopifyThrottled`, `ShopifyUnavailable`, `TokenGrantError`; `ShopifyGraphQLError(messages: list[str], codes: tuple[str, ...] = ())` with `.messages` and `.codes`.
- **Used in:** TokenManager, ShopifyClient.
- **Notes:** error messages never include secrets/tokens. `codes` is the optional second positional (Phase 1 security fix); callers passing only `messages` still work. (The former `subscriptions._raise_on_user_errors` caller was removed 2026-08-15 with the webhook self-heal.)

## TokenManager (ADR-003)
- **File:** backend/app/shopify/token_manager.py
- **Purpose:** client-credentials access token with store persistence, 1h refresh margin, single-flight.
- **Public API:** `TokenManager(http, config, settings, now=time.time)`; async `get_token() -> str`, `force_refresh() -> str`. `REFRESH_MARGIN_SECONDS = 3600`.
- **Used in:** ShopifyClient, deps.Container, scripts/smoke_shopify.
- **Notes:** caches in-memory + persists token (secret) & expiry (plain) to ConfigService; asyncio.Lock double-checked → one grant under concurrency. Grant failures raise `TokenGrantError` without leaking client_secret.

## ShopifyClient (5 ops + 1 grounded search, Phase 4)
- **File:** backend/app/shopify/client.py
- **Purpose:** Admin GraphQL client — transport + 3 reads + 2 mutations + 1 grounded product search (Phase 4 Task 5, 2026-08-06).
- **Public API:** `ShopifyClient(http, tokens, settings)`; `@property api_version -> str` (read-only accessor for `settings.shopify_api_version`; used by subscriptions self-heal instead of reaching into the private attr); async `get_order(gid) -> Order | None`, `find_order_by_name(raw_name) -> Order | None`, `find_customer_orders_by_phone(phone_e164) -> list[Order]`, `search_products(query, limit=5) -> list[Product]` (Phase 4 Task 5 — active/in-stock-eligible grounded product search, backs `agents.product_search`/`agents.recommendations`), `add_tags(auth: AuthorizedOrder, tags) -> None`, `cancel_order(auth: AuthorizedOrder, *, reason="CUSTOMER", restock=True) -> CancelRequested`, `get_product_image_url(product_gid) -> str | None` (Q19, 2026-08-15 — best-effort featured-image fetch for the cod_confirmation header). Module: `ORDER_FIELDS`, `PRODUCT_FIELDS`, `_order_from_node`, `_product_from_node`, `_is_safe_product_query`.
- **Used in:** deps.Container, scripts/smoke_shopify, core (Phase 2+), agents.product_search / agents.recommendations (Phase 4, via the `ProductSource` Protocol).
- **Notes:** URL uses `settings.shopify_api_version` (2026-07). HTTP 401 → force_refresh + retry once → `ShopifyAuthError`. THROTTLED → `ShopifyThrottled`; errors+null data → `ShopifyGraphQLError`; partial data+errors → returns data. Mutations accept ONLY `AuthorizedOrder` (ADR-004); userErrors raise `ShopifyGraphQLError`. Direct order-by-phone is NOT supported by Shopify — customer→orders fallback used, returns `[]` on access-denied (read_customers scope may be absent). **`search_products(query, limit=5) -> list[Product]` (Phase 4 Task 5):** active, in-stock-eligible product search — queries `status:active` always AND-ed onto the caller's query so a draft/archived product can never surface to a customer; `Product(gid, title, handle, price: Money|None, available, product_type, tags)` (`app/shopify/models.py`). **`_is_safe_product_query(query) -> bool` (module function, security fix added during Task 5's review, not in the original brief):** rejects quotes, colons (field-prefix operators like `status:`/`title:`), `*` wildcards, standalone `AND`/`OR`/`NOT` boolean operators, and unbalanced parentheses before the string is interpolated into the Shopify search-query DSL — an unfiltered LLM-derived query string could otherwise inject Shopify search operators (e.g. `status:draft` to bypass the active-only constraint, or `OR`-inject to widen results); an unsafe/empty query short-circuits to `[]` rather than reaching Shopify at all. **`get_product_image_url(product_gid) -> str | None` (Q19, 2026-08-15):** fetches `product(id){featuredImage{url}}` for the confirmation-template header; fully isolated + best-effort (mirrors `get_order_fulfillments`) — any `ShopifyError` / image-less product → `None`, and only a public `https` URL is returned (Meta rejects non-https), so a Shopify hiccup never blocks the confirmation send.

## Container / deps (composition root)
- **File:** backend/app/deps.py
- **Purpose:** singleton wiring of the whole Shopify layer + the Phase 4 conversation engine's provider resolution.
- **Public API:** `Container` dataclass (settings, vault, config_repo, config, http, tokens, shopify, ingest, messages, `conversations: ConversationStore`); `get_container() -> Container` (module singleton); `reset_container() -> None` (tests); `build_provider(settings: Settings) -> LiteLLMProvider` (constructs the LLM verifier with a `VertexConfig` from `settings.vertex_*`); `async active_llm(settings: Settings, config: ConfigService) -> tuple[LLMProvider, str, str, dict[str, object] | None] | None` (Task 13).
- **Used in:** FastAPI app / routes, tests, admin.router (POST /provider verifier); Phase 4 conversation engine (router/agents call `active_llm` to get a ready-to-call provider).
- **Notes:** when `settings.database_url` is set → `PostgresConfigRepo` + `PostgresIngestStore` + `PostgresMessageStore` + `PostgresConversationStore` over ONE shared `LazyPool` (no connection made at build time — LazyPool connects on first acquire); else the four in-memory impls. `http = AsyncClient(follow_redirects=False)`. `build_provider` runs at call time (never at import) — `LiteLLMProvider` still imports litellm lazily inside `complete`, so nothing here touches the webhook cold path; it wires `VertexConfig(vertex_credentials_json or None, vertex_project or None, vertex_location)` so verification uses env creds. `active_llm` mirrors `admin.router`'s own resolution of `llm:active_provider`/`llm:api_key:{provider}` (GET /config, POST /provider): reads the active provider key via `config.get_plain`, looks it up in the provider registry, and for `auth_kind="env"` (Vertex) returns `(provider, default_model, "", request_params)` with no stored key required; for `auth_kind="api_key"` it requires `config.get_secret(f"llm:api_key:{active}")` to be present, else returns `None`. Returns `None` if nothing is active yet or an unknown provider key is stored. Built via `build_provider(settings)`, so every call constructs a fresh `LiteLLMProvider` (cheap — litellm import stays lazy inside `complete`).

## FastAPI app + GET /health + routers
- **File:** backend/app/main.py (app), backend/api/index.py (Vercel ASGI entrypoint)
- **Purpose:** deployable FastAPI app; liveness probe; mounts the Phase 2 routers.
- **Public API:** `app = FastAPI(...)`; `GET /health` → `{"status": "ok", "service": "thetavas-order-bot"}`. Includes `shopify_webhook_router`, `whatsapp_router`, `jobs_router`, and `admin_router`; registers slowapi (`app.state.limiter` + `RateLimitExceeded` handler), a global `RequestValidationError` handler (`_validation_handler`), `AdminBodyCapMiddleware` + `AdminSecurityHeadersMiddleware`, and mounts the static admin panel at `/admin/ui` (`StaticFiles(..., html=True)`).
- **Used in:** Vercel deploy (`vercel.json`, region bom1).
- **Notes:** entrypoint `api/index.py` re-exports `app`; all routes → `api/index.py`. Phase 1 security fix: OpenAPI/docs/redoc are DISABLED in prod — `_docs_enabled()` reads `Settings().app_env` (docs off when `app_env == "prod"`, default on otherwise / on missing key). Preserve this construction when adding routers. `AdminBodyCapMiddleware` must be added at module import (before first request) — it 413s oversized `/admin` requests by Content-Length before FastAPI parses the body. **Security-review fix (2026-07-30):** global `_validation_handler` for `RequestValidationError` returns 422 with each error dict stripped of `input`/`url`/`ctx` — the default handler echoes `input` verbatim, leaking over-length creds (api_key/client_secret/access_token/password) into the body. NOTE: FastAPI's `RequestValidationError.errors()` takes NO kwargs (unlike pydantic's `ValidationError.errors(include_input=...)`) — filter the dicts by hand. `AdminSecurityHeadersMiddleware` added LAST so it is outermost (headers on every `/admin` response incl. rejections).

## Jobs dispatcher (internal, authenticated)
- **File:** backend/app/jobs/router.py
- **Purpose:** single authenticated cron/self-invoke endpoint running a named-job registry.
- **Public API:** `router` — `POST /internal/jobs/{name}`; `JOBS: dict[str, JobFn]` registry; `JobFn = Callable[[Container], Awaitable[dict[str, Any]]]`. Registered: `ensure_subscription` (reads config `public_base_url`, calls subscriptions.ensure_subscription against `{base}/webhooks/shopify`); `retention_purge` (DPDP age-based purge, 2026-08-06); **`outbox_drain` (Phase 5 — Flow A send drain) and `reconcile_cancels` (Phase 5 — final cancelled tag), both 2026-08-10.** (The route is POST-only via `@router.api_route(..., methods=["POST"])`.)
- **Used in:** main.app, Vercel cron (future).
- **Notes:** `settings.cron_secret` empty/<16 chars → 503 (never an open endpoint, F11); header `X-Cron-Secret` constant-time compared → 403 on mismatch/missing; unknown job → 404; `ensure_subscription` with no `public_base_url` → 200 `{"error": "public_base_url not configured"}`. A job raising any `ShopifyError` (base class) → 502 `{"job": name, "error": "job failed"}` — exception text is NEVER echoed (may carry vendor detail); non-`ShopifyError` exceptions still propagate as raw 500.

## Retention purge job (DPDP, config-gated)
- **File:** backend/app/jobs/retention.py
- **Purpose:** scheduled-job-ready age-based purge across the erasure/retention tables (item 5).
- **Public API:** `async run_retention_purge(c: Container) -> dict[str, Any]`; registered as the `retention_purge` job.
- **Used in:** jobs.router (JOBS registry → `GET|POST /internal/jobs/retention_purge`, CRON_SECRET-authed).
- **Notes:** reads `retention_days` from `load_controls` (ADR-005 config). **DEFAULT 0 = disabled → `{"status": "disabled"}` no-op** (no policy invented until client answers Q15). When > 0: `cutoff = now(UTC) - retention_days days` → `c.ingest.purge_older_than(cutoff)` → `{"status": "purged", "retention_days", "deleted": {...per-table counts}}`. Cron wiring is live (safe: default is a no-op); flip on by setting `retention_days` from the panel once the client confirms the period.

## Rate limiter (slowapi)
- **File:** backend/app/ratelimit.py
- **Purpose:** shared slowapi `Limiter` used to rate-limit the admin login.
- **Public API:** `limiter: Limiter = Limiter(key_func=get_remote_address)`.
- **Used in:** admin.router (`@limiter.limit("5/minute")` on POST /admin/login), main.app (`app.state.limiter` + `RateLimitExceeded` → 429 handler). `limiter.reset()` clears counters (tests).
- **Notes:** NEW dep `slowapi>=0.1.9` (untyped → `[[tool.mypy.overrides]] module="slowapi.*"`). Keyed by remote address; per-process in-memory storage (fine for one Vercel instance; not distributed).

## Admin auth primitives
- **File:** backend/app/admin/auth.py
- **Purpose:** signed expiring session token + constant-time password check (cafe-verbatim, dependency-free).
- **Public API:** `issue_token(secret, now: datetime, ttl_hours=12) -> str` (format `<unix_exp>.<b64url_hmac_sha256>`; empty secret → `ValueError`); `verify_token(secret, token, now) -> bool` (False, never raises, on empty secret / malformed / bad sig / expiry); `check_password(supplied, expected) -> bool` (constant-time; empty `expected` → False, fail closed).
- **Used in:** admin.router (login issues, require_admin verifies).
- **Notes:** the session token is signed with `settings.app_master_key` (not `admin_password`) so rotating the display password doesn't need re-keying. `verify_token` fails closed on every error path.

## Admin JSON API router
- **File:** backend/app/admin/router.py
- **Purpose:** the whole `/admin/*` JSON API — auth, creds (Shopify/WhatsApp/LLM), knowledge, controls, read-only views.
- **Public API:** `admin_router: APIRouter` (prefix `/admin`); `require_admin(admin_session: str | None = Cookie())` dependency (401 unless the session cookie verifies); `AdminBodyCapMiddleware` (ASGI, 413 on oversized `/admin` bodies); `_audit(action, outcome, *, resource=None, detail=None)` actor-free audit helper (2026-08-06). Endpoints: `POST /login` (rate-limited 5/min, 503 if unset password, 401 on wrong, sets httponly `admin_session` cookie), `GET /session`, `GET|POST /shopify`, `GET|POST /whatsapp`, `GET /providers`, `GET /config`, `POST /provider`, `GET|PUT /knowledge/{kind}`, `GET|PUT /controls`, `GET /mappings`, `GET /outbox`, `POST /erasure` (DPDP delete-by-phone).
- **Used in:** main.app, static panel (`/admin/ui`).
- **Notes:** every route except `/login` depends on `require_admin`. **Audit logging (item 4, 2026-08-06):** `_audit` writes `admin_audit action=… [resource=…] outcome=…` at INFO on `logger("app.admin")` (lands in Vercel function logs, no new infra). Actor-free (single shared `ADMIN_PASSWORD` → no per-user identity); logs the action/resource-NAME/outcome ONLY, NEVER a value (passwords, keys, credential contents, knowledge bodies, or the erased phone). Wired at: login success/failure/not_configured; `credential_set` (resource `shopify`/`whatsapp`); `provider_set` (resource `llm:{provider}`, all 3 success paths); `knowledge_set` (resource `knowledge:{kind}`); `controls_set`; `erasure` (resource `phone`, never the number). **DPDP erasure (item 5, 2026-08-06):** `POST /erasure` `{phone: E.164 ^\+[0-9]{7,15}$}` (rate-limited `@limiter.limit("10/minute")`, needs `request: Request` like login) → `ingest.delete_by_phone` → `{ok, deleted:{order_mappings,outbound_messages,conversations,messages,pending_actions,order_actions}}`; a bad phone → 422 with `input` stripped by the global handler (PII never echoed). **Security-review fixes (2026-08-06):** (a) erasure audit resource is now `phone:{_phone_fingerprint(phone)}` — an HMAC-SHA256 of the number keyed with `settings.app_master_key` (the Fernet key REUSED as an HMAC key, no new secret), truncated to 16 hex chars: recomputable from a customer's number to correlate/prove compliance, not PII at rest, not reversible; the audit line also appends `deleted={sum of counts}` (blast radius without touching the response body). `_audit` gained an optional `detail=` suffix param for this. (b) E.164 pattern pinned to `[0-9]` (was `\d`, Unicode-aware → Arabic-Indic/fullwidth digits passed yet matched zero rows = false success + fake audit); the `ErasureRequest` body pattern stays `^\+[0-9]{7,15}$` (pydantic-core regex, not vulnerable to the Python `re` `$`-before-newline quirk). (c) `require_admin` takes `request: Request` and emits `_audit("authz","denied", resource=…)` before every 401. **ROUND 2 (2026-08-06):** the resource is now the matched route TEMPLATE via `_audit_resource(request)` (`request.scope["route"].path`, e.g. `/admin/knowledge/{kind}`) — round 1 logged the raw `request.url.path`, which uvicorn percent-decodes (`%0A`→literal newline in `scope["path"]`) BEFORE auth, letting an unauthenticated attacker split the log line and forge a fake `outcome=success` record (or flood a 4000+ char line). `_sanitize_path(raw)` (strip C0 controls + DEL via `_CONTROL_CHARS_RE`, truncate to `_MAX_RESOURCE_LEN=128`, before the logger) is the fallback used only when no route matched. Secrets are NEVER echoed — GET status routes return `{"configured": bool}` + non-secret fields only. Creds POSTs are partial-update (blank/omitted keeps the stored value; first-time setup requires the full set). `_clean(v)` trims blank→None. LLM `POST /provider` verifies with the provider BEFORE persisting (verify-key-before-save); it builds the verifier via `deps.build_provider(settings)` (Vertex env creds wired in). Provider verify is monkeypatchable at `app.admin.router.verify_key`. **Vertex/env-auth branch:** `GET /providers` returns `auth_kind` per provider; `set_provider` branches on `info.auth_kind` — `env` (Vertex) verifies with an empty key + `info.request_params` and, on success, sets `llm:active_provider` WITHOUT storing any key (on failure `_env_verify_detail(kind)` returns a SAFE message, never `raw_error`, which could embed the service-account JSON); `api_key` keeps the require-key + store-key path. **Env failure wording (code-review LOW 2026-08-04):** env-auth providers have their OWN message map `_ENV_KIND_MESSAGES` (AUTH/RATE_LIMIT/NOT_FOUND/TIMEOUT) that references "the service-account JSON, project, and location" — NOT "the API key" (Vertex has no key). `_env_verify_detail` maps a known kind via `_ENV_KIND_MESSAGES`, else (UNKNOWN/None) falls back to `_ENV_VERIFY_FAILED`; the api_key wording in `_KIND_MESSAGES` is used only by the api_key path. `GET /config` reports `configured=true` for an active env provider (no key needed) OR an api_key provider with a stored key. **Security-review fixes (2026-07-30):** (1) login cookie `secure` flag now comes from `settings.app_env == "prod"` (NOT the client-supplied `x-forwarded-proto`) — prod always forces Secure, dev/tests over http stay usable; (2) `PUT /knowledge/{kind}` maps pydantic `ValidationError` with `errors(include_url=False, include_input=False, include_context=False)` — a custom field_validator's raw `ValueError` in `ctx` was non-serializable → 500; (3) `WhatsAppCredsRequest` `phone_number_id`/`waba_id` are `pattern=r"^\d{5,20}$"`, `api_version` `pattern=r"^v\d+\.\d+$"` (path-like junk can't reach the Graph URL; still optional so partial update works); (4) `set_provider` `UNKNOWN` provider-error kind → generic "Could not verify the key with the provider." (added to `_KIND_MESSAGES`), never the raw litellm/vendor text.

## AdminBodyCapMiddleware
- **File:** backend/app/admin/router.py
- **Purpose:** cap `/admin` request bodies at the ASGI layer, before FastAPI parses the body — two rejections so the cap cannot be bypassed at the edge.
- **Public API:** `AdminBodyCapMiddleware(app, max_body=1_048_576)` (pure ASGI); registered via `app.add_middleware(AdminBodyCapMiddleware)`.
- **Used in:** main.app.
- **Notes:** a router-level dependency CANNOT do this — FastAPI parses (and may 422 on) the JSON body before path dependencies run, so an oversized invalid body would 422 before any cap check. Enforced on Content-Length + explicit no-length rejection at the ASGI layer, matching the webhook edges' raw-body posture: (1) `Content-Length` over the cap → **413**; (2) a body-bearing method (POST/PUT/PATCH) with NO `Content-Length` header — e.g. chunked transfer-encoding, which would otherwise skip the header check and reach a pre-auth route unbounded — → **411** (length required). Browsers/fetch always send Content-Length for JSON, so 411 rejects only unusual clients. Only inspects `/admin` paths.

## AdminSecurityHeadersMiddleware
- **File:** backend/app/admin/router.py
- **Purpose:** set security response headers on every `/admin` response (JSON API + static panel) — security-review fix (2026-07-30).
- **Public API:** `AdminSecurityHeadersMiddleware(app)` (pure ASGI); registered via `app.add_middleware(...)` LAST so it is the outermost middleware. Module tuple `_ADMIN_SECURITY_HEADERS`.
- **Used in:** main.app.
- **Notes:** sets `Content-Security-Policy: default-src 'self'; style-src 'self' 'unsafe-inline'; base-uri 'none'; frame-ancestors 'none'` (scripts stay `'self'` — external same-origin `admin.js` is allowed, inline `<style>`/`style="..."` covered by `style-src 'unsafe-inline'`, NO `'unsafe-inline'` for scripts) + `X-Frame-Options: DENY` + `X-Content-Type-Options: nosniff` + `Referrer-Policy: no-referrer` + `Cache-Control: no-store` (mappings/outbox views carry customer phone PII — must never be cached). Replaces any pre-existing header of the same name (dedupe) then appends, so `Cache-Control` can't double up. Only inspects `/admin` paths.

## Admin knowledge validation models
- **File:** backend/app/admin/knowledge_models.py
- **Purpose:** kind-specific Pydantic validation for knowledge PUTs; serialize to the cafe-loader-compatible stored format.
- **Public API:** `validate_and_serialize(kind, payload: dict[str, object]) -> str` (raises `pydantic.ValidationError` → router maps to 422; unknown kind → `KeyError`, guarded upstream). Models `BrandVoiceBody`, `FaqBody`/`FaqItem`, `PatternsBody`/`PatternItem`, `BusinessBody`.
- **Used in:** admin.router (PUT /knowledge/{kind}).
- **Notes:** faq/patterns store a JSON **list**, business a JSON **object**, brand_voice raw markdown — so `KnowledgeLoader` reads overrides and seeds identically. Size caps: brand_voice ≤100k chars; faq 1..200 items (q/a ≤2000); patterns 1..100 items (≤20 examples, **each example ≤200 chars** — security-review 2026-07-30, per-element cap so one giant example can't inflate the Phase-4 prompt); business fields ≤2000, extra dict ≤50 pairs (each key ≤200 / value ≤2000, `_cap_extra_entry_lengths`).

## Admin operational controls
- **File:** backend/app/admin/controls.py
- **Purpose:** ADR-002 (send-mode kill switch) + ADR-005 (client decisions as config) document; each field persists as its own plain `app_config` key so runtime readers keep their existing keys.
- **Public API:** `AdminControls` (Pydantic) + `TagLists`; `async load_controls(config) -> AdminControls` (stored-or-default, never crashes on corrupt values); `async save_controls(config, controls) -> None`; `REVEAL_ALLOWED`. **`TagLists` fields: `pending`, `confirmed`, `cancelled`, and (Phase 5, 2026-08-10) `cancel_requested` (DEFAULT `["bot-cancel-requested"]`)** — the provisional tag written when a cancel is REQUESTED, before Shopify confirms cancelledAt; migration-safe (an older stored `tags` JSON without the key validates via the field default).
- **Used in:** admin.router (GET|PUT /controls); the individual keys are read by webhook eligibility / jobs / the Phase 5 outbox drain (`send_mode`, `allowlist_phones`) and button dispatch + reconcile (`tags.{confirmed,cancel_requested,cancelled}`, `default_language`).
- **Notes:** validated: `send_mode` ∈ off/shadow/allowlist/live; `push_policy` ∈ cod_only/all/all_prepaid_no_buttons; `default_language` ∈ en/hi/gu; `reveal_fields` ⊆ {order_number,email,status}; `allowlist_phones`/`owner_alert_number` E.164; `public_base_url` https-or-empty (trailing slash stripped); `push_staleness_hours` 1..168; **`vip_order_count_threshold` 1..100, DEFAULT 3** (Phase 4 Task 4, 2026-08-06 — order-COUNT only, not spend: `order_mappings` has no amount column and `total_spend` would need a live per-lookup Shopify call, out of scope; read by `core.conversation` via `ingest.count_orders_by_phone` to set `AgentContext.is_vip`); **`retention_days` 0..3650, DEFAULT 0 = disabled** (DPDP retention window, 2026-08-06 — 0 invents no policy; exact period is pending client decision Q15; persisted like `push_staleness_hours` via the `_INT_KEYS` int-string path). **Security-review fix (2026-08-06, ROUND 2 — supersedes round 1):** the `_INT_KEYS` load path now requires `raw.isascii() and raw.isdigit()` BEFORE `int()` (round 1's `try:int()` closed the `"²"` crash but `int("٣٠")==30` still enabled an unintended retention period — a bare int() is Unicode-permissive); `_E164_RE` pinned to `^\+[0-9]{7,15}\Z` (round 1 used `[0-9]` but kept `$`, which matches before a trailing newline — `\Z` rejects `"+91…\n"`) so allowlist_phones/owner_alert_number reject Arabic-Indic/fullwidth digits and trailing newlines. **each tag ≤64 chars (`_Tag` Annotated cap — security-review 2026-07-30; Shopify caps tags at 255 so an over-long tag would fail in tagsAdd)**. EXISTING keys reused unchanged: `push_policy`, `push_staleness_hours`, `public_base_url`. New keys: `send_mode`, `allowlist_phones`, `reveal_fields`, `tags`, `default_language`, `owner_alert_number`. **`load_controls` per-field fail-safe (security-review 2026-08-06 ROUND 2 — supersedes the round-1 whole-doc fallback):** validation goes through `_validate_per_field(data)` — it validates the assembled dict and, on a `ValidationError`, drops only the top-level keys pydantic reports invalid and re-validates, so ONE corrupt field (e.g. an out-of-range `retention_days`) defaults only itself instead of resetting the whole document (round 1 fell back to `AdminControls()` on any error, which silently re-widened a deliberately-narrowed `reveal_fields` back to the PII-exposing default).

## KnowledgeLoader + Thetavas seeds
- **File:** backend/app/knowledge/loader.py (+ backend/app/knowledge/seeds/{brand_voice.md,faq.json,business.json,patterns.json})
- **Purpose:** override-else-seed reader for the store's voice + policy knowledge (cafe pattern, our names).
- **Public API:** `KnowledgeLoader(repo: ConfigRepo, seeds_dir: Path)` with `get(kind) -> str`, `knowledge_version() -> str` (config `knowledge_version` or "0"), `assemble_all() -> dict[str, str]`; module `KINDS = ("brand_voice","faq","business","patterns")`, `SEEDS_DIR: Path`.
- **Used in:** admin.router (knowledge GET); Phase 4 engine (prompt assembly) will consume it.
- **Notes:** DB override (from `knowledge_overrides`) wins; else the packaged seed FILE. Seeds are Thetavas defaults (no emojis, plain text, no menu — we don't sell in chat); business support fields are blank for the store team to fill from the panel. `knowledge_version` bumps on every PUT (Phase 4 cache invalidation).

## Providers layer (LLM behind LLMProvider)
- **File:** backend/app/providers/{base.py,registry.py,litellm_provider.py,verify.py}
- **Purpose:** minimal Phase 3.5 LLM provider abstraction — enough to verify a provider's credentials before saving; full engine use is Phase 4.
- **Public API:** base: `LLMProvider` Protocol (`complete(model, messages, api_key, timeout, *, extra_params=None) -> CompletionResult`), `Message`, `CompletionResult`, `ProviderError(.kind)`, `ProviderErrorKind(StrEnum: AUTH/RATE_LIMIT/NOT_FOUND/TIMEOUT/UNKNOWN)`. registry: `AuthKind = Literal["api_key","env"]`; `ProviderInfo(key,label,default_model,accept_on_rate_limit=False,auth_kind: AuthKind="api_key",request_params=None)`, `PROVIDERS` (`gemini` api_key + `vertex` env), `get_provider(key)`, `list_providers()`. litellm_provider: frozen `VertexConfig(credentials_json,project,location)`; `LiteLLMProvider(vertex: VertexConfig|None=None)` (lazy `import litellm` inside `complete`; api_key scrubbed from api_key-provider errors, vertex errors collapsed to a fixed message). verify: `VerifyResult(ok,error,kind)`, `async verify_key(provider, model, api_key, timeout=15.0, *, extra_params=None) -> VerifyResult` (never raises, never leaks the key/creds).
- **Used in:** admin.router (GET /providers, GET /config, POST /provider verify-before-save), deps.build_provider.
- **Notes:** NEW dep `litellm>=1.89,<2` (untyped → `[[tool.mypy.overrides]] module="litellm.*"`), imported LAZILY so the webhook cold path never pays its import cost (rule F10). `ProviderErrorKind` is a `StrEnum` (repo ruff UP042). **Two providers:** `gemini` (`auth_kind="api_key"`, direct key pasted+stored encrypted) and `vertex` ("Gemini (Vertex AI)", model `vertex_ai/gemini-3.5-flash`, `auth_kind="env"`, `request_params={"temperature":0.3,"reasoning_effort":"medium"}`). `ProviderInfo` with a dict `request_params` is unhashable — fine, only stored as dict values. **LiteLLMProvider.complete** branches on the model: `vertex_ai/*` injects `vertex_credentials/vertex_project/vertex_location` (from `VertexConfig`) and NO `api_key` — raises `ProviderError(AUTH, "Vertex AI credentials are not configured")` if creds missing; all other models inject `api_key` as before. **Error handling (security-review LOW-1 2026-08-04):** a `vertex_ai/*` upstream error collapses to a FIXED `ProviderError("Vertex AI request failed", kind)` — the raw litellm text is DISCARDED entirely (exact-substring redaction can't catch a reformatted/re-serialized copy or a lone `private_key` field of the service-account JSON). Only the api_key path still uses `_redact(str(exc), api_key)` (now a 2-arg helper — `model`/`vertex` params removed). `verify_key`'s `extra_params` forwards a provider's `request_params` so env providers verify with their tuned params. `ProviderInfo.auth_kind` is `Literal["api_key","env"]` (`AuthKind`), not bare `str`.

## send-policy decision helper (kill switch) — Phase 5
- **File:** backend/app/core/send_policy.py
- **Purpose:** pure ADR-002 kill-switch decision — map `send_mode` + allowlist to send/suppress for one phone.
- **Public API:** `send_decision(send_mode: str, allowlist: list[str], phone: str) -> str` → `"send" | "suppress"`; never raises.
- **Used in:** `jobs.outbox_drain` (per-row send/suppress decision).
- **Notes:** `live` → send; `allowlist` → send iff `normalize_phone(phone)` ∈ the normalized allowlist else suppress; `off`/`shadow`/**any unknown or corrupt mode** → suppress (fail safe). Depends only on `core.phone.normalize_phone` (pure core, no adapters). The drain returns before looping when `send_mode=="off"`, but this helper still maps `off`→suppress so it is safe to call in every mode.

## Outbox drain job (Flow A) — Phase 5
- **File:** backend/app/jobs/outbox_drain.py
- **Purpose:** drain the queued `outbound_messages` rows — send each `cod_confirmation`/`prepaid_order` template (2026-08-16: routed by payment type, was `cod_confirmation` only; Q19, 2026-08-15, was `order_confirmation_cod`) with the deterministic confirm/cancel button payloads (COD only). The WATI-replacement outbound push.
- **Public API:** `async run_outbox_drain(c: Container) -> dict[str, object]` (the drain loop — now the **BACKSTOP** send path since the 2026-08-15 inline-send amendment; still driven by the 1-minute Vercel Cron `GET /internal/jobs/outbox_drain` if a scheduler is configured, and runnable manually via `POST` + `X-Cron-Secret`); `async send_one_outbound(c: Container, cfg: WhatsAppConfig, controls: AdminControls, row: OutboundClaim, timeout: float | None = None) -> str` (**shared per-row state machine**, returns an outcome constant `OUTCOME_{SENT,SUPPRESSED,UNDELIVERABLE,FAILED,RETRY}`; **`timeout` added 2026-08-15** — overrides the WhatsApp call's per-request timeout, None keeps `send_template`'s 20s default so the cron path is unchanged, the inline path passes the short `_INLINE_SEND_TIMEOUT_SECONDS`); **`async send_inline_outbound(c: Container, outbound_id: int | None) -> str | None` (2026-08-15, the PRIMARY send trigger)** — claims exactly the just-queued row (`claim_outbound_by_id`) and runs `send_one_outbound` with the short timeout; mirrors `run_outbox_drain`'s guards (`send_mode == 'off'` → leave queued & return None; unconfigured/lost-claim/no-row → None); NEVER raises (outer `except Exception: log+None`) so it can't 5xx the Shopify ack. Module helpers `_gid_from_dedupe_key` (**2026-08-14: now strips EITHER prefix in `_DEDUPE_PREFIXES = ("order_created:", "order_reminder:")`** — a Q17 reminder row carries the same gid and sends the same template + `order:confirm/cancel:{gid}` buttons as the original push, so the drain treats them identically; only the dedupe_key prefix differs), `_parse_payload` (+ `_TemplatePayload`), `_meta_error_code`; constants `_UNDELIVERABLE_CODES = {131026,131047,131049}`, `_META_CODE_RE = re.compile(r"(?:^|; )code=(\d+)")` (Phase-5 review fix 2026-08-10 — anchored to the TOP-LEVEL `code=` field, which `_safe_error` renders first or after `"; "`, so a nested `error_subcode=131026` is no longer misread as an undeliverable send code → that send retries instead of terminating), `_CLAIM_LIMIT = 5` (2026-08-13 security fix — cut 25→5 so a run fits any per-invocation budget given `send_template`'s 20s timeout; 5/min = 7200/day ≫ the 100-500 orders/day target, a leftover row waits for the next 1-minute tick), **`_INLINE_SEND_TIMEOUT_SECONDS = 3.0`** (2026-08-15 — the inline send's short WhatsApp-call timeout, distinct from the 20s cron default, keeps the webhook handler under Shopify's <5s ack budget).
- **Used in:** jobs.router (JOBS registry → `GET|POST /internal/jobs/outbox_drain`, CRON_SECRET-authed) as the BACKSTOP drain; **and `channels.shopify_webhook` calls `send_inline_outbound` inline as the PRIMARY send (2026-08-15 ADR-001 amendment)**. The webhook now `await`s the send before its ack (a genuine bounded `await`, NOT `BackgroundTasks`/`create_task` — those don't reliably run after the response on Vercel Python; the 2026-08-13 self-invoke experiment used BackgroundTasks and was reverted). Safe because: the ack is delayed only by the short `_INLINE_SEND_TIMEOUT_SECONDS`, the inline path claims ONLY its own row by id (never the shared drain queue), and it never raises. `send_inline_outbound` lives in this job module (not the channels edge) so `channels/` stays free of `admin.controls` imports — same layering discipline as the Phase-4 `core.conversation` extraction.
- **Notes:** **kill switch in ONE place (ADR-002):** `send_mode=="off"` returns `{"drained":0,"sent":0,"suppressed":0,"reason":"send_mode off"}` BEFORE touching the queue; every other mode is decided per-row by `send_policy.send_decision` (inside `send_one_outbound`). `load_whatsapp_config` None → `{"drained":0,"error":"whatsapp not configured"}`. Otherwise `claim_queued_outbound(_CLAIM_LIMIT)` **atomically** claims rows (`queued -> processing`, `FOR UPDATE SKIP LOCKED` so two overlapping cron ticks never both claim one row — 2026-08-13 fix) and runs `send_one_outbound` per row. **The claim predicate also RECLAIMS stale in-flight rows** (2026-08-13 security fix): `WHERE state = 'queued' OR (state = 'processing' AND updated_at < now() - interval '10 minutes')` — a row a killed invocation left in `processing` (nothing else re-queues it: `bump` only fires on a reached send, `purge_older_than` excludes `outbound_messages`) is recovered after 10 minutes; that threshold ≫ the 20s send timeout so a genuinely in-flight row is never stolen. The in-memory store mirrors this (`_OutboundRow.updated_at`, stamped on write + claim). **`send_one_outbound` per-row** (each row is already `processing` when it arrives — the function never re-reads/assumes `queued`): `_gid_from_dedupe_key('order_created:{gid}')` (strips the prefix WHOLE — the gid contains `:`; a non-`gid://` remainder → terminal fail via `bump_outbound_attempt(max_attempts=1)`, `OUTCOME_FAILED`); suppress-decision → `mark_outbound_suppressed` (`OUTCOME_SUPPRESSED`); bad payload_json → `mark_outbound_undeliverable(id,"bad_payload")`; else `send_template(..., body_params={customer_name, order_id, product_name, product_color, product_size, product_amount}, button_payloads=buttons, header_image_url=payload.image_url)` — **Q19 (2026-08-15): NAMED body params for the `cod_confirmation`/`prepaid_order` templates + optional live product-photo header.** **2026-08-16 (order-type routing):** `buttons = ["order:confirm:{gid}","order:cancel:{gid}"] if payload.template == "cod_confirmation" else []` — `prepaid_order` has no BUTTONS component approved on the WABA (informational only, no Confirm/Cancel step for a prepaid customer, and no `confirmed`/`cancel_requested` tag can ever follow it since there's no button to tap); everything else about the send (body params, header image, retry) is identical for both templates. `_TemplatePayload`/`_parse_payload` now require the 6 named params (`_REQUIRED_PAYLOAD_KEYS`) + `template`/`language`; `image_url` is OPTIONAL and only kept if a public https link (else no header — Q19a graceful degradation). A legacy row still queued under the OLD shape (`order_name`/`amount`, no product fields) fails parse → terminal `undeliverable` (the old template no longer exists on the WABA anyway). `r.ok` → `mark_outbound_sent(id,wamid)` (`processing -> sent`) + `set_mapping_status(gid,"template_sent")` (`OUTCOME_SENT`). **Undeliverable vs retryable (⚠ deviation from the plan's literal `r.status_code in _UNDELIVERABLE_CODES`):** those are META error CODES (in the body), not the HTTP status (always 400 for all of them), so the code is extracted from `SendResult.error` (`whatsapp_sender._safe_error` renders `code=<n>`); a code in `_UNDELIVERABLE_CODES` → `mark_outbound_undeliverable` (`processing -> undeliverable`, `OUTCOME_UNDELIVERABLE`), else `bump_outbound_attempt(str(status_code))` (moves `processing -> queued` so a LATER cron run re-claims + retries, or `-> failed` at the attempt cap → `OUTCOME_FAILED`, else `OUTCOME_RETRY` — **never stuck in `processing`**; retry-across-runs is required now that the cron is the sole one-per-tick sender). `WhatsAppSendError` (transport) → `bump_outbound_attempt` (`processing -> queued`) + `OUTCOME_RETRY` (logged as `type(exc).__name__` only, never the rendered traceback/args) — one poison row never aborts the drain. NOTHING here mutates Shopify (send only). `run_outbox_drain` maps each outcome to its `{drained,sent,suppressed,failed,undeliverable}` counter (`RETRY` counts toward none).

## Order-confirmation reminder sweep (Flow A, Q17) — 2026-08-14
- **File:** backend/app/jobs/reminders.py
- **Purpose:** if a customer got the `cod_confirmation` template and did not tap Confirm/Cancel within 1 hour, resend the EXACT same template ONCE as a reminder, never again for that order (owner-directed, Q17). Reuses the original push's payload_json verbatim (`replace(original, dedupe_key=...)`), so it inherits the Q19 named-param + header-image shape automatically — no reminder-side change needed.
- **Public API:** `async run_send_reminders(c: Container) -> dict[str, object]` → `{"swept": N, "queued": M}`. Constants `_REMINDER_THRESHOLD_MINUTES = 60`, `_REMINDER_CEILING_MINUTES = 24*60` (2026-08-14 security fix — see below).
- **Used in:** jobs.router (JOBS registry → `GET|POST /internal/jobs/send_reminders`, CRON_SECRET-authed) + a NEW Vercel Cron `*/5 * * * *` in `backend/vercel.json` (a 1-hour threshold needs no minute-level sweep; keeps invocation count low).
- **Notes:** reuses the EXISTING outbox pipeline, no parallel machinery. Per eligible mapping from `find_stale_template_sent(older_than_minutes=60, max_age_minutes=24*60)`: look up the original `order_created:{gid}` payload — **batched** across all swept gids in ONE `find_outbound_by_dedupe_keys(list) -> dict` round-trip (2026-08-14 fix, was a per-row `find_outbound_by_dedupe_key` N+1 on the shared pool) — skip if gone (never fabricate), `dataclasses.replace` its `dedupe_key` with `order_reminder:{gid}`, and `enqueue_outbound` it. **⚠ 2026-08-14 security fixes (2 HIGH + 1 MEDIUM):** the sweep window is anchored on `order_mappings.updated_at` (the moment the drain stamped `template_sent` via `set_mapping_status`), NOT `created_at` (webhook-ingest time) — else a backlog flushed hours after ingest would be reminded the instant its template sends (HIGH#1). Bounded on BOTH ends: `updated_at <= now()-60min AND updated_at > now()-24h`, so a historically-unanswered mapping (kept indefinitely, Q15) is never mass-reminded on first run (HIGH#2). The `status='template_sent'` filter keeps `updated_at`-anchoring correct: any tap moves status OFF `template_sent`, so a `template_sent` row's most-recent `updated_at` IS its send (or a duplicate-ingest) time — a duplicate ingest only DELAYS a reminder, never duplicates one. Query is `ORDER BY updated_at LIMIT 25` (was unbounded) + index `idx_order_mappings_status_updated (status, updated_at)` in schema.sql (MEDIUM#1). **Exactly-once = the UNIQUE `dedupe_key`** (`ON CONFLICT DO NOTHING`), so the sweep can run every tick/overlap and still queue at most one reminder per order — no "already reminded" flag (proven by a twice-run test: `swept=1, queued=0` the second time). This job only QUEUES; the reminder row then flows through the UNCHANGED `claim_queued_outbound -> send_one_outbound` drain (which required only widening `_gid_from_dedupe_key` to accept the reminder prefix), so the `send_mode` kill switch stays enforced in ONE place (the drain), not here. **Natural cutoff:** a Confirm/Cancel tap before the 1-hour mark moves the mapping off `status='template_sent'`, auto-excluding it from the sweep. **⚠ Known long-term-growth note:** the reminder SEND leaves the mapping at `template_sent` (only a button tap moves it off), so an order that is never tapped is re-swept every tick forever (queuing nothing new — dedupe holds); acceptable at the v1 100-500 orders/day scale and inherent to the owner's chosen design, but flagged for a future `template_sent`-terminal-state or reminded-flag optimization if the untapped set grows unbounded.

## Cancel reconciliation job — Phase 5
- **File:** backend/app/jobs/reconcile.py
- **Purpose:** apply the FINAL `cancelled` tag once Shopify confirms an order is actually cancelled (`orderCancel` is async), and — **2026-08-16, new capability** — notify the customer with the `cod_cancel` template once that happens (this job sent nothing before this feature).
- **Public API:** `async run_reconcile_cancels(c: Container) -> dict[str, Any]`; registered as the `reconcile_cancels` job. `_RECONCILE_LIMIT = 50`. Module helpers: `async _notify_cancelled(c, cfg, controls, auth, gid) -> Literal["notified","skipped"]` (best-effort `cod_cancel` send — kill-switch gated, "skipped" for every non-send branch: no deliverable phone, suppressed, >=400 Meta response, or transport failure); `async _release_claim(c, gid) -> None` (reverts a claimed order back to `cancel_requested` on any skip/failure so the next run retries it; a store-layer error here is logged and swallowed, never aborts the batch). `_CANCEL_TEMPLATE = "cod_cancel"`, `_CANCEL_TEMPLATE_LANGUAGE = "en"` (Meta-approved in `en` ONLY, same as `cod_confirmmsg`/`cod_confirmation`/`prepaid_order`, Q19c — pinned regardless of the order's detected language).
- **Used in:** jobs.router (JOBS registry → `POST /internal/jobs/reconcile_cancels`, CRON_SECRET-authed).
- **Notes:** **Claim state machine (three states, 2026-08-16 — was a plain status filter with no claim):** a mapping sits at `cancel_requested` until a run atomically CLAIMS it via `ingest.orders_awaiting_cancel_reconcile(50)` into the transient `cancel_reconciling` status (`FOR UPDATE SKIP LOCKED` + a 10-minute stale-reclaim — see the `IngestStore` entry), so two overlapping runs never double-process the same order and `cod_cancel` is never double-sent from that race. Every claimed gid is finalized within the run: advanced to `cancelled` on success, or RELEASED back to `cancel_requested` via `_release_claim` on any skip/failure (missing order, not yet cancelled per Shopify, no phone to authorize, or a `ShopifyError`) so the row is not lost. **Per-gid flow:** re-fetch live via `c.shopify.get_order(gid)`; only when `order.is_cancelled()` (cancelledAt set) → `authorize_own_order(order)` (constructs the `AuthorizedOrder` from the order's own best phone INSIDE `core.order_resolver`, so ADR-004's "only order_resolver builds it" holds) → `add_tags(auth, controls.tags.cancelled)` + `set_mapping_status(gid,"cancelled")` + `record_order_action(gid,"cancelled","system",None,"ok",None)`, THEN (if the WhatsApp config loaded) `_notify_cancelled`. This keeps a false `cancelled` tag from ever being written before Shopify actually cancels (ADR-004 #3). **`_notify_cancelled`'s phone resolution:** prefers `ingest.get_mapping_phone(gid)` (the order's actual conversation phone, already E.164) over `auth.verified_phone` normalized via `core.phone.normalize_phone` — `auth.verified_phone` is a RAW Shopify string (not guaranteed E.164), so sending it straight to Meta risked a bare 10-digit number resolving under the wrong country code and delivering the customer's name + order number to an unrelated person (found + fixed in review, commit `584b4af`); neither resolving → skip (gid-only log, no PII). **Kill-switch gating (new for this job, 2026-08-16):** `send_decision(controls.send_mode, controls.allowlist_phones, to)` is checked before every `cod_cancel` send — `suppress` skips the notification but the tag/status write and `reconciled` count are UNAFFECTED (the kill switch affects notification only, never the mutation). A missing/corrupt WhatsApp config (`load_whatsapp_config` raising `VaultError`) is caught ONCE per run (not per-order) and disables notifications for the whole run without affecting reconciliation. A `>=400` Meta response (`result.ok is False`) and a `WhatsAppSendError` (transport) are both logged (`result.error` already token-redacted) and degrade to `"skipped"` — never rolls back the tag/status write, never aborts the batch (added 2026-08-16, commit `584b4af`, "log send failures" — a failing send was previously silently swallowed with no visibility). Returns `{checked,reconciled,pending,skipped,notified,notify_skipped}` (the last two counters new 2026-08-16). **⚠ Documented, accepted residual risk (not a TODO):** the success path writes the tag BEFORE it advances the status (`add_tags` → `set_mapping_status("cancelled")` → notify) — a crash between a successful `add_tags` and `set_mapping_status` leaves the row stuck at `cancel_reconciling`; the 10-minute stale-reclaim then re-processes it, re-applying `add_tags` (idempotent, harmless) but possibly re-sending `cod_cancel` (a POSSIBLE duplicate customer notification). Bounded in practice: a Vercel invocation times out far short of 10 minutes, so a genuinely still-running process can never collide with its own stale-reclaimed row (both a code review and an independent security review judged it practically closed; reordering the writes is explicitly out of scope). **⚠ Not yet verified against real Postgres:** the atomic-claim concurrency test `backend/tests/store/test_reconcile_claim_pg.py` is gated on `TEST_DATABASE_URL`, which was UNSET in the review environment — the `FOR UPDATE SKIP LOCKED` claim behavior itself has not been proven against a live Postgres instance yet; flagged as a pre-cron-wiring verification step (see `_pipeline_status.md`).

## Deterministic button-tap dispatch (confirm/cancel mutation core) — Phase 5
- **File:** backend/app/core/order_actions.py
- **Purpose:** the Flow B mutation-safety core — handle a confirm/cancel button tap deterministically (NO LLM), re-fetching + ownership-checking live before any `tagsAdd`/`orderCancel`, with two-phase cancel. Never raises.
- **Public API:** `async dispatch_button(c: Container, event: InboundButton | InboundInteractive) -> None`. Module helpers: `_parse_payload` (exact-prefix → `('confirm'|'cancel'|'cancel_confirm'|'cancel_abort', gid)`; two-token cancel prefixes matched first; gid must be `gid://...`), `_is_dispatched`, `_has_any_tag`, `_safe_send_text`/`_safe_send_buttons` (swallow `WhatsAppSendError`), **`async _safe_send_template(c, cfg, to, template_name, body_params: Sequence[str]) -> None`** (2026-08-16 — mirrors `_safe_send_text`/`_safe_send_buttons`: best-effort `send_template(..., _CONFIRM_TEMPLATE_LANGUAGE, body_params)`, swallows `WhatsAppSendError`, never raises), `_shopify_error_detail` (secret-free audit detail: a `ShopifyGraphQLError`'s structured messages/codes JSON, else None). `Event = InboundButton | InboundInteractive`; `_CANCELLABLE_FULFILLMENT = {None,"UNFULFILLED"}`; `_MUTATION_AUDIT_ACTION = {"confirm":"confirm","cancel_confirm":"cancel_requested"}`; `_COD_CONFIRMMSG_TEMPLATE = "cod_confirmmsg"`, `_CONFIRM_TEMPLATE_LANGUAGE = "en"` (2026-08-16).
- **Used in:** `channels.whatsapp.receive_webhook` (the sole caller; gated on the send-mode kill switch there too — defense in depth).
- **Notes:** **Kill switch is enforced HERE too (Phase-5 review fix 2026-08-10, the SINGLE source of truth, mirroring the drain + inbound path):** the FIRST thing `dispatch_button` does after `load_controls` is `send_decision(send_mode, allowlist_phones, event.wa_id)`; `suppress` (shadow / allowlist-miss / off / unknown) → return immediately with NO resolve, NO `tagsAdd`/`orderCancel`, NO reply (a shadow/allowlisted-out tap has no live effect, consistent with the drain suppressing those numbers' templates). Only `send` (live / allowlist-hit) proceeds. The webhook keeps its own off-gate. **ADR-004 mutation safety, proven by tests:** every branch calls `resolve_by_gid(c.shopify, event.wa_id, gid)` (the ONLY `AuthorizedOrder` constructor) BEFORE any mutation; a non-owner or unknown gid → `copy_for("not_found")` refusal, NO mutation, NO order detail leaked (non-enumerable). Language = `choose_language(order.customer_locale, controls.default_language)`. **confirm:** cancelled→already_cancelled (plain text, unchanged). **2026-08-16: the success reply is now the `cod_confirmmsg` template, not plain text** — `confirm_params = [customer_display_name(order), order.name]` (positional `{{1}}` name / `{{2}}` order number, pinned to `en` regardless of the order's detected language — same Q19c en-only situation as `cod_confirmation`/`prepaid_order`). Confirmed-tag present→idempotent re-tap: NO 2nd `add_tags`, but still sends `cod_confirmmsg` via `_safe_send_template` (the template's wording — "your order has been confirmed successfully" — is accurate on a re-tap too, so both branches converge on the same send). Else `add_tags(confirmed)` + `record_order_action("confirm","ok")` + `set_mapping_status("confirmed")` + `_safe_send_template(..., _COD_CONFIRMMSG_TEMPLATE, confirm_params)`. The reply target is `event.wa_id` (the raw Meta sender id, no leading `+`), same as every other reply in this file — NOT the order's E.164. The old `confirm_success`/`already_confirmed` `copy_for` keys are no longer sent from this handler (still exist in `copy.py` for other/legacy use, not deleted). **cancel (1st tap):** cancelled→already_cancelled; `_is_dispatched`→cancel_too_late (NO mutation); else `send_buttons("are you sure?", [order:cancel:confirm:{gid}, order:cancel:abort:{gid}])` — cancel_order is NEVER called on the first tap. **cancel:confirm:** cancelled→already_cancelled; dispatched-now→cancel_too_late; **provisional cancel_requested tag already present → cancel_requested reply, NO 2nd orderCancel**; else **advance state FIRST (provisional `add_tags(cancel_requested)` best-effort + `set_mapping_status("cancel_requested")`) THEN `cancel_order(auth)`** — Phase-5 review fix 2026-08-10 (LOW): writing the provisional marker before the async orderCancel makes a racing second tap's live re-fetch see it and refuse a duplicate cancel; on success `record_order_action("cancel_requested","ok")` + cancel_requested reply; a `ShopifyGraphQLError` (userErrors) → `record_order_action("cancel_requested","error",json)` + cancel_failed (the order stays cancel_requested — reconcile leaves it alone until cancelledAt appears). **cancel:abort:** cancel_kept, no mutation. Garbage/foreign payload → error_fallback, resolve not even attempted. **Audit symmetry (Phase-5 review fix 2026-08-10, MEDIUM):** the outer `except ShopifyError` now records an `order_actions` "error" row for the ATTEMPTED mutation (confirm → `"confirm"`, cancel_confirm → `"cancel_requested"`, via `_MUTATION_AUDIT_ACTION`) covering ALL ShopifyError subtypes (GraphQL + transport) BEFORE the error_fallback reply — so a FAILED add_tags/transport cancel leaves a row, not just successes; the cancel handler's own GraphQL branch records-and-returns so a GraphQL cancel never double-writes. `except ShopifyError`/`except Exception` → error_fallback send, never raises (webhook still acks 200). **⚠ deviation:** `_is_dispatched` uses the CONSERVATIVE inverse (`fulfillment_status not in {None,"UNFULFILLED"}` = dispatched) rather than the plan's literal positive set, to stay consistent with the Phase 4 order_tracking agent's own eligibility promise and because a false-refuse (route to support) is safe while a false-cancel of a shipped order is not.
